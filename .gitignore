# Per-install / runtime — never commit, generated locally on first launch.
secret.key          # per-host JWT secret, generated by wyld-local on first run
saves/              # local player accounts + encrypted save files

# OS cruft
.DS_Store
**/.DS_Store
