// Generated by Haxe 4.3.6
(function ($global) { "use strict";
var $estr = function() { return js_Boot.__string_rec(this,''); },$hxEnums = $hxEnums || {},$_;
class DateTools {
	static __format_get(d,e) {
		switch(e) {
		case "%":
			return "%";
		case "A":
			return DateTools.DAY_NAMES[d.getDay()];
		case "B":
			return DateTools.MONTH_NAMES[d.getMonth()];
		case "C":
			return StringTools.lpad(Std.string(d.getFullYear() / 100 | 0),"0",2);
		case "D":
			return DateTools.__format(d,"%m/%d/%y");
		case "F":
			return DateTools.__format(d,"%Y-%m-%d");
		case "M":
			return StringTools.lpad(Std.string(d.getMinutes()),"0",2);
		case "R":
			return DateTools.__format(d,"%H:%M");
		case "S":
			return StringTools.lpad(Std.string(d.getSeconds()),"0",2);
		case "T":
			return DateTools.__format(d,"%H:%M:%S");
		case "Y":
			return Std.string(d.getFullYear());
		case "a":
			return DateTools.DAY_SHORT_NAMES[d.getDay()];
		case "d":
			return StringTools.lpad(Std.string(d.getDate()),"0",2);
		case "e":
			return Std.string(d.getDate());
		case "b":case "h":
			return DateTools.MONTH_SHORT_NAMES[d.getMonth()];
		case "H":case "k":
			return StringTools.lpad(Std.string(d.getHours()),e == "H" ? "0" : " ",2);
		case "I":case "l":
			let hour = d.getHours() % 12;
			return StringTools.lpad(Std.string(hour == 0 ? 12 : hour),e == "I" ? "0" : " ",2);
		case "m":
			return StringTools.lpad(Std.string(d.getMonth() + 1),"0",2);
		case "n":
			return "\n";
		case "p":
			if(d.getHours() > 11) {
				return "PM";
			} else {
				return "AM";
			}
			break;
		case "r":
			return DateTools.__format(d,"%I:%M:%S %p");
		case "s":
			return Std.string(d.getTime() / 1000 | 0);
		case "t":
			return "\t";
		case "u":
			let t = d.getDay();
			if(t == 0) {
				return "7";
			} else if(t == null) {
				return "null";
			} else {
				return "" + t;
			}
			break;
		case "w":
			return Std.string(d.getDay());
		case "y":
			return StringTools.lpad(Std.string(d.getFullYear() % 100),"0",2);
		default:
			throw new haxe_exceptions_NotImplementedException("Date.format %" + e + "- not implemented yet.",null,{ fileName : "DateTools.hx", lineNumber : 101, className : "DateTools", methodName : "__format_get"});
		}
	}
	static __format(d,f) {
		let r_b = "";
		let p = 0;
		while(true) {
			let np = f.indexOf("%",p);
			if(np < 0) {
				break;
			}
			let len = np - p;
			r_b += len == null ? HxOverrides.substr(f,p,null) : HxOverrides.substr(f,p,len);
			r_b += Std.string(DateTools.__format_get(d,HxOverrides.substr(f,np + 1,1)));
			p = np + 2;
		}
		let len = f.length - p;
		r_b += len == null ? HxOverrides.substr(f,p,null) : HxOverrides.substr(f,p,len);
		return r_b;
	}
	static format(d,f) {
		return DateTools.__format(d,f);
	}
}
DateTools.__name__ = "DateTools";
class EReg {
	constructor(r,opt) {
		this.r = new RegExp(r,opt.split("u").join(""));
	}
	match(s) {
		if(this.r.global) {
			this.r.lastIndex = 0;
		}
		this.r.m = this.r.exec(s);
		this.r.s = s;
		return this.r.m != null;
	}
	matched(n) {
		if(this.r.m != null && n >= 0 && n < this.r.m.length) {
			return this.r.m[n];
		} else {
			throw haxe_Exception.thrown("EReg::matched");
		}
	}
	matchedRight() {
		if(this.r.m == null) {
			throw haxe_Exception.thrown("No string matched");
		}
		let sz = this.r.m.index + this.r.m[0].length;
		return HxOverrides.substr(this.r.s,sz,this.r.s.length - sz);
	}
	matchedPos() {
		if(this.r.m == null) {
			throw haxe_Exception.thrown("No string matched");
		}
		return { pos : this.r.m.index, len : this.r.m[0].length};
	}
}
EReg.__name__ = "EReg";
Object.assign(EReg.prototype, {
	__class__: EReg
});
class HxOverrides {
	static strDate(s) {
		switch(s.length) {
		case 8:
			let k = s.split(":");
			let d = new Date();
			d["setTime"](0);
			d["setUTCHours"](k[0]);
			d["setUTCMinutes"](k[1]);
			d["setUTCSeconds"](k[2]);
			return d;
		case 10:
			let k1 = s.split("-");
			return new Date(k1[0],k1[1] - 1,k1[2],0,0,0);
		case 19:
			let k2 = s.split(" ");
			let y = k2[0].split("-");
			let t = k2[1].split(":");
			return new Date(y[0],y[1] - 1,y[2],t[0],t[1],t[2]);
		default:
			throw haxe_Exception.thrown("Invalid date format : " + s);
		}
	}
	static cca(s,index) {
		let x = s.charCodeAt(index);
		if(x != x) {
			return undefined;
		}
		return x;
	}
	static substr(s,pos,len) {
		if(len == null) {
			len = s.length;
		} else if(len < 0) {
			if(pos == 0) {
				len = s.length + len;
			} else {
				return "";
			}
		}
		return s.substr(pos,len);
	}
	static now() {
		return Date.now();
	}
}
HxOverrides.__name__ = "HxOverrides";
class Lambda {
	static count(it,pred) {
		let n = 0;
		if(pred == null) {
			let _ = $getIterator(it);
			while(_.hasNext()) {
				let _1 = _.next();
				++n;
			}
		} else {
			let x = $getIterator(it);
			while(x.hasNext()) {
				let x1 = x.next();
				if(pred(x1)) {
					++n;
				}
			}
		}
		return n;
	}
}
Lambda.__name__ = "Lambda";
class LangParser {
	static main() {
		let allEntries = [];
		dn_data_GetText.parseSourceCode(allEntries,"src");
		dn_data_GetText.parseLdtk(allEntries,"res/levels/sampleWorld.ldtk",{ entityFields : [], levelFieldIds : []});
		dn_data_GetText.parseCastleDB(allEntries,"res/data.cdb");
		dn_data_GetText.writePOT("res/lang/sourceTexts.pot",allEntries);
		process.stdout.write("Done.");
		process.stdout.write("\n");
	}
}
LangParser.__name__ = "LangParser";
Math.__name__ = "Math";
class Reflect {
	static field(o,field) {
		try {
			return o[field];
		} catch( _g ) {
			return null;
		}
	}
	static fields(o) {
		let a = [];
		if(o != null) {
			let hasOwnProperty = Object.prototype.hasOwnProperty;
			for( var f in o ) {
			if(f != "__id__" && f != "hx__closures__" && hasOwnProperty.call(o,f)) {
				a.push(f);
			}
			}
		}
		return a;
	}
	static isFunction(f) {
		if(typeof(f) == "function") {
			return !(f.__name__ || f.__ename__);
		} else {
			return false;
		}
	}
	static compare(a,b) {
		if(a == b) {
			return 0;
		} else if(a > b) {
			return 1;
		} else {
			return -1;
		}
	}
	static deleteField(o,field) {
		if(!Object.prototype.hasOwnProperty.call(o,field)) {
			return false;
		}
		delete(o[field]);
		return true;
	}
	static copy(o) {
		if(o == null) {
			return null;
		}
		let o2 = { };
		let _g = 0;
		let _g1 = Reflect.fields(o);
		while(_g < _g1.length) {
			let f = _g1[_g];
			++_g;
			o2[f] = Reflect.field(o,f);
		}
		return o2;
	}
}
Reflect.__name__ = "Reflect";
class Std {
	static string(s) {
		return js_Boot.__string_rec(s,"");
	}
	static parseInt(x) {
		let v = parseInt(x);
		if(isNaN(v)) {
			return null;
		}
		return v;
	}
	static random(x) {
		if(x <= 0) {
			return 0;
		} else {
			return Math.floor(Math.random() * x);
		}
	}
}
Std.__name__ = "Std";
class StringTools {
	static isSpace(s,pos) {
		let c = HxOverrides.cca(s,pos);
		if(!(c > 8 && c < 14)) {
			return c == 32;
		} else {
			return true;
		}
	}
	static ltrim(s) {
		let l = s.length;
		let r = 0;
		while(r < l && StringTools.isSpace(s,r)) ++r;
		if(r > 0) {
			return HxOverrides.substr(s,r,l - r);
		} else {
			return s;
		}
	}
	static rtrim(s) {
		let l = s.length;
		let r = 0;
		while(r < l && StringTools.isSpace(s,l - r - 1)) ++r;
		if(r > 0) {
			return HxOverrides.substr(s,0,l - r);
		} else {
			return s;
		}
	}
	static trim(s) {
		return StringTools.ltrim(StringTools.rtrim(s));
	}
	static lpad(s,c,l) {
		if(c.length <= 0) {
			return s;
		}
		let buf_b = "";
		l -= s.length;
		while(buf_b.length < l) buf_b += c == null ? "null" : "" + c;
		buf_b += s == null ? "null" : "" + s;
		return buf_b;
	}
	static replace(s,sub,by) {
		return s.split(sub).join(by);
	}
}
StringTools.__name__ = "StringTools";
class haxe_io_Output {
	writeByte(c) {
		throw new haxe_exceptions_NotImplementedException(null,null,{ fileName : "haxe/io/Output.hx", lineNumber : 47, className : "haxe.io.Output", methodName : "writeByte"});
	}
	writeBytes(s,pos,len) {
		if(pos < 0 || len < 0 || pos + len > s.length) {
			throw haxe_Exception.thrown(haxe_io_Error.OutsideBounds);
		}
		let b = s.b;
		let k = len;
		while(k > 0) {
			this.writeByte(b[pos]);
			++pos;
			--k;
		}
		return len;
	}
	writeFullBytes(s,pos,len) {
		while(len > 0) {
			let k = this.writeBytes(s,pos,len);
			pos += k;
			len -= k;
		}
	}
	writeString(s,encoding) {
		let b = haxe_io_Bytes.ofString(s,encoding);
		this.writeFullBytes(b,0,b.length);
	}
}
haxe_io_Output.__name__ = "haxe.io.Output";
Object.assign(haxe_io_Output.prototype, {
	__class__: haxe_io_Output
});
class _$Sys_FileOutput extends haxe_io_Output {
	constructor(fd) {
		super();
		this.fd = fd;
	}
	writeByte(c) {
		js_node_Fs.writeSync(this.fd,String.fromCodePoint(c));
	}
	writeBytes(s,pos,len) {
		let data = s.b;
		return js_node_Fs.writeSync(this.fd,js_node_buffer_Buffer.from(data.buffer,data.byteOffset,s.length),pos,len);
	}
	writeString(s,encoding) {
		js_node_Fs.writeSync(this.fd,s);
	}
	flush() {
		js_node_Fs.fsyncSync(this.fd);
	}
	close() {
		js_node_Fs.closeSync(this.fd);
	}
}
_$Sys_FileOutput.__name__ = "_Sys.FileOutput";
Object.assign(_$Sys_FileOutput.prototype, {
	__class__: _$Sys_FileOutput
});
class haxe_io_Input {
}
haxe_io_Input.__name__ = "haxe.io.Input";
class _$Sys_FileInput extends haxe_io_Input {
	constructor(fd) {
		super();
		this.fd = fd;
	}
	readByte() {
		let buf = js_node_buffer_Buffer.alloc(1);
		try {
			js_node_Fs.readSync(this.fd,buf,0,1,null);
		} catch( _g ) {
			let e = haxe_Exception.caught(_g).unwrap();
			if(e.code == "EOF") {
				throw haxe_Exception.thrown(new haxe_io_Eof());
			} else {
				throw haxe_Exception.thrown(haxe_io_Error.Custom(e));
			}
		}
		return buf[0];
	}
	readBytes(s,pos,len) {
		let data = s.b;
		let buf = js_node_buffer_Buffer.from(data.buffer,data.byteOffset,s.length);
		try {
			return js_node_Fs.readSync(this.fd,buf,pos,len,null);
		} catch( _g ) {
			let e = haxe_Exception.caught(_g).unwrap();
			if(e.code == "EOF") {
				throw haxe_Exception.thrown(new haxe_io_Eof());
			} else {
				throw haxe_Exception.thrown(haxe_io_Error.Custom(e));
			}
		}
	}
	close() {
		js_node_Fs.closeSync(this.fd);
	}
}
_$Sys_FileInput.__name__ = "_Sys.FileInput";
Object.assign(_$Sys_FileInput.prototype, {
	__class__: _$Sys_FileInput
});
var ValueType = $hxEnums["ValueType"] = { __ename__:true,__constructs__:null
	,TNull: {_hx_name:"TNull",_hx_index:0,__enum__:"ValueType",toString:$estr}
	,TInt: {_hx_name:"TInt",_hx_index:1,__enum__:"ValueType",toString:$estr}
	,TFloat: {_hx_name:"TFloat",_hx_index:2,__enum__:"ValueType",toString:$estr}
	,TBool: {_hx_name:"TBool",_hx_index:3,__enum__:"ValueType",toString:$estr}
	,TObject: {_hx_name:"TObject",_hx_index:4,__enum__:"ValueType",toString:$estr}
	,TFunction: {_hx_name:"TFunction",_hx_index:5,__enum__:"ValueType",toString:$estr}
	,TClass: ($_=function(c) { return {_hx_index:6,c:c,__enum__:"ValueType",toString:$estr}; },$_._hx_name="TClass",$_.__params__ = ["c"],$_)
	,TEnum: ($_=function(e) { return {_hx_index:7,e:e,__enum__:"ValueType",toString:$estr}; },$_._hx_name="TEnum",$_.__params__ = ["e"],$_)
	,TUnknown: {_hx_name:"TUnknown",_hx_index:8,__enum__:"ValueType",toString:$estr}
};
ValueType.__constructs__ = [ValueType.TNull,ValueType.TInt,ValueType.TFloat,ValueType.TBool,ValueType.TObject,ValueType.TFunction,ValueType.TClass,ValueType.TEnum,ValueType.TUnknown];
class Type {
	static getEnum(o) {
		if(o == null) {
			return null;
		}
		return $hxEnums[o.__enum__];
	}
	static createEnum(e,constr,params) {
		let f = Reflect.field(e,constr);
		if(f == null) {
			throw haxe_Exception.thrown("No such constructor " + constr);
		}
		if(Reflect.isFunction(f)) {
			if(params == null) {
				throw haxe_Exception.thrown("Constructor " + constr + " need parameters");
			}
			return f.apply(e,params);
		}
		if(params != null && params.length != 0) {
			throw haxe_Exception.thrown("Constructor " + constr + " does not need parameters");
		}
		return f;
	}
	static createEnumIndex(e,index,params) {
		let c;
		let _g = e.__constructs__[index];
		if(_g == null) {
			c = null;
		} else {
			let ctor = _g;
			c = ctor._hx_name;
		}
		if(c == null) {
			throw haxe_Exception.thrown(index + " is not a valid enum constructor index");
		}
		return Type.createEnum(e,c,params);
	}
	static typeof(v) {
		switch(typeof(v)) {
		case "boolean":
			return ValueType.TBool;
		case "function":
			if(v.__name__ || v.__ename__) {
				return ValueType.TObject;
			}
			return ValueType.TFunction;
		case "number":
			if(Math.ceil(v) == v % 2147483648.0) {
				return ValueType.TInt;
			}
			return ValueType.TFloat;
		case "object":
			if(v == null) {
				return ValueType.TNull;
			}
			let e = v.__enum__;
			if(e != null) {
				return ValueType.TEnum($hxEnums[e]);
			}
			let c = js_Boot.getClass(v);
			if(c != null) {
				return ValueType.TClass(c);
			}
			return ValueType.TObject;
		case "string":
			return ValueType.TClass(String);
		case "undefined":
			return ValueType.TNull;
		default:
			return ValueType.TUnknown;
		}
	}
	static enumParameters(e) {
		let enm = $hxEnums[e.__enum__];
		let params = enm.__constructs__[e._hx_index].__params__;
		if(params != null) {
			let _g = [];
			let _g1 = 0;
			while(_g1 < params.length) {
				let p = params[_g1];
				++_g1;
				_g.push(e[p]);
			}
			return _g;
		} else {
			return [];
		}
	}
}
Type.__name__ = "Type";
class UInt {
	static gte(a,b) {
		let aNeg = a < 0;
		let bNeg = b < 0;
		if(aNeg != bNeg) {
			return aNeg;
		} else {
			return a >= b;
		}
	}
}
var cdb_ColumnType = $hxEnums["cdb.ColumnType"] = { __ename__:true,__constructs__:null
	,TId: {_hx_name:"TId",_hx_index:0,__enum__:"cdb.ColumnType",toString:$estr}
	,TString: {_hx_name:"TString",_hx_index:1,__enum__:"cdb.ColumnType",toString:$estr}
	,TBool: {_hx_name:"TBool",_hx_index:2,__enum__:"cdb.ColumnType",toString:$estr}
	,TInt: {_hx_name:"TInt",_hx_index:3,__enum__:"cdb.ColumnType",toString:$estr}
	,TFloat: {_hx_name:"TFloat",_hx_index:4,__enum__:"cdb.ColumnType",toString:$estr}
	,TEnum: ($_=function(values) { return {_hx_index:5,values:values,__enum__:"cdb.ColumnType",toString:$estr}; },$_._hx_name="TEnum",$_.__params__ = ["values"],$_)
	,TRef: ($_=function(sheet) { return {_hx_index:6,sheet:sheet,__enum__:"cdb.ColumnType",toString:$estr}; },$_._hx_name="TRef",$_.__params__ = ["sheet"],$_)
	,TImage: {_hx_name:"TImage",_hx_index:7,__enum__:"cdb.ColumnType",toString:$estr}
	,TList: {_hx_name:"TList",_hx_index:8,__enum__:"cdb.ColumnType",toString:$estr}
	,TCustom: ($_=function(name) { return {_hx_index:9,name:name,__enum__:"cdb.ColumnType",toString:$estr}; },$_._hx_name="TCustom",$_.__params__ = ["name"],$_)
	,TFlags: ($_=function(values) { return {_hx_index:10,values:values,__enum__:"cdb.ColumnType",toString:$estr}; },$_._hx_name="TFlags",$_.__params__ = ["values"],$_)
	,TColor: {_hx_name:"TColor",_hx_index:11,__enum__:"cdb.ColumnType",toString:$estr}
	,TLayer: ($_=function(type) { return {_hx_index:12,type:type,__enum__:"cdb.ColumnType",toString:$estr}; },$_._hx_name="TLayer",$_.__params__ = ["type"],$_)
	,TFile: {_hx_name:"TFile",_hx_index:13,__enum__:"cdb.ColumnType",toString:$estr}
	,TTilePos: {_hx_name:"TTilePos",_hx_index:14,__enum__:"cdb.ColumnType",toString:$estr}
	,TTileLayer: {_hx_name:"TTileLayer",_hx_index:15,__enum__:"cdb.ColumnType",toString:$estr}
	,TDynamic: {_hx_name:"TDynamic",_hx_index:16,__enum__:"cdb.ColumnType",toString:$estr}
	,TProperties: {_hx_name:"TProperties",_hx_index:17,__enum__:"cdb.ColumnType",toString:$estr}
	,TGradient: {_hx_name:"TGradient",_hx_index:18,__enum__:"cdb.ColumnType",toString:$estr}
	,TCurve: {_hx_name:"TCurve",_hx_index:19,__enum__:"cdb.ColumnType",toString:$estr}
};
cdb_ColumnType.__constructs__ = [cdb_ColumnType.TId,cdb_ColumnType.TString,cdb_ColumnType.TBool,cdb_ColumnType.TInt,cdb_ColumnType.TFloat,cdb_ColumnType.TEnum,cdb_ColumnType.TRef,cdb_ColumnType.TImage,cdb_ColumnType.TList,cdb_ColumnType.TCustom,cdb_ColumnType.TFlags,cdb_ColumnType.TColor,cdb_ColumnType.TLayer,cdb_ColumnType.TFile,cdb_ColumnType.TTilePos,cdb_ColumnType.TTileLayer,cdb_ColumnType.TDynamic,cdb_ColumnType.TProperties,cdb_ColumnType.TGradient,cdb_ColumnType.TCurve];
class cdb_TileMode {
	static _new(s) {
		return s;
	}
	static ofString(s) {
		return cdb_TileMode._new(s);
	}
	static toString(this1) {
		return this1;
	}
}
class cdb_Parser {
	static saveType(t) {
		switch(t._hx_index) {
		case 5:
			let values = t.values;
			return t._hx_index + ":" + values.join(",");
		case 6:
			let _g = t.sheet;
			return t._hx_index + ":" + Std.string(Type.enumParameters(t)[0]);
		case 9:
			let _g1 = t.name;
			return t._hx_index + ":" + Std.string(Type.enumParameters(t)[0]);
		case 10:
			let values1 = t.values;
			return t._hx_index + ":" + values1.join(",");
		case 12:
			let _g2 = t.type;
			return t._hx_index + ":" + Std.string(Type.enumParameters(t)[0]);
		case 0:case 1:case 2:case 3:case 4:case 7:case 8:case 11:case 13:case 14:case 15:case 16:case 17:case 18:case 19:
			if(t._hx_index == null) {
				return "null";
			} else {
				return "" + t._hx_index;
			}
			break;
		}
	}
	static getType(str) {
		let _g = Std.parseInt(str);
		if(_g == null) {
			throw haxe_Exception.thrown("Unknown type " + str);
		} else {
			switch(_g) {
			case 0:
				return cdb_ColumnType.TId;
			case 1:
				return cdb_ColumnType.TString;
			case 2:
				return cdb_ColumnType.TBool;
			case 3:
				return cdb_ColumnType.TInt;
			case 4:
				return cdb_ColumnType.TFloat;
			case 5:
				return cdb_ColumnType.TEnum(HxOverrides.substr(str,str.indexOf(":") + 1,null).split(","));
			case 6:
				return cdb_ColumnType.TRef(HxOverrides.substr(str,str.indexOf(":") + 1,null));
			case 7:
				return cdb_ColumnType.TImage;
			case 8:
				return cdb_ColumnType.TList;
			case 9:
				return cdb_ColumnType.TCustom(HxOverrides.substr(str,str.indexOf(":") + 1,null));
			case 10:
				return cdb_ColumnType.TFlags(HxOverrides.substr(str,str.indexOf(":") + 1,null).split(","));
			case 11:
				return cdb_ColumnType.TColor;
			case 12:
				return cdb_ColumnType.TLayer(HxOverrides.substr(str,str.indexOf(":") + 1,null));
			case 13:
				return cdb_ColumnType.TFile;
			case 14:
				return cdb_ColumnType.TTilePos;
			case 15:
				return cdb_ColumnType.TTileLayer;
			case 16:
				return cdb_ColumnType.TDynamic;
			case 17:
				return cdb_ColumnType.TProperties;
			case 18:
				return cdb_ColumnType.TGradient;
			case 19:
				return cdb_ColumnType.TCurve;
			default:
				throw haxe_Exception.thrown("Unknown type " + str);
			}
		}
	}
	static parse(content,editMode) {
		if(content == null) {
			throw haxe_Exception.thrown("CDB content is null");
		}
		let data = JSON.parse(content);
		let _g = 0;
		let _g1 = data.sheets;
		while(_g < _g1.length) {
			let s = _g1[_g];
			++_g;
			let _g2 = 0;
			let _g3 = s.columns;
			while(_g2 < _g3.length) {
				let c = _g3[_g2];
				++_g2;
				c.type = cdb_Parser.getType(c.typeStr);
				c.typeStr = null;
			}
		}
		let _g2 = 0;
		let _g3 = data.customTypes;
		while(_g2 < _g3.length) {
			let t = _g3[_g2];
			++_g2;
			let _g = 0;
			let _g1 = t.cases;
			while(_g < _g1.length) {
				let c = _g1[_g];
				++_g;
				let _g2 = 0;
				let _g3 = c.args;
				while(_g2 < _g3.length) {
					let a = _g3[_g2];
					++_g2;
					a.type = cdb_Parser.getType(a.typeStr);
					a.typeStr = null;
				}
			}
		}
		let _g4 = 0;
		let _g5 = data.sheets;
		while(_g4 < _g5.length) {
			let s = _g5[_g4];
			++_g4;
			if(!(editMode || s.props.hasGroup)) {
				continue;
			}
			if(s.separators == null) {
				let ids = Reflect.field(s,"separatorIds");
				let _g = [];
				let _g1 = 0;
				while(_g1 < ids.length) {
					let i = ids[_g1];
					++_g1;
					if(typeof(i) == "number" && ((i | 0) === i)) {
						_g.push({ index : i});
					} else {
						_g.push({ id : i});
					}
				}
				s.separators = _g;
				Reflect.deleteField(s,"separatorIds");
			}
			let indexMap = null;
			let _g = 0;
			let _g1 = s.separators.length;
			while(_g < _g1) {
				let i = _g++;
				let sep = s.separators[i];
				if(typeof(sep) == "number" && ((sep | 0) === sep)) {
					s.separators[i] = { index : sep};
				} else if(sep.id != null) {
					if(indexMap == null) {
						let idField = null;
						let _g = 0;
						let _g1 = s.columns;
						while(_g < _g1.length) {
							let c = _g1[_g];
							++_g;
							if(c.type == cdb_ColumnType.TId) {
								idField = c.name;
								break;
							}
						}
						indexMap = new haxe_ds_StringMap();
						let _g2 = 0;
						let _g3 = s.lines.length;
						while(_g2 < _g3) {
							let i = _g2++;
							let l = s.lines[i];
							let id = Reflect.field(l,idField);
							if(id != null) {
								indexMap.h[id] = i;
							}
						}
					}
					sep.index = indexMap.h[sep.id];
					Reflect.deleteField(sep,"id");
				}
			}
			let titles = Reflect.field(s.props,"separatorTitles");
			if(titles != null) {
				Reflect.deleteField(s.props,"separatorTitles");
				let _g = 0;
				let _g1 = titles.length;
				while(_g < _g1) {
					let i = _g++;
					if(titles[i] != null) {
						s.separators[i].title = titles[i];
					}
				}
			}
		}
		return data;
	}
	static save(data) {
		let save = [];
		let seps = [];
		let _g = 0;
		let _g1 = data.sheets;
		while(_g < _g1.length) {
			let s = _g1[_g];
			++_g;
			let idField = null;
			let _g2 = 0;
			let _g3 = s.columns;
			while(_g2 < _g3.length) {
				let c = _g3[_g2];
				++_g2;
				if(c.type == cdb_ColumnType.TId && idField == null) {
					idField = c.name;
				}
				save.push(c.type);
				if(c.typeStr == null) {
					c.typeStr = cdb_Parser.saveType(c.type);
				}
				Reflect.deleteField(c,"type");
			}
			let oldSeps = null;
			if(idField != null && s.separators.length > 0) {
				let uniqueIDs = true;
				let uids_h = Object.create(null);
				let _g = 0;
				let _g1 = s.lines;
				while(_g < _g1.length) {
					let l = _g1[_g];
					++_g;
					let id = Reflect.field(l,idField);
					if(id != null) {
						if(uids_h[id]) {
							uniqueIDs = false;
							break;
						}
						uids_h[id] = true;
					}
				}
				if(uniqueIDs) {
					oldSeps = s.separators;
					s.separators = [];
					let _g = 0;
					while(_g < oldSeps.length) {
						let sep = oldSeps[_g];
						++_g;
						let obj = s.lines[sep.index];
						let id = obj != null ? Reflect.field(obj,idField) : null;
						let sep1 = sep;
						if(id != null) {
							sep1 = Reflect.copy(sep1);
							Reflect.deleteField(sep1,"index");
							sep1.id = id;
						}
						s.separators.push(sep1);
					}
				}
			}
			seps.push(oldSeps);
		}
		let _g2 = 0;
		let _g3 = data.customTypes;
		while(_g2 < _g3.length) {
			let t = _g3[_g2];
			++_g2;
			let _g = 0;
			let _g1 = t.cases;
			while(_g < _g1.length) {
				let c = _g1[_g];
				++_g;
				let _g2 = 0;
				let _g3 = c.args;
				while(_g2 < _g3.length) {
					let a = _g3[_g2];
					++_g2;
					save.push(a.type);
					if(a.typeStr == null) {
						a.typeStr = cdb_Parser.saveType(a.type);
					}
					Reflect.deleteField(a,"type");
				}
			}
		}
		let str = JSON.stringify(data,null,"\t");
		let _g4 = 0;
		let _g5 = data.sheets;
		while(_g4 < _g5.length) {
			let s = _g5[_g4];
			++_g4;
			let _g = 0;
			let _g1 = s.columns;
			while(_g < _g1.length) {
				let c = _g1[_g];
				++_g;
				c.type = save.shift();
			}
			let oldSeps = seps.shift();
			if(oldSeps != null) {
				s.separators = oldSeps;
			}
		}
		let _g6 = 0;
		let _g7 = data.customTypes;
		while(_g6 < _g7.length) {
			let t = _g7[_g6];
			++_g6;
			let _g = 0;
			let _g1 = t.cases;
			while(_g < _g1.length) {
				let c = _g1[_g];
				++_g;
				let _g2 = 0;
				let _g3 = c.args;
				while(_g2 < _g3.length) {
					let a = _g3[_g2];
					++_g2;
					a.type = save.shift();
				}
			}
		}
		return str;
	}
}
cdb_Parser.__name__ = "cdb.Parser";
class dn_Cooldown {
	constructor(fps,maxSize) {
		if(dn_Cooldown.INDEXES == null) {
			if(haxe_rtti_Meta.getType(dn_Cooldown).indexes != null) {
				let _g = [];
				let _g1 = 0;
				let _g2 = haxe_rtti_Meta.getType(dn_Cooldown).indexes;
				while(_g1 < _g2.length) {
					let str = _g2[_g1];
					++_g1;
					_g.push(Std.string(str));
				}
				dn_Cooldown.INDEXES = _g;
			}
		}
		this.baseFps = fps;
		this.changeMaxSizeAndReset(maxSize == null ? dn_Cooldown.DEFAULT_COUNT_LIMIT : maxSize);
	}
	msToFrames(ms) {
		return ms * this.baseFps / 1000.;
	}
	secToFrames(s) {
		return s * this.baseFps;
	}
	changeMaxSizeAndReset(newMaxSize) {
		this.cds = new dn_struct_RecyclablePool(newMaxSize,function() {
			return new dn__$Cooldown_CdInst();
		});
		this.cds.nalloc = 0;
		this.fastCheck = new haxe_ds_IntMap();
	}
	dispose() {
		this.cds.dispose(null);
		this.cds = null;
		this.fastCheck = null;
	}
	destroy() {
		this.dispose();
	}
	toString() {
		return "Cooldowns(" + this.cds.nalloc + "/" + this.cds.size + ")";
	}
	count() {
		return this.cds.nalloc;
	}
	reset() {
		this.cds.nalloc = 0;
		this.fastCheck = new haxe_ds_IntMap();
	}
	_getF(k) {
		let cd = this._getCdObject(k);
		if(cd == null) {
			return 0;
		} else {
			return cd.frames;
		}
	}
	_getInitialValueF(k) {
		let cd = this._getCdObject(k);
		if(cd == null) {
			return 0;
		} else {
			return cd.initial;
		}
	}
	_getRatio(k) {
		let cd = this._getCdObject(k);
		let max = cd == null ? 0 : cd.initial;
		if(max <= 0) {
			return 0;
		} else {
			let cd = this._getCdObject(k);
			return (cd == null ? 0 : cd.frames) / max;
		}
	}
	_onComplete(k,onceCB) {
		let cd = this._getCdObject(k);
		if(cd == null) {
			throw haxe_Exception.thrown("cannot bind onComplete(" + k + "): cooldown " + k + " isn't running");
		}
		cd.onCompleteOnce = onceCB;
	}
	_setF(k,frames,allowLower,onComplete) {
		if(allowLower == null) {
			allowLower = true;
		}
		let cur = this._getCdObject(k);
		if(cur != null && frames < cur.frames && !allowLower) {
			return;
		}
		if(frames <= 0) {
			if(cur != null) {
				this.fastCheck.remove(cur.k);
				let _this = this.cds;
				let _g = 0;
				let _g1 = _this.nalloc;
				while(_g < _g1) {
					let i = _g++;
					if(_this.pool[i] == cur) {
						if(i >= 0 && i < _this.nalloc) {
							if(i == _this.nalloc - 1) {
								_this.nalloc--;
							} else {
								let tmp = _this.pool[i];
								_this.pool[i] = _this.pool[_this.nalloc - 1];
								_this.pool[_this.nalloc - 1] = tmp;
								_this.nalloc--;
							}
						}
						break;
					}
				}
			}
		} else {
			this.fastCheck.h[k] = true;
			if(cur != null) {
				cur.frames = frames;
				cur.initial = frames;
			} else {
				let _this = this.cds;
				if(_this.nalloc >= _this.size) {
					_this.garbageCollectNow();
					if(_this.nalloc >= _this.size) {
						throw haxe_Exception.thrown("RecyclablePool limit reached (" + _this.size + ")");
					}
				}
				let e = _this.pool[_this.nalloc++];
				e.recycle();
				let cd = e;
				cd.k = k;
				cd.frames = frames;
				cd.initial = frames;
			}
		}
		if(onComplete != null) {
			if(frames <= 0) {
				onComplete();
			} else {
				let cd = this._getCdObject(k);
				if(cd == null) {
					throw haxe_Exception.thrown("cannot bind onComplete(" + k + "): cooldown " + k + " isn't running");
				}
				cd.onCompleteOnce = onComplete;
			}
		}
	}
	_unset(k) {
		let _g = 0;
		let _g1 = this.cds.nalloc;
		while(_g < _g1) {
			let i = _g++;
			let _this = this.cds;
			if((i < 0 || i >= _this.nalloc ? null : _this.pool[i]).k == k) {
				let _this = this.cds;
				this.fastCheck.remove((i < 0 || i >= _this.nalloc ? null : _this.pool[i]).k);
				let _this1 = this.cds;
				if(i >= 0 && i < _this1.nalloc) {
					if(i == _this1.nalloc - 1) {
						_this1.nalloc--;
					} else {
						let tmp = _this1.pool[i];
						_this1.pool[i] = _this1.pool[_this1.nalloc - 1];
						_this1.pool[_this1.nalloc - 1] = tmp;
						_this1.nalloc--;
					}
				}
				break;
			}
		}
	}
	_has(k) {
		return this.fastCheck.h.hasOwnProperty(k);
	}
	_hasSetF(k,frames) {
		if(this.fastCheck.h.hasOwnProperty(k)) {
			return true;
		} else {
			let onComplete = null;
			let cur = this._getCdObject(k);
			if(!(cur != null && frames < cur.frames && false)) {
				if(frames <= 0) {
					if(cur != null) {
						this.fastCheck.remove(cur.k);
						let _this = this.cds;
						let _g = 0;
						let _g1 = _this.nalloc;
						while(_g < _g1) {
							let i = _g++;
							if(_this.pool[i] == cur) {
								if(i >= 0 && i < _this.nalloc) {
									if(i == _this.nalloc - 1) {
										_this.nalloc--;
									} else {
										let tmp = _this.pool[i];
										_this.pool[i] = _this.pool[_this.nalloc - 1];
										_this.pool[_this.nalloc - 1] = tmp;
										_this.nalloc--;
									}
								}
								break;
							}
						}
					}
				} else {
					this.fastCheck.h[k] = true;
					if(cur != null) {
						cur.frames = frames;
						cur.initial = frames;
					} else {
						let _this = this.cds;
						if(_this.nalloc >= _this.size) {
							_this.garbageCollectNow();
							if(_this.nalloc >= _this.size) {
								throw haxe_Exception.thrown("RecyclablePool limit reached (" + _this.size + ")");
							}
						}
						let e = _this.pool[_this.nalloc++];
						e.recycle();
						let cd = e;
						cd.k = k;
						cd.frames = frames;
						cd.initial = frames;
					}
				}
				if(onComplete != null) {
					if(frames <= 0) {
						onComplete();
					} else {
						let cd = this._getCdObject(k);
						if(cd == null) {
							throw haxe_Exception.thrown("cannot bind onComplete(" + k + "): cooldown " + k + " isn't running");
						}
						cd.onCompleteOnce = onComplete;
					}
				}
			}
			return false;
		}
	}
	_getCdObject(k) {
		let _g_rpool = this.cds;
		let _g_i = 0;
		while(_g_i < _g_rpool.nalloc) {
			let cd = _g_rpool.pool[_g_i++];
			if(cd.k == k) {
				return cd;
			}
		}
		return null;
	}
	unsetCdInst(cd) {
		this.fastCheck.remove(cd.k);
		let _this = this.cds;
		let _g = 0;
		let _g1 = _this.nalloc;
		while(_g < _g1) {
			let i = _g++;
			if(_this.pool[i] == cd) {
				if(i >= 0 && i < _this.nalloc) {
					if(i == _this.nalloc - 1) {
						_this.nalloc--;
					} else {
						let tmp = _this.pool[i];
						_this.pool[i] = _this.pool[_this.nalloc - 1];
						_this.pool[_this.nalloc - 1] = tmp;
						_this.nalloc--;
					}
				}
				break;
			}
		}
	}
	unsetIndex(idx) {
		let _this = this.cds;
		this.fastCheck.remove((idx < 0 || idx >= _this.nalloc ? null : _this.pool[idx]).k);
		let _this1 = this.cds;
		if(idx >= 0 && idx < _this1.nalloc) {
			if(idx == _this1.nalloc - 1) {
				_this1.nalloc--;
			} else {
				let tmp = _this1.pool[idx];
				_this1.pool[idx] = _this1.pool[_this1.nalloc - 1];
				_this1.pool[_this1.nalloc - 1] = tmp;
				_this1.nalloc--;
			}
		}
	}
	debug() {
		let _g = [];
		let _g_rpool = this.cds;
		let _g_i = 0;
		while(_g_i < _g_rpool.nalloc) {
			let cd = _g_rpool.pool[_g_i++];
			_g.push(cd.toString());
		}
		return _g.join("\n");
	}
	update(tmod) {
		let i = 0;
		let cd;
		let cb = null;
		while(i < this.cds.nalloc) {
			let _this = this.cds;
			cd = i < 0 || i >= _this.nalloc ? null : _this.pool[i];
			cd.frames -= tmod;
			if(cd.frames <= 0) {
				cb = cd.onCompleteOnce;
				let _this = this.cds;
				this.fastCheck.remove((i < 0 || i >= _this.nalloc ? null : _this.pool[i]).k);
				let _this1 = this.cds;
				if(i >= 0 && i < _this1.nalloc) {
					if(i == _this1.nalloc - 1) {
						_this1.nalloc--;
					} else {
						let tmp = _this1.pool[i];
						_this1.pool[i] = _this1.pool[_this1.nalloc - 1];
						_this1.pool[_this1.nalloc - 1] = tmp;
						_this1.nalloc--;
					}
				}
				if(cb != null) {
					cb();
				}
			} else {
				++i;
			}
		}
	}
}
dn_Cooldown.__name__ = "dn.Cooldown";
Object.assign(dn_Cooldown.prototype, {
	__class__: dn_Cooldown
});
class dn__$Cooldown_CdInst {
	constructor() {
	}
	set(key,frames) {
		this.k = key;
		this.frames = frames;
		this.initial = frames;
	}
	recycle() {
		this.onCompleteOnce = null;
	}
	getRemainingRatio() {
		if(this.initial == 0) {
			return 0;
		} else {
			return this.frames / this.initial;
		}
	}
	getProgressRatio() {
		if(this.initial == 0) {
			return 0;
		} else {
			return 1 - (this.initial == 0 ? 0 : this.frames / this.initial);
		}
	}
	toString() {
		let x = (this.initial == 0 ? 0 : 1 - (this.initial == 0 ? 0 : this.frames / this.initial)) * 100;
		return dn_Cooldown.INDEXES[this.k >>> 22] + "|" + (this.k & 4194303) + (": " + this.frames + "/" + this.initial + " (" + ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) + "%)");
	}
}
dn__$Cooldown_CdInst.__name__ = "dn._Cooldown.CdInst";
Object.assign(dn__$Cooldown_CdInst.prototype, {
	__class__: dn__$Cooldown_CdInst
});
class dn__$Delayer_Task {
	constructor(id,frames,cb) {
		this.frames = frames;
		this.cb = cb;
		this.id = id;
	}
	toString() {
		return this.frames + "f " + Std.string(this.cb);
	}
}
dn__$Delayer_Task.__name__ = "dn._Delayer.Task";
Object.assign(dn__$Delayer_Task.prototype, {
	__class__: dn__$Delayer_Task
});
class dn_Delayer {
	constructor(fps) {
		this.delays = [];
		this.fps = fps;
	}
	toString() {
		let _gthis = this;
		let _this = this.delays;
		let result = new Array(_this.length);
		let _g = 0;
		let _g1 = _this.length;
		while(_g < _g1) {
			let i = _g++;
			let v = _this[i].frames / _gthis.fps;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			result[i] = tmp + "s";
		}
		return "Delayer(timers=" + result.join(",") + ")";
	}
	isDestroyed() {
		return this.delays == null;
	}
	destroy() {
		this.dispose();
	}
	dispose() {
		this.delays = null;
	}
	completeEverything() {
		let all = this.delays.slice();
		this.delays = [];
		let _g = 0;
		while(_g < all.length) {
			let d = all[_g];
			++_g;
			d.cb();
		}
	}
	cancelEverything() {
		this.delays = [];
	}
	hasId(id) {
		let _g = 0;
		let _g1 = this.delays;
		while(_g < _g1.length) {
			let e = _g1[_g];
			++_g;
			if(e.id == id) {
				return true;
			}
		}
		return false;
	}
	cancelById(id) {
		let i = 0;
		while(i < this.delays.length) if(this.delays[i].id == id) {
			this.delays.splice(i,1);
		} else {
			++i;
		}
	}
	runImmediately(id) {
		let i = 0;
		while(i < this.delays.length) if(this.delays[i].id == id) {
			let cb = this.delays[i].cb;
			this.delays.splice(i,1);
			cb();
		} else {
			++i;
		}
	}
	cmp(a,b) {
		if(a.frames < b.frames) {
			return -1;
		} else if(a.frames > b.frames) {
			return 1;
		} else {
			return 0;
		}
	}
	addMs(id,cb,ms) {
		if(ms <= 0) {
			cb();
		} else {
			this.delays.push(new dn__$Delayer_Task(id,this.fps * ms / 1000,cb));
			haxe_ds_ArraySort.sort(this.delays,$bind(this,this.cmp));
		}
	}
	addS(id,cb,sec) {
		if(sec <= 0) {
			cb();
		} else {
			this.delays.push(new dn__$Delayer_Task(id,this.fps * sec,cb));
			haxe_ds_ArraySort.sort(this.delays,$bind(this,this.cmp));
		}
	}
	addF(id,cb,frames) {
		if(frames <= 0) {
			cb();
		} else {
			this.delays.push(new dn__$Delayer_Task(id,frames,cb));
			haxe_ds_ArraySort.sort(this.delays,$bind(this,this.cmp));
		}
	}
	nextFrame(cb) {
		this.addF(null,cb,0.000001);
	}
	hasAny() {
		if(this.delays != null) {
			return this.delays.length > 0;
		} else {
			return false;
		}
	}
	update(tmod) {
		let i = 0;
		while(i < this.delays.length) {
			this.delays[i].frames -= tmod;
			if(this.delays[i].frames <= 0) {
				this.delays[i].cb();
				if(this.delays == null || this.delays[i] == null) {
					break;
				}
				this.delays[i].cb = null;
				this.delays.shift();
			} else {
				++i;
			}
		}
	}
}
dn_Delayer.__name__ = "dn.Delayer";
Object.assign(dn_Delayer.prototype, {
	__class__: dn_Delayer
});
var dn_PathSlashMode = $hxEnums["dn.PathSlashMode"] = { __ename__:true,__constructs__:null
	,Preserve: {_hx_name:"Preserve",_hx_index:0,__enum__:"dn.PathSlashMode",toString:$estr}
	,OnlyBackslashes: {_hx_name:"OnlyBackslashes",_hx_index:1,__enum__:"dn.PathSlashMode",toString:$estr}
	,OnlySlashes: {_hx_name:"OnlySlashes",_hx_index:2,__enum__:"dn.PathSlashMode",toString:$estr}
};
dn_PathSlashMode.__constructs__ = [dn_PathSlashMode.Preserve,dn_PathSlashMode.OnlyBackslashes,dn_PathSlashMode.OnlySlashes];
class dn_FilePath {
	constructor() {
		this._useWinNetDriveUriFormat = false;
		this.isWindowsNetworkDrive = false;
		this.init();
	}
	init() {
		this.backslashes = false;
		this.directory = null;
		this.set_fileName(null);
		this.set_extension(null);
	}
	isEmpty() {
		return ((this.isWindowsNetworkDrive && !this._useWinNetDriveUriFormat ? this.backslashes ? "\\" : "/" : this.uriScheme != null ? this.uriAuthority == null ? "" + this.uriScheme + ":/" : "" + this.uriScheme + "://" + this.uriAuthority + "/" : "") + (this.directory == null ? "" : this.fileName == null && this.extension == null || this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory == null ? null : this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory + (this.backslashes ? "\\" : "/")) + ((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) == null ? "" : this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension))).length == 0;
	}
	clone() {
		let p = new dn_FilePath();
		p.backslashes = this.backslashes;
		p.directory = this.directory;
		p.set_fileName(this.fileName);
		p.set_extension(this.extension);
		return p;
	}
	removeTrailingSlashes(path) {
		while(path.charAt(path.length - 1) == (this.backslashes ? "\\" : "/")) path = HxOverrides.substr(path,0,path.length - 1);
		return path;
	}
	parseFilePath(filePath) {
		this.parse(filePath,true);
		return this;
	}
	appendDirectory(extraDirs) {
		if(extraDirs == null || extraDirs.length == 0) {
			return this;
		}
		let p = new dn_FilePath();
		p.parse(extraDirs,false);
		let fp = p;
		if(fp.directory != null) {
			if(this.directory == null) {
				this.directory = fp.directory;
			} else {
				this.directory += (this.backslashes ? "\\" : "/") + fp.directory;
			}
		}
		return this;
	}
	prependDirectory(extraDirs) {
		if(extraDirs == null || extraDirs.length == 0) {
			return this;
		}
		let p = new dn_FilePath();
		p.parse(extraDirs,false);
		let fp = p;
		if(fp.directory != null) {
			if(this.directory == null) {
				this.directory = fp.directory;
			} else {
				this.directory = fp.directory + (this.backslashes ? "\\" : "/") + this.directory;
			}
		}
		return this;
	}
	setDirectory(dir) {
		this.directory = dir;
		return this;
	}
	parseDirPath(dirPath) {
		this.parse(dirPath,false);
		return this;
	}
	useSlashes() {
		if(this.isWindowsNetworkDrive && !this._useWinNetDriveUriFormat) {
			return this;
		} else {
			if(this.directory != null) {
				this.directory = StringTools.replace(this.directory,"\\","/");
			}
			this.backslashes = false;
			return this;
		}
	}
	useBackslashes() {
		if(this.directory != null) {
			this.directory = StringTools.replace(this.directory,"/","\\");
		}
		this.backslashes = true;
		return this;
	}
	convertWindowsNetworkDriveToUri() {
		if(!this._useWinNetDriveUriFormat && this.isWindowsNetworkDrive) {
			this._useWinNetDriveUriFormat = true;
			this.removeFirstDirectory();
			this.removeFirstDirectory();
			this.useSlashes();
		}
		return this;
	}
	hasDriveLetter() {
		return this.getDriveLetter() != null;
	}
	getDriveLetter(forceLowerCase) {
		if(forceLowerCase == null) {
			forceLowerCase = true;
		}
		let letterReg = this.uriScheme != null ? new EReg("([^a-z]|^)([a-z])[:$][/\\\\]?","gi") : new EReg("([^a-z]|^)([a-z]):[/\\\\]?","gi");
		if(this.directory != null && letterReg.match(this.directory)) {
			if(forceLowerCase) {
				return letterReg.matched(2).toLowerCase();
			} else {
				return letterReg.matched(2);
			}
		} else {
			return null;
		}
	}
	isUri() {
		return this.uriScheme != null;
	}
	isAbsolute() {
		if(!(this.uriScheme != null || this.directory != null && this.getDirectoryArray()[0] == "")) {
			return this.getDriveLetter() != null;
		} else {
			return true;
		}
	}
	makeRelativeTo(dirPath) {
		let cur = this.getDirectoryArray();
		let p = new dn_FilePath();
		p.parse(dirPath,false);
		let other = p;
		if(this.uriScheme != other.uriScheme || this.getDriveLetter() != other.getDriveLetter()) {
			return this;
		}
		let ref = other.getDirectoryArray();
		if(cur[0] != ref[0]) {
			return this;
		}
		while(cur.length > 0 && ref.length > 0 && cur[0] == ref[0]) {
			cur.shift();
			ref.shift();
		}
		let i = 0;
		while(i < ref.length) {
			cur.splice(0,0,"..");
			++i;
		}
		this.directory = cur.length == 0 ? null : cur.join(this.backslashes ? "\\" : "/");
		return this;
	}
	parseFileName(raw) {
		if(raw.indexOf(".") < 0) {
			this.set_fileName(raw);
			this.set_extension(null);
		} else if(raw.indexOf(".") == 0 && raw.lastIndexOf(".") == 0) {
			this.set_fileName(null);
			this.set_extension(HxOverrides.substr(raw,1,null));
		} else {
			this.set_fileName(HxOverrides.substr(raw,0,raw.lastIndexOf(".")));
			this.set_extension(HxOverrides.substr(raw,raw.lastIndexOf(".") + 1,null));
		}
	}
	parse(rawPath,containsFileName) {
		this.init();
		if(rawPath == null || rawPath.length == 0) {
			return;
		}
		switch(dn_FilePath.SLASH_MODE._hx_index) {
		case 0:
			break;
		case 1:
			rawPath = StringTools.replace(rawPath,"/","\\");
			break;
		case 2:
			rawPath = StringTools.replace(rawPath,"\\","/");
			break;
		}
		if(rawPath.indexOf("\\") >= 0) {
			if(rawPath.indexOf("/") >= 0) {
				this.backslashes = rawPath.indexOf("\\") < rawPath.indexOf("/");
			} else {
				this.backslashes = true;
			}
		}
		rawPath = StringTools.replace(rawPath,this.backslashes ? "/" : "\\",this.backslashes ? "\\" : "/");
		if(containsFileName && rawPath.indexOf(this.backslashes ? "\\" : "/") < 0) {
			this.parseFileName(rawPath);
		} else {
			let uriSchemeReg = new EReg("^([a-z]{2,}):[/]{2}(.*?)/|^([a-z]{2,}):[/]{1}","gi");
			if(uriSchemeReg.match(rawPath)) {
				if(uriSchemeReg.matched(3) != null) {
					this.uriScheme = uriSchemeReg.matched(3);
					this.uriAuthority = null;
				} else {
					this.uriScheme = uriSchemeReg.matched(1);
					this.uriAuthority = uriSchemeReg.matched(2);
					if(this.uriAuthority == "") {
						this.uriAuthority = null;
					}
				}
				rawPath = uriSchemeReg.matchedRight();
			}
			this.isWindowsNetworkDrive = false;
			if(dn_FilePath.WIN_NETWORK_DRIVE_REG.match(rawPath)) {
				this.isWindowsNetworkDrive = true;
				this.uriScheme = "file";
				this.uriAuthority = dn_FilePath.WIN_NETWORK_DRIVE_REG.matched(1);
			}
			while(rawPath.indexOf((this.backslashes ? "\\" : "/") + (this.backslashes ? "\\" : "/")) >= 0) rawPath = StringTools.replace(rawPath,(this.backslashes ? "\\" : "/") + (this.backslashes ? "\\" : "/"),this.backslashes ? "\\" : "/");
			if(!containsFileName && rawPath.length > 1 && rawPath.charAt(rawPath.length - 1) == (this.backslashes ? "\\" : "/")) {
				rawPath = HxOverrides.substr(rawPath,0,rawPath.length - 1);
			}
			if(rawPath.indexOf(this.backslashes ? "\\" : "/") < 0) {
				this.directory = containsFileName ? null : rawPath;
			} else {
				this.directory = containsFileName ? HxOverrides.substr(rawPath,0,rawPath.lastIndexOf(this.backslashes ? "\\" : "/")) : rawPath;
				if(this.directory.length == 0 && containsFileName && rawPath.charAt(0) == (this.backslashes ? "\\" : "/")) {
					this.directory = "/";
				} else if(this.directory.length == 0) {
					this.directory = null;
				}
			}
			if(containsFileName && rawPath.lastIndexOf(this.backslashes ? "\\" : "/") < rawPath.length - 1) {
				let rawFile = HxOverrides.substr(rawPath,rawPath.lastIndexOf(this.backslashes ? "\\" : "/") + 1,null);
				this.parseFileName(rawFile);
			}
			let dirs = this.getDirectoryArray();
			if(dirs.length > 0) {
				let i = 0;
				while(i < dirs.length) if(dirs[i] == ".." && i > 0 && dirs[i - 1] != ".." && dirs[i - 1] != "") {
					dirs.splice(i - 1,2);
					--i;
				} else {
					++i;
				}
				if(dirs.length == 0) {
					this.directory = null;
				} else if(dirs.length == 1 && dirs[0] == "") {
					this.directory = this.backslashes ? "\\" : "/";
				} else {
					this.directory = dirs.join(this.backslashes ? "\\" : "/");
				}
			}
			let dirs1 = this.getDirectoryArray();
			if(dirs1.length > 1) {
				let i = 1;
				while(i < dirs1.length) if(dirs1[i] == ".") {
					dirs1.splice(i,1);
				} else {
					++i;
				}
				if(dirs1.length == 0) {
					this.directory = null;
				} else {
					this.directory = dirs1.join(this.backslashes ? "\\" : "/");
				}
			}
			if(this.directory != (this.backslashes ? "\\" : "/") && this.directory != null) {
				let ignore = 0;
				if(this.getDriveLetter() != null) {
					++ignore;
				}
				let dirs = this.getDirectoryArray();
				let _g = 0;
				let _g1 = dirs.length;
				while(_g < _g1) {
					let i = _g++;
					dirs[i] = i < ignore ? this.sanitize(dirs[i],true) : this.sanitize(dirs[i]);
				}
				this.directory = dirs.join(this.backslashes ? "\\" : "/");
			}
		}
	}
	slash() {
		if(this.backslashes) {
			return "\\";
		} else {
			return "/";
		}
	}
	sanitize(v,ignoreDoubleDots) {
		if(ignoreDoubleDots == null) {
			ignoreDoubleDots = false;
		}
		if(ignoreDoubleDots) {
			let _this_r = new RegExp("[*{}/\\\\<>?|]","g".split("u").join(""));
			return v.replace(_this_r,"_");
		} else {
			let _this_r = new RegExp("[*{}/\\\\<>?|:]","g".split("u").join(""));
			return v.replace(_this_r,"_");
		}
	}
	set_extension(v) {
		if(v == null) {
			return this.extension = null;
		} else {
			while(v.charAt(0) == ".") v = HxOverrides.substr(v,1,null);
			v = StringTools.replace(v," ","_");
			if(v.length == 0) {
				return this.extension = null;
			}
			return this.extension = this.sanitize(v);
		}
	}
	set_fileName(v) {
		if(v == null) {
			return this.fileName = null;
		}
		v = this.sanitize(v);
		return this.fileName = v;
	}
	get_fileWithExt() {
		if(this.fileName == null && this.extension == null) {
			return null;
		} else {
			return (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension);
		}
	}
	set_fileWithExt(v) {
		if(v == null) {
			this.set_fileName(null);
			this.set_extension(null);
		} else {
			v = this.sanitize(v);
			this.parseFileName(v);
		}
		if(this.fileName == null && this.extension == null) {
			return null;
		} else {
			return (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension);
		}
	}
	get_extWithDot() {
		if(this.extension == null) {
			return null;
		} else {
			return "." + this.extension;
		}
	}
	get_full() {
		return (this.isWindowsNetworkDrive && !this._useWinNetDriveUriFormat ? this.backslashes ? "\\" : "/" : this.uriScheme != null ? this.uriAuthority == null ? "" + this.uriScheme + ":/" : "" + this.uriScheme + "://" + this.uriAuthority + "/" : "") + (this.directory == null ? "" : this.fileName == null && this.extension == null || this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory == null ? null : this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory + (this.backslashes ? "\\" : "/")) + ((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) == null ? "" : this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension));
	}
	get_directoryWithSlash() {
		if(this.directory == null) {
			return null;
		} else if(this.directory == (this.backslashes ? "\\" : "/")) {
			return this.directory;
		} else {
			return this.directory + (this.backslashes ? "\\" : "/");
		}
	}
	getDirectoryArray() {
		if(this.directory == null) {
			return [];
		} else if(this.directory == (this.backslashes ? "\\" : "/")) {
			return [this.backslashes ? "\\" : "/"];
		} else {
			return this.directory.split(this.backslashes ? "\\" : "/");
		}
	}
	getSubDirectories(discardDriveLetter) {
		let parts = this.getDirectoryArray();
		if(parts.length == 0) {
			return [];
		}
		let subs = [];
		let _g = 0;
		let _g1 = parts.length;
		while(_g < _g1) {
			let i = _g++;
			if(discardDriveLetter && i == 0 && this.getDriveLetter() != null) {
				continue;
			}
			let sub = [];
			let _g1 = 0;
			let _g2 = i + 1;
			while(_g1 < _g2) {
				let j = _g1++;
				sub.push(parts[j]);
			}
			let p = sub.join(this.backslashes ? "\\" : "/");
			if(p == "") {
				p = this.backslashes ? "\\" : "/";
			}
			subs.push(p);
		}
		return subs;
	}
	getDirectoryAndFileArray() {
		let out = this.getDirectoryArray();
		if((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) != null) {
			out.push(this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension));
		}
		return out;
	}
	getLastDirectory() {
		if(this.directory == null) {
			return null;
		}
		let arr = this.getDirectoryArray();
		return arr[arr.length - 1];
	}
	removeLastDirectory() {
		if(this.directory == null) {
			return this;
		}
		let arr = this.getDirectoryArray();
		arr.pop();
		if(arr.length == 0) {
			this.directory = null;
		} else {
			this.directory = arr.join(this.backslashes ? "\\" : "/");
		}
		return this;
	}
	getFirstDirectory() {
		if(this.directory == null) {
			return null;
		}
		let arr = this.getDirectoryArray();
		if(this.getDriveLetter() != null) {
			return arr[1];
		} else {
			return arr[0];
		}
	}
	removeFirstDirectory() {
		if(this.directory == null) {
			return this;
		}
		let arr = this.getDirectoryArray();
		if(this.getDriveLetter() != null) {
			arr.splice(1,1);
		} else {
			arr.shift();
		}
		if(arr.length == 0) {
			this.directory = null;
		} else {
			this.directory = arr.join(this.backslashes ? "\\" : "/");
		}
		return this;
	}
	toString() {
		return (this.isWindowsNetworkDrive && !this._useWinNetDriveUriFormat ? this.backslashes ? "\\" : "/" : this.uriScheme != null ? this.uriAuthority == null ? "" + this.uriScheme + ":/" : "" + this.uriScheme + "://" + this.uriAuthority + "/" : "") + (this.directory == null ? "" : this.fileName == null && this.extension == null || this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory == null ? null : this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory + (this.backslashes ? "\\" : "/")) + ((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) == null ? "" : this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension));
	}
	debug() {
		return "dir=" + this.directory + ", fileName=" + this.fileName + ", ext=" + this.extension + ", uri=" + this.uriScheme + "+" + this.uriAuthority + ", drive=" + this.getDriveLetter();
	}
	hasMultipleExtensions() {
		if((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) != null) {
			return (this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)).indexOf(".") != (this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)).lastIndexOf(".");
		} else {
			return false;
		}
	}
	getMultipleExtensions() {
		if((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) == null) {
			return [];
		}
		let parts = (this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)).split(".");
		parts.shift();
		return parts;
	}
	fromString(v) {
		let p = new dn_FilePath();
		p.parse(v,true);
		return p;
	}
	getFull() {
		return (this.isWindowsNetworkDrive && !this._useWinNetDriveUriFormat ? this.backslashes ? "\\" : "/" : this.uriScheme != null ? this.uriAuthority == null ? "" + this.uriScheme + ":/" : "" + this.uriScheme + "://" + this.uriAuthority + "/" : "") + (this.directory == null ? "" : this.fileName == null && this.extension == null || this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory == null ? null : this.directory == (this.backslashes ? "\\" : "/") ? this.directory : this.directory + (this.backslashes ? "\\" : "/")) + ((this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension)) == null ? "" : this.fileName == null && this.extension == null ? null : (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension));
	}
	getFileAndExt() {
		if(this.fileName == null && this.extension == null) {
			return null;
		} else {
			return (this.fileName == null ? "" : this.fileName) + (this.extension == null ? "" : "." + this.extension);
		}
	}
	get_path() {
		return this.directory;
	}
	get_file() {
		return this.fileName;
	}
	get_ext() {
		return this.extension;
	}
	set_ext(v) {
		return this.set_extension(v);
	}
	static convertToSlashes(path) {
		return StringTools.replace(path,"\\","/");
	}
	static convertToBackslashes(path) {
		return StringTools.replace(path,"/","\\");
	}
	static sameDriveLetter(pathA,pathB) {
		if(pathA == null || pathB == null) {
			return true;
		} else {
			let p = new dn_FilePath();
			p.parse(pathA,false);
			let tmp = p.getDriveLetter();
			let p1 = new dn_FilePath();
			p1.parse(pathB,false);
			return tmp == p1.getDriveLetter();
		}
	}
	static cleanUpFileName(fileName,replaceWith) {
		if(replaceWith == null) {
			replaceWith = "_";
		}
		if(fileName == null) {
			return "";
		} else {
			let _this_r = new RegExp("[*{}/\\\\<>?|:]","g".split("u").join(""));
			return fileName.replace(_this_r,replaceWith);
		}
	}
	static fromFile(path) {
		let p = new dn_FilePath();
		p.parse(path,true);
		return p;
	}
	static fromDir(path) {
		let p = new dn_FilePath();
		p.parse(path,false);
		return p;
	}
	static cleanUp(path,isFile) {
		let p;
		if(isFile) {
			let p1 = new dn_FilePath();
			p1.parse(path,true);
			p = p1;
		} else {
			let p1 = new dn_FilePath();
			p1.parse(path,false);
			p = p1;
		}
		return (p.isWindowsNetworkDrive && !p._useWinNetDriveUriFormat ? p.backslashes ? "\\" : "/" : p.uriScheme != null ? p.uriAuthority == null ? "" + p.uriScheme + ":/" : "" + p.uriScheme + "://" + p.uriAuthority + "/" : "") + (p.directory == null ? "" : p.fileName == null && p.extension == null || p.directory == (p.backslashes ? "\\" : "/") ? p.directory : p.directory == null ? null : p.directory == (p.backslashes ? "\\" : "/") ? p.directory : p.directory + (p.backslashes ? "\\" : "/")) + ((p.fileName == null && p.extension == null ? null : (p.fileName == null ? "" : p.fileName) + (p.extension == null ? "" : "." + p.extension)) == null ? "" : p.fileName == null && p.extension == null ? null : (p.fileName == null ? "" : p.fileName) + (p.extension == null ? "" : "." + p.extension));
	}
	static extractFileWithExt(path) {
		let p = new dn_FilePath();
		p.parse(path,true);
		let _this = p;
		if(_this.fileName == null && _this.extension == null) {
			return null;
		} else {
			return (_this.fileName == null ? "" : _this.fileName) + (_this.extension == null ? "" : "." + _this.extension);
		}
	}
	static extractFileName(path) {
		let p = new dn_FilePath();
		p.parse(path,true);
		return p.fileName;
	}
	static extractExtension(path,lowercase) {
		if(lowercase == null) {
			lowercase = false;
		}
		let p = new dn_FilePath();
		p.parse(path,true);
		let e = p.extension;
		if(lowercase && e != null) {
			return e.toLowerCase();
		} else {
			return e;
		}
	}
	static extractDirectoryWithoutSlash(path,containsFileName) {
		if(containsFileName) {
			let p = new dn_FilePath();
			p.parse(path,true);
			return p.directory;
		} else {
			let p = new dn_FilePath();
			p.parse(path,false);
			return p.directory;
		}
	}
	static extractDirectoryWithSlash(path,containsFileName) {
		if(containsFileName) {
			let p = new dn_FilePath();
			p.parse(path,true);
			let _this = p;
			if(_this.directory == null) {
				return null;
			} else if(_this.directory == (_this.backslashes ? "\\" : "/")) {
				return _this.directory;
			} else {
				return _this.directory + (_this.backslashes ? "\\" : "/");
			}
		} else {
			let p = new dn_FilePath();
			p.parse(path,false);
			let _this = p;
			if(_this.directory == null) {
				return null;
			} else if(_this.directory == (_this.backslashes ? "\\" : "/")) {
				return _this.directory;
			} else {
				return _this.directory + (_this.backslashes ? "\\" : "/");
			}
		}
	}
	static fullPath(v) {
		let p = new dn_FilePath();
		p.parse(v,true);
		let _this = p;
		return (_this.isWindowsNetworkDrive && !_this._useWinNetDriveUriFormat ? _this.backslashes ? "\\" : "/" : _this.uriScheme != null ? _this.uriAuthority == null ? "" + _this.uriScheme + ":/" : "" + _this.uriScheme + "://" + _this.uriAuthority + "/" : "") + (_this.directory == null ? "" : _this.fileName == null && _this.extension == null || _this.directory == (_this.backslashes ? "\\" : "/") ? _this.directory : _this.directory == null ? null : _this.directory == (_this.backslashes ? "\\" : "/") ? _this.directory : _this.directory + (_this.backslashes ? "\\" : "/")) + ((_this.fileName == null && _this.extension == null ? null : (_this.fileName == null ? "" : _this.fileName) + (_this.extension == null ? "" : "." + _this.extension)) == null ? "" : _this.fileName == null && _this.extension == null ? null : (_this.fileName == null ? "" : _this.fileName) + (_this.extension == null ? "" : "." + _this.extension));
	}
	static fileAndExt(v) {
		let p = new dn_FilePath();
		p.parse(v,true);
		let _this = p;
		if(_this.fileName == null && _this.extension == null) {
			return null;
		} else {
			return (_this.fileName == null ? "" : _this.fileName) + (_this.extension == null ? "" : "." + _this.extension);
		}
	}
}
dn_FilePath.__name__ = "dn.FilePath";
Object.assign(dn_FilePath.prototype, {
	__class__: dn_FilePath
});
var dn_WeekDay = $hxEnums["dn.WeekDay"] = { __ename__:true,__constructs__:null
	,Sunday: {_hx_name:"Sunday",_hx_index:0,__enum__:"dn.WeekDay",toString:$estr}
	,Monday: {_hx_name:"Monday",_hx_index:1,__enum__:"dn.WeekDay",toString:$estr}
	,Tuesday: {_hx_name:"Tuesday",_hx_index:2,__enum__:"dn.WeekDay",toString:$estr}
	,Wednesday: {_hx_name:"Wednesday",_hx_index:3,__enum__:"dn.WeekDay",toString:$estr}
	,Thursday: {_hx_name:"Thursday",_hx_index:4,__enum__:"dn.WeekDay",toString:$estr}
	,Friday: {_hx_name:"Friday",_hx_index:5,__enum__:"dn.WeekDay",toString:$estr}
	,Saturday: {_hx_name:"Saturday",_hx_index:6,__enum__:"dn.WeekDay",toString:$estr}
};
dn_WeekDay.__constructs__ = [dn_WeekDay.Sunday,dn_WeekDay.Monday,dn_WeekDay.Tuesday,dn_WeekDay.Wednesday,dn_WeekDay.Thursday,dn_WeekDay.Friday,dn_WeekDay.Saturday];
class dn_Lib {
	static p(v) {
		dn_Lib.println(v);
	}
	static println(v) {
		console.log(Std.string(v));
	}
	static print(v) {
		console.log(Std.string(v));
	}
	static printArray(v) {
		let _g = 0;
		while(_g < v.length) {
			let e = v[_g];
			++_g;
			dn_Lib.println(e);
		}
	}
	static safeEscape(str,escapedChar) {
		let r_r = new RegExp("([^\\\\]|^)\\" + escapedChar,"gim".split("u").join(""));
		return str.replace(r_r,"$1\\" + escapedChar);
	}
	static countDaysUntil(now,day) {
		let delta = day._hx_index - now.getDay();
		if(delta < 0) {
			return 7 + delta;
		} else {
			return delta;
		}
	}
	static getWeekDay(date) {
		return Type.createEnumIndex(dn_WeekDay,date.getDay());
	}
	static setTime(date,h,m,s) {
		if(s == null) {
			s = 0;
		}
		if(m == null) {
			m = 0;
		}
		let str = "%Y-%m-%d " + StringTools.lpad("" + h,"0",2) + ":" + StringTools.lpad("" + m,"0",2) + ":" + StringTools.lpad("" + s,"0",2);
		return HxOverrides.strDate(DateTools.format(date,str));
	}
	static countDeltaDays(now,next) {
		let str = "%Y-%m-%d " + StringTools.lpad("" + 5,"0",2) + ":" + StringTools.lpad("" + 0,"0",2) + ":" + StringTools.lpad("" + 0,"0",2);
		let now1 = HxOverrides.strDate(DateTools.format(now,str));
		let str1 = "%Y-%m-%d " + StringTools.lpad("" + 5,"0",2) + ":" + StringTools.lpad("" + 0,"0",2) + ":" + StringTools.lpad("" + 0,"0",2);
		let next1 = HxOverrides.strDate(DateTools.format(next,str1));
		let x = (next1.getTime() - now1.getTime()) / 86400000.;
		return (x | 0) + (x >= 0 || x == (x | 0) ? 0 : -1);
	}
	static leadingZeros(s,zeros) {
		if(zeros == null) {
			zeros = 2;
		}
		let str = Std.string(s);
		while(str.length < zeros) str = "0" + str;
		return str;
	}
	static wtrim(str) {
		if(str == null) {
			return "";
		} else {
			str = str.replace(dn_Lib.L_WHITESPACE_TRIM.r,"");
			str = str.replace(dn_Lib.R_WHITESPACE_TRIM.r,"");
			return str;
		}
	}
	static repeatChar(c,n) {
		let out = "";
		let _g = 0;
		let _g1 = n;
		while(_g < _g1) {
			let i = _g++;
			out += c;
		}
		return out;
	}
	static intToRoman(n) {
		if(n == 0) {
			return "N";
		}
		if(n < 0 || n > 4999) {
			return "OutOfBounds";
		}
		let values = [1000,900,500,400,100,90,50,40,10,9,5,4,1];
		let numerals = ["M","CM","D","CD","C","XC","L","XL","X","IX","V","IV","I"];
		let out = "";
		let _g = 0;
		let _g1 = values.length;
		while(_g < _g1) {
			let i = _g++;
			while(n >= values[i]) {
				n -= values[i];
				out += numerals[i];
			}
		}
		return out;
	}
	static padLeft(str,minLen,padChar) {
		if(padChar == null) {
			padChar = " ";
		}
		while(str.length < minLen) str = padChar + str;
		return str;
	}
	static padRight(str,minLen,padChar) {
		if(padChar == null) {
			padChar = " ";
		}
		while(str.length < minLen) str += padChar;
		return str;
	}
	static padCenter(str,minLen,padChar) {
		if(padChar == null) {
			padChar = " ";
		}
		let side = true;
		while(str.length < minLen) {
			if(side) {
				str += padChar;
			} else {
				str = padChar + str;
			}
			side = !side;
		}
		return str;
	}
	static shuffleArray(arr,randFunc) {
		let m = arr.length;
		let i = 0;
		let tmp = null;
		while(m > 0) {
			i = randFunc(m--);
			tmp = arr[m];
			arr[m] = arr[i];
			arr[i] = tmp;
		}
	}
	static shuffleVector(arr,randFunc) {
		let m = arr.length;
		let i = 0;
		let tmp = null;
		while(m > 0) {
			i = randFunc(m);
			--m;
			tmp = arr[m];
			arr[m] = arr[i];
			arr[i] = tmp;
		}
	}
	static findInArray(arr,checkElement,defaultIfNotFound) {
		let _g = 0;
		while(_g < arr.length) {
			let e = arr[_g];
			++_g;
			if(checkElement(e)) {
				return e;
			}
		}
		return defaultIfNotFound;
	}
	static arrayContains(arr,v,checkElement) {
		let _g = 0;
		while(_g < arr.length) {
			let e = arr[_g];
			++_g;
			if(checkElement != null && checkElement(e) || e == v) {
				return true;
			}
		}
		return false;
	}
	static findBestInArray(arr,scoreElement) {
		if(arr.length == 0) {
			return null;
		}
		let best = arr[0];
		let _g = 0;
		while(_g < arr.length) {
			let e = arr[_g];
			++_g;
			if(scoreElement(e) > scoreElement(best)) {
				best = e;
			}
		}
		return best;
	}
	static findMostFrequentValueInArray(arr,isEqual) {
		if(arr.length == 0) {
			return null;
		}
		let bestCount = 1;
		let best = arr[0];
		let _g = 0;
		while(_g < arr.length) {
			let e = arr[_g];
			++_g;
			if(isEqual == null && e == best || isEqual != null && isEqual(e,best)) {
				continue;
			}
			let count = 0;
			let _g1 = 0;
			while(_g1 < arr.length) {
				let ee = arr[_g1];
				++_g1;
				if(isEqual == null && ee == e || isEqual != null && isEqual(e,ee)) {
					++count;
				}
			}
			if(count > bestCount) {
				bestCount = count;
				best = e;
			}
		}
		return best;
	}
	static splitArray(arr,splitCount) {
		if(splitCount <= 1) {
			return [arr];
		}
		let out = [];
		let x = arr.length / splitCount;
		let splitSize;
		if(x > .0) {
			let t = x + .5 | 0;
			splitSize = t < x ? t + 1 : t;
		} else if(x < .0) {
			let t = x - .5 | 0;
			splitSize = t < x ? t + 1 : t;
		} else {
			splitSize = 0;
		}
		let _g = 0;
		let _g1 = splitCount;
		while(_g < _g1) {
			let i = _g++;
			let start = i * splitSize;
			let x = (i + 1) * splitSize;
			let y = arr.length;
			let end = x < y ? x : y;
			if(start < end) {
				out.push(arr.slice(start,end));
			}
		}
		while(out.length < splitCount) out.push([]);
		return out;
	}
	static randomSpread(total,nbStacks,maxStackValue,randFunc) {
		if(total <= 0 || nbStacks <= 0) {
			return [];
		}
		if(maxStackValue != null && total / nbStacks > maxStackValue) {
			let a = [];
			let _g = 0;
			let _g1 = nbStacks;
			while(_g < _g1) {
				let i = _g++;
				a.push(maxStackValue);
			}
			return a;
		}
		if(nbStacks > total) {
			let a = [];
			let _g = 0;
			let _g1 = total;
			while(_g < _g1) {
				let i = _g++;
				a.push(1);
			}
			return a;
		}
		let plist = [];
		let _g = 0;
		let _g1 = nbStacks;
		while(_g < _g1) {
			let i = _g++;
			plist[i] = 1;
		}
		let remain = total - plist.length;
		while(remain > 0) {
			let x = total * (randFunc(8) + 1) / 100;
			let move;
			if(x > .0) {
				let t = x + .5 | 0;
				move = t < x ? t + 1 : t;
			} else if(x < .0) {
				let t = x - .5 | 0;
				move = t < x ? t + 1 : t;
			} else {
				move = 0;
			}
			if(move > remain) {
				move = remain;
			}
			let p = randFunc(nbStacks);
			if(maxStackValue != null && plist[p] + move > maxStackValue) {
				move = maxStackValue - plist[p];
			}
			plist[p] += move;
			remain -= move;
		}
		return plist;
	}
	static replaceTag(str,char,open,close) {
		let char1 = "\\" + char.split("").join("\\");
		let re = char1 + "([^" + char1 + "]+)" + char1;
		try {
			let _this_r = new RegExp(re,"g".split("u").join(""));
			return str.replace(_this_r,open + "$1" + close);
		} catch( _g ) {
			if(typeof(haxe_Exception.caught(_g).unwrap()) == "string") {
				return str;
			} else {
				throw _g;
			}
		}
	}
	static getNextPower2_32bits(n) {
		--n;
		n |= n >> 1;
		n |= n >> 2;
		n |= n >> 4;
		n |= n >> 8;
		n |= n >> 16;
		return n + 1;
	}
	static getNextPower2_8bits(n) {
		--n;
		n |= n >> 1;
		n |= n >> 2;
		n |= n >> 4;
		return n + 1;
	}
	static rnd(min,max,sign) {
		if(sign == null) {
			sign = false;
		}
		if(sign) {
			return (min + Math.random() * (max - min)) * (Std.random(2) * 2 - 1);
		} else {
			return min + Math.random() * (max - min);
		}
	}
	static irnd(min,max,sign) {
		if(sign == null) {
			sign = false;
		}
		if(sign) {
			return (min + Std.random(max - min + 1)) * (Std.random(2) * 2 - 1);
		} else {
			return min + Std.random(max - min + 1);
		}
	}
	static prettyTime(t) {
		if(t <= 31536000000.) {
			let s = (t < 0 ? -t : t) / 1000;
			let m = s / 60;
			let h = m / 60;
			h = h % 24 | 0;
			let tmp = (t < 0 ? "-" : "") + (h > 0 ? h + "h " : "");
			let str = Std.string(m % 60 | 0);
			while(str.length < 2) str = "0" + str;
			let tmp1 = tmp + str + "m ";
			let str1 = Std.string(s % 60 | 0);
			while(str1.length < 2) str1 = "0" + str1;
			return tmp1 + str1 + "s";
		} else {
			return DateTools.format(new Date(t),"%Hh %Mm %Ss (%Y-%m-%d)");
		}
	}
	static ludumProtection(p,allowLocal) {
		if(allowLocal == null) {
			allowLocal = true;
		}
		let ok = false;
		let s = "d" + "e" + "e" + "p" + "" + "n" + "i" + "" + "g" + "" + "h" + "t";
		let d = window.document.URL;
		ok = d.indexOf(s + "." + "n" + "e" + "t") >= 0 || allowLocal && d.indexOf("file://") == 0;
		if(!ok) {
			p.delayer.addS(null,function() {
				p.cd = null;
			},4 + Math.random() * 3);
		}
		return ok;
	}
	static getEnumMetaFloat(e,varName,def) {
		if(def == null) {
			def = 0.;
		}
		let meta = haxe_rtti_Meta.getFields(Type.getEnum(e));
		if(meta == null) {
			return def;
		}
		let f = Reflect.field(meta,$hxEnums[e.__enum__].__constructs__[e._hx_index]._hx_name);
		if(f == null || !Object.prototype.hasOwnProperty.call(f,varName) || typeof(Reflect.field(f,varName)[0]) != "number") {
			return def;
		}
		let v = Reflect.field(f,varName)[0];
		if(isNaN(v)) {
			return def;
		} else {
			return v;
		}
	}
	static getEnumMetaInt(e,varName,def) {
		if(def == null) {
			def = 0;
		}
		return dn_Lib.getEnumMetaFloat(e,varName,def) | 0;
	}
	static getArrayIndex(v,arr) {
		if(arr.length == 0) {
			return -1;
		}
		let _g = 0;
		let _g1 = arr.length;
		while(_g < _g1) {
			let i = _g++;
			if(arr[i] == v) {
				return i;
			}
		}
		return -1;
	}
	static getArrayValueBefore(v,arr) {
		if(arr.length == 0) {
			return v;
		}
		let idx = dn_Lib.getArrayIndex(v,arr);
		if(idx > 0 && idx < arr.length) {
			return arr[idx - 1];
		} else {
			return v;
		}
	}
	static getArrayValueAfter(v,arr) {
		if(arr.length == 0) {
			return v;
		}
		let idx = dn_Lib.getArrayIndex(v,arr);
		if(idx >= 0 && idx < arr.length - 1) {
			return arr[idx + 1];
		} else {
			return v;
		}
	}
	static prettyBytesSize(bytesCount) {
		if(bytesCount <= 1024) {
			return "" + bytesCount + " bytes";
		} else if(bytesCount <= 1048576) {
			let v = bytesCount / 1024;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			return "" + tmp + " Kb";
		} else {
			let v = bytesCount / 1048576;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			return "" + tmp + " Mb";
		}
	}
	static parseSignedInt(v) {
		return Std.parseInt(v) & -1;
	}
	static rectangleTouches(aX,aY,aWid,aHei,bX,bY,bWid,bHei) {
		if(aY + aHei < bY || bY + bHei < aY) {
			return false;
		} else if(aX + aWid < bX || bX + bWid < aX) {
			return false;
		} else {
			return true;
		}
	}
	static rectangleOverlaps(aX,aY,aWid,aHei,bX,bY,bWid,bHei) {
		if(aY + aHei <= bY || bY + bHei <= aY) {
			return false;
		} else if(aX + aWid <= bX || bX + bWid <= aX) {
			return false;
		} else {
			return true;
		}
	}
	static iterateArrayRec(arr,cb) {
		let _g = 0;
		let _g1 = arr.length;
		while(_g < _g1) {
			let i = _g++;
			let _g1 = Type.typeof(arr[i]);
			switch(_g1._hx_index) {
			case 4:
				dn_Lib.iterateObjectRec(arr[i],cb);
				break;
			case 6:
				if(_g1.c == Array) {
					dn_Lib.iterateArrayRec(arr[i],cb);
				}
				break;
			default:
			}
			cb(arr[i],function(v) {
				arr[i] = v;
			});
		}
	}
	static iterateObjectRec(obj,cb) {
		if(obj == null) {
			return;
		}
		let _g = 0;
		let _g1 = Reflect.fields(obj);
		while(_g < _g1.length) {
			let k = _g1[_g];
			++_g;
			let f = Reflect.field(obj,k);
			let _g2 = Type.typeof(f);
			switch(_g2._hx_index) {
			case 4:
				dn_Lib.iterateObjectRec(f,cb);
				break;
			case 6:
				if(_g2.c == Array) {
					dn_Lib.iterateArrayRec(f,cb);
				}
				break;
			default:
			}
			cb(f,function(v) {
				obj[k] = v;
			});
		}
	}
	static trimEmptyLines(str) {
		let lines = str.split("\n");
		while(lines.length > 0 && StringTools.trim(lines[0]).length == 0) lines.shift();
		while(lines.length > 0 && StringTools.trim(lines[lines.length - 1]).length == 0) lines.pop();
		return lines.join("\n");
	}
	static isVowel(c) {
		if(c != null && c.length >= 1) {
			return Object.prototype.hasOwnProperty.call(dn_Lib.VOWELS.h,c.charAt(0).toLowerCase());
		} else {
			return false;
		}
	}
	static isConsonant(c) {
		if(c != null && c.length >= 1 && !Object.prototype.hasOwnProperty.call(dn_Lib.VOWELS.h,c.charAt(0).toLowerCase())) {
			if(!(c >= "a" && c <= "z")) {
				if(c >= "A") {
					return c <= "Z";
				} else {
					return false;
				}
			} else {
				return true;
			}
		} else {
			return false;
		}
	}
	static groupArray(arr) {
		let out = [];
		let _g = 0;
		let _g1 = arr.length;
		while(_g < _g1) {
			let i = _g++;
			let v = arr[i];
			let found = false;
			let _g1 = 0;
			while(_g1 < out.length) {
				let e = out[_g1];
				++_g1;
				if(e.value == v) {
					found = true;
					break;
				}
			}
			if(found) {
				continue;
			}
			let n = 1;
			let _g2 = i + 1;
			let _g3 = arr.length;
			while(_g2 < _g3) {
				let j = _g2++;
				if(arr[j] == v) {
					++n;
				}
			}
			out.push({ value : v, count : n});
		}
		return out;
	}
	static prettyGroupedArray(arr,join) {
		if(join == null) {
			join = ",";
		}
		let _this = dn_Lib.groupArray(arr);
		let result = new Array(_this.length);
		let _g = 0;
		let _g1 = _this.length;
		while(_g < _g1) {
			let i = _g++;
			let g = _this[i];
			result[i] = g.count == 1 ? Std.string(g.value) : "" + Std.string(g.value) + "(" + g.count + ")";
		}
		return result.join(join);
	}
	static buildShortName(longName,maxLen) {
		if(maxLen == null) {
			maxLen = 8;
		}
		let out = "";
		let i = 0;
		let c = "";
		while(out.length < maxLen && i < longName.length) {
			c = longName.charAt(i);
			if(i == 0) {
				out += c;
			} else if(c != null && c.length >= 1 && !Object.prototype.hasOwnProperty.call(dn_Lib.VOWELS.h,c.charAt(0).toLowerCase()) && (c >= "a" && c <= "z" || c >= "A" && c <= "Z")) {
				out += c;
			} else if(c >= "A" && c <= "Z") {
				out += c;
			}
			++i;
		}
		return out;
	}
}
dn_Lib.__name__ = "dn.Lib";
class dn_M {
	static NaN() {
		return NaN;
	}
	static POSITIVE_INFINITY() {
		return Infinity;
	}
	static NEGATIVE_INFINITY() {
		return -Infinity;
	}
	static toRad(deg) {
		return deg * 0.0174532925199432955;
	}
	static toDeg(rad) {
		return rad * 57.2957795130823229;
	}
	static imin(x,y) {
		if(x < y) {
			return x;
		} else {
			return y;
		}
	}
	static imax(x,y) {
		if(x > y) {
			return x;
		} else {
			return y;
		}
	}
	static iabs(x) {
		if(x < 0) {
			return -x;
		} else {
			return x;
		}
	}
	static sign(x) {
		if(x > 0) {
			return 1;
		} else if(x < 0) {
			return -1;
		} else {
			return 0;
		}
	}
	static signEq(x,y) {
		return (x > 0 ? 1 : x < 0 ? -1 : 0) == (y > 0 ? 1 : y < 0 ? -1 : 0);
	}
	static notZero(v,ifZero) {
		if(ifZero == null) {
			ifZero = 0.000001;
		}
		if(v == 0) {
			return ifZero;
		} else {
			return v;
		}
	}
	static iclamp(x,min,max) {
		if(x < min) {
			return min;
		} else if(x > max) {
			return max;
		} else {
			return x;
		}
	}
	static clampSym(x,i) {
		if(x < -i) {
			return -i;
		} else if(x > i) {
			return i;
		} else {
			return x;
		}
	}
	static wrap(x,min,max) {
		if(x < min) {
			return x - min + max + 1;
		} else if(x > max) {
			return x - max + min - 1;
		} else {
			return x;
		}
	}
	static fmin(x,y) {
		if(x < y) {
			return x;
		} else {
			return y;
		}
	}
	static fmax(x,y) {
		if(x > y) {
			return x;
		} else {
			return y;
		}
	}
	static fabs(x) {
		if(x < 0) {
			return -x;
		} else {
			return x;
		}
	}
	static fsgn(x) {
		if(x > 0.) {
			return 1;
		} else if(x < 0.) {
			return -1;
		} else {
			return 0;
		}
	}
	static fclamp(x,min,max) {
		if(x < min) {
			return min;
		} else if(x > max) {
			return max;
		} else {
			return x;
		}
	}
	static fclampSym(x,i) {
		if(x < -i) {
			return -i;
		} else if(x > i) {
			return i;
		} else {
			return x;
		}
	}
	static fwrap(value,lower,upper) {
		return value - ((value - lower) / (upper - lower) | 0) * (upper - lower);
	}
	static eqSgn(x,y) {
		return (x ^ y) >= 0;
	}
	static feqSgn(x,y) {
		return x * y >= 0;
	}
	static isEven(x) {
		return (x & 1) == 0;
	}
	static isPow2(x) {
		if(x > 0) {
			return (x & x - 1) == 0;
		} else {
			return false;
		}
	}
	static nearestPow2(x) {
		return Math.pow(2,Math.round(Math.log(x) / Math.log(2)));
	}
	static lerp(from,to,t) {
		return from + (to - from) * t;
	}
	static lerpTmod(from,to,t,tmod) {
		let x = t * tmod;
		return from + (to - from) * (x < 1 ? x : 1);
	}
	static slerp(a,b,t) {
		let m = Math;
		let c1 = m.sin(a * .5);
		let r1 = m.cos(a * .5);
		let c2 = m.sin(b * .5);
		let r2 = m.cos(b * .5);
		let c = r1 * r2 + c1 * c2;
		if(c < 0.) {
			if(1. + c > 1e-6) {
				let o = m.acos(-c);
				let s = m.sin(o);
				let s0 = m.sin((1 - t) * o) / s;
				let s1 = m.sin(t * o) / s;
				return m.atan2(s0 * c1 - s1 * c2,s0 * r1 - s1 * r2) * 2.;
			} else {
				let s0 = 1 - t;
				let s1 = t;
				return m.atan2(s0 * c1 - s1 * c2,s0 * r1 - s1 * r2) * 2;
			}
		} else if(1 - c > 1e-6) {
			let o = m.acos(c);
			let s = m.sin(o);
			let s0 = m.sin((1 - t) * o) / s;
			let s1 = m.sin(t * o) / s;
			return m.atan2(s0 * c1 + s1 * c2,s0 * r1 + s1 * r2) * 2.;
		} else {
			let s0 = 1 - t;
			let s1 = t;
			return m.atan2(s0 * c1 + s1 * c2,s0 * r1 + s1 * r2) * 2;
		}
	}
	static nextPow2(x) {
		let t = x;
		t |= t >> 1;
		t |= t >> 2;
		t |= t >> 3;
		t |= t >> 4;
		t |= t >> 5;
		return t + 1;
	}
	static exp(a,n) {
		let t = 1;
		let r = 0;
		while(true) {
			if((n & 1) != 0) {
				t = a * t;
			}
			n >>= 1;
			if(n == 0) {
				r = t;
				break;
			} else {
				a *= a;
			}
		}
		return r;
	}
	static decimals(x) {
		let x1 = x - (x | 0);
		if(x1 < 0) {
			return -x1;
		} else {
			return x1;
		}
	}
	static roundTo(x,y) {
		let x1 = x / y;
		return ((x1 > 0 ? x1 + .5 : x1 < 0 ? x1 - .5 : 0) | 0) * y;
	}
	static round(x) {
		return (x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0;
	}
	static ceil(x) {
		if(x > .0) {
			let t = x + .5 | 0;
			if(t < x) {
				return t + 1;
			} else {
				return t;
			}
		} else if(x < .0) {
			let t = x - .5 | 0;
			if(t < x) {
				return t + 1;
			} else {
				return t;
			}
		} else {
			return 0;
		}
	}
	static floor(x) {
		return (x | 0) + (x >= 0 || x == (x | 0) ? 0 : -1);
	}
	static invSqrt(x) {
		return 1 / Math.sqrt(x);
	}
	static cmpAbs(x,y,eps) {
		let d = x - y;
		if(d > 0) {
			return d < eps;
		} else {
			return -d < eps;
		}
	}
	static cmpZero(x,eps) {
		if(x > 0) {
			return x < eps;
		} else {
			return -x < eps;
		}
	}
	static inRange(x,min,max) {
		if(x >= min) {
			return x <= max;
		} else {
			return false;
		}
	}
	static randSeedCoords(seed,x,y,max) {
		let h = seed + x * 374761393 + y * 668265263;
		h = (h ^ h >> 13) * 1274126177;
		return (h ^ h >> 16) % max;
	}
	static rand(max,rnd) {
		if(max == null) {
			max = 2147483647;
		}
		return (rnd == null ? Math.random() : rnd()) * max | 0;
	}
	static randRange(min,max,rnd) {
		let l = min - .4999;
		let h = max + .4999;
		let x = l + (h - l) * (rnd == null ? Math.random() : rnd());
		return (x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0;
	}
	static randRangeSym(range,rnd) {
		let l = -range - .4999;
		let h = range + .4999;
		let x = l + (h - l) * (rnd == null ? Math.random() : rnd());
		return (x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0;
	}
	static frand(rnd) {
		if(rnd == null) {
			return Math.random();
		} else {
			return rnd();
		}
	}
	static frandRange(min,max,rnd) {
		return min + (max - min) * (rnd == null ? Math.random() : rnd());
	}
	static frandRangeSym(range,rnd) {
		let min = -range;
		return min + (range - min) * (rnd == null ? Math.random() : rnd());
	}
	static wrapToPi(x) {
		let x1 = x / 6.283185307179586;
		let t = (x1 > 0 ? x1 + .5 : x1 < 0 ? x1 - .5 : 0) | 0;
		if(x < -3.14159265358979312) {
			return x - t * 6.283185307179586;
		} else if(x > 3.141592653589793) {
			return x - t * 6.283185307179586;
		} else {
			return x;
		}
	}
	static wrapTo(n,mod) {
		let x = n / mod;
		let t = (x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0;
		if(n < -2 * mod) {
			return n - t * mod;
		} else if(n > 2 * mod) {
			return n - t * mod;
		} else {
			return n;
		}
	}
	static sMod(n,mod) {
		if(mod != 0.0) {
			while(n >= mod) n -= mod;
			while(n < 0) n += mod;
		}
		return n;
	}
	static hMod(n,mod) {
		while(n > mod) n -= mod * 2;
		while(n < -mod) n += mod * 2;
		return n;
	}
	static gcd(x,y) {
		let d = 0;
		let r = 0;
		if(x < 0) {
			x = -x;
		}
		if(y < 0) {
			y = -y;
		}
		while(true) if(y == 0) {
			d = x;
			break;
		} else {
			r = x % y;
			x = y;
			y = r;
		}
		return d;
	}
	static maxPrecision(x,precision) {
		if(x == 0) {
			return x;
		} else {
			let correction = 10;
			let _g = 0;
			let _g1 = precision - 1;
			while(_g < _g1) {
				let i = _g++;
				correction *= 10;
			}
			let x1 = correction * x;
			return ((x1 > 0 ? x1 + .5 : x1 < 0 ? x1 - .5 : 0) | 0) / correction;
		}
	}
	static ofBool(x) {
		if(x) {
			return 1;
		} else {
			return 0;
		}
	}
	static posMod(i,m) {
		let mod = i % m;
		if(mod >= 0) {
			return mod;
		} else {
			return mod + m;
		}
	}
	static distSqr(ax,ay,bx,by) {
		return (ax - bx) * (ax - bx) + (ay - by) * (ay - by);
	}
	static idistSqr(ax,ay,bx,by) {
		return (ax - bx) * (ax - bx) + (ay - by) * (ay - by);
	}
	static dist(ax,ay,bx,by) {
		return Math.sqrt((ax - bx) * (ax - bx) + (ay - by) * (ay - by));
	}
	static distSegment(x,y,ax,ay,bx,by) {
		let l2 = (ax - bx) * (ax - bx) + (ay - by) * (ay - by);
		let tmp;
		if(l2 == 0) {
			tmp = (x - ax) * (x - ax) + (y - ay) * (y - ay);
		} else {
			let x1 = ((x - ax) * (bx - ax) + (y - ay) * (by - ay)) / l2;
			let t = x1 < 0 ? 0 : x1 > 1 ? 1 : x1;
			let bx1 = ax + t * (bx - ax);
			let by1 = ay + t * (by - ay);
			tmp = (x - bx1) * (x - bx1) + (y - by1) * (y - by1);
		}
		return Math.sqrt(tmp);
	}
	static distSegmentSqr(x,y,ax,ay,bx,by) {
		let l2 = (ax - bx) * (ax - bx) + (ay - by) * (ay - by);
		if(l2 == 0) {
			return (x - ax) * (x - ax) + (y - ay) * (y - ay);
		}
		let x1 = ((x - ax) * (bx - ax) + (y - ay) * (by - ay)) / l2;
		let t = x1 < 0 ? 0 : x1 > 1 ? 1 : x1;
		let bx1 = ax + t * (bx - ax);
		let by1 = ay + t * (by - ay);
		return (x - bx1) * (x - bx1) + (y - by1) * (y - by1);
	}
	static factorial(v) {
		let r = 1;
		let _g = 1;
		let _g1 = v + 1;
		while(_g < _g1) {
			let i = _g++;
			r *= i;
		}
		return r;
	}
	static snapValue(v,snapInterval) {
		let x = v / snapInterval;
		return ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) * snapInterval;
	}
	static normalizeDeg(a) {
		while(a < -180) a += 360;
		while(a > 180) a -= 360;
		return a;
	}
	static normalizeRad(a) {
		while(a < -3.14159265358979312) a += 6.283185307179586;
		while(a > 3.141592653589793) a -= 6.283185307179586;
		return a;
	}
	static degDistance(a,b) {
		let a1 = a;
		while(a1 < -180) a1 += 360;
		while(a1 > 180) a1 -= 360;
		let a2 = a1;
		let a3 = b;
		while(a3 < -180) a3 += 360;
		while(a3 > 180) a3 -= 360;
		let a4 = a2 - a3;
		while(a4 < -180) a4 += 360;
		while(a4 > 180) a4 -= 360;
		let x = a4;
		if(x < 0) {
			return -x;
		} else {
			return x;
		}
	}
	static degSubstract(a,b) {
		let a1 = a;
		while(a1 < -180) a1 += 360;
		while(a1 > 180) a1 -= 360;
		let a2 = a1;
		let a3 = b;
		while(a3 < -180) a3 += 360;
		while(a3 > 180) a3 -= 360;
		let a4 = a2 - a3;
		while(a4 < -180) a4 += 360;
		while(a4 > 180) a4 -= 360;
		return a4;
	}
	static radClamp(a,refAng,maxDelta) {
		let a1 = a;
		let b = refAng;
		let a2 = a1;
		while(a2 < -3.14159265358979312) a2 += 6.283185307179586;
		while(a2 > 3.141592653589793) a2 -= 6.283185307179586;
		a1 = a2;
		let a3 = b;
		while(a3 < -3.14159265358979312) a3 += 6.283185307179586;
		while(a3 > 3.141592653589793) a3 -= 6.283185307179586;
		b = a3;
		let a4 = a1 - b;
		while(a4 < -3.14159265358979312) a4 += 6.283185307179586;
		while(a4 > 3.141592653589793) a4 -= 6.283185307179586;
		let d = a4;
		if(d > maxDelta) {
			return refAng + maxDelta;
		}
		if(d < -maxDelta) {
			return refAng - maxDelta;
		}
		return a;
	}
	static radDistance(a,b) {
		let a1 = a;
		let b1 = b;
		let a2 = a1;
		while(a2 < -3.14159265358979312) a2 += 6.283185307179586;
		while(a2 > 3.141592653589793) a2 -= 6.283185307179586;
		a1 = a2;
		let a3 = b1;
		while(a3 < -3.14159265358979312) a3 += 6.283185307179586;
		while(a3 > 3.141592653589793) a3 -= 6.283185307179586;
		b1 = a3;
		let a4 = a1 - b1;
		while(a4 < -3.14159265358979312) a4 += 6.283185307179586;
		while(a4 > 3.141592653589793) a4 -= 6.283185307179586;
		let x = a4;
		if(x < 0) {
			return -x;
		} else {
			return x;
		}
	}
	static radCloseTo(curAng,target,maxAngDist) {
		let a = curAng;
		let b = target;
		let a1 = a;
		while(a1 < -3.14159265358979312) a1 += 6.283185307179586;
		while(a1 > 3.141592653589793) a1 -= 6.283185307179586;
		a = a1;
		let a2 = b;
		while(a2 < -3.14159265358979312) a2 += 6.283185307179586;
		while(a2 > 3.141592653589793) a2 -= 6.283185307179586;
		b = a2;
		let a3 = a - b;
		while(a3 < -3.14159265358979312) a3 += 6.283185307179586;
		while(a3 > 3.141592653589793) a3 -= 6.283185307179586;
		let x = a3;
		return (x < 0 ? -x : x) <= (maxAngDist < 0 ? -maxAngDist : maxAngDist);
	}
	static radSubstract(a,b) {
		let a1 = a;
		while(a1 < -3.14159265358979312) a1 += 6.283185307179586;
		while(a1 > 3.141592653589793) a1 -= 6.283185307179586;
		a = a1;
		let a2 = b;
		while(a2 < -3.14159265358979312) a2 += 6.283185307179586;
		while(a2 > 3.141592653589793) a2 -= 6.283185307179586;
		b = a2;
		let a3 = a - b;
		while(a3 < -3.14159265358979312) a3 += 6.283185307179586;
		while(a3 > 3.141592653589793) a3 -= 6.283185307179586;
		return a3;
	}
	static angTo(fx,fy,tx,ty) {
		return Math.atan2(ty - fy,tx - fx);
	}
	static pretty(v,precision) {
		if(precision == null) {
			precision = 2;
		}
		if(precision <= 0) {
			return (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
		} else {
			let d = Math.pow(10,precision);
			let x = v * d;
			return ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
		}
	}
	static prettyPct(v,precision) {
		if(precision == null) {
			precision = 0;
		}
		if(precision <= 0) {
			let x = v * 100;
			return ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) + "%";
		} else {
			let d = Math.pow(10,precision);
			let x = v * 100 * d;
			return ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d + "%";
		}
	}
	static truncateStr(v,maxPrecision) {
		if(maxPrecision == null) {
			maxPrecision = 2;
		}
		if(maxPrecision <= 0) {
			return Std.string(v | 0);
		} else {
			let d = Math.pow(10,maxPrecision);
			return Std.string((v * d | 0) / d);
		}
	}
	static prettyPad(v,precision) {
		if(precision == null) {
			precision = 2;
		}
		let precision1 = precision;
		if(precision1 == null) {
			precision1 = 2;
		}
		let str;
		if(precision1 <= 0) {
			str = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
		} else {
			let d = Math.pow(10,precision1);
			let x = v * d;
			str = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
		}
		let str1 = Std.string(str);
		if(precision == 0) {
			return str1;
		} else {
			let idx = str1.lastIndexOf(".");
			let n = idx < 0 ? precision : precision - (str1.length - idx - 1);
			if(idx < 0) {
				str1 += ".";
			}
			let _g = 0;
			let _g1 = n;
			while(_g < _g1) {
				let i = _g++;
				str1 += "0";
			}
			return str1;
		}
	}
	static addSign(v) {
		if(v >= 0) {
			return "+" + v;
		} else if(v == null) {
			return "null";
		} else {
			return "" + v;
		}
	}
	static groupNumbers(v,sep) {
		if(sep == null) {
			sep = " ";
		}
		let str = v == null ? "null" : "" + v;
		if(str.length <= 3) {
			return str;
		} else {
			let i = 1;
			let out = "";
			while(i <= str.length) {
				out = HxOverrides.substr(str,-i,1) + out;
				if(i % 3 == 0 && i < str.length) {
					out = sep + out;
				}
				++i;
			}
			return out;
		}
	}
	static unit(v,precision) {
		if(precision == null) {
			precision = 1;
		}
		if((v < 0 ? -v : v) < 1000) {
			let precision1 = precision;
			if(precision1 == null) {
				precision1 = 2;
			}
			let tmp;
			if(precision1 <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision1);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			return Std.string(tmp);
		} else if((v < 0 ? -v : v) < 1000000) {
			let v1 = v / 1000;
			let precision1 = precision;
			if(precision1 == null) {
				precision1 = 2;
			}
			let tmp;
			if(precision1 <= 0) {
				tmp = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision1);
				let x = v1 * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			return tmp + "K";
		} else {
			let v1 = v / 1000000;
			let precision1 = precision;
			if(precision1 == null) {
				precision1 = 2;
			}
			let tmp;
			if(precision1 <= 0) {
				tmp = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision1);
				let x = v1 * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			return tmp + "M";
		}
	}
	static unitMega(v,precision) {
		if(precision == null) {
			precision = 1;
		}
		let v1 = v / 1000000;
		let precision1 = precision;
		if(precision1 == null) {
			precision1 = 2;
		}
		let tmp;
		if(precision1 <= 0) {
			tmp = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
		} else {
			let d = Math.pow(10,precision1);
			let x = v1 * d;
			tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
		}
		return tmp + "M";
	}
	static intToBitString(v,pad) {
		if(pad == null) {
			pad = 8;
		}
		let out = "";
		let i = 0;
		while((0 | 1 << i) <= v && i < 31) out = ((v & 1 << i++) != 0 ? "1" : "0") + out;
		while(out.length < pad) out = "0" + out;
		return out;
	}
	static uIntToBitString(v,pad) {
		if(pad == null) {
			pad = 8;
		}
		let out = "";
		let i = 0;
		while(UInt.gte(v,0 | 1 << i) && UInt.gte(31,i)) {
			let one = 1;
			out = ((v & one << i++) != 0 ? "1" : "0") + out;
		}
		while(out.length < pad) out = "0" + out;
		return out;
	}
	static setBit(baseValue,bitIdx) {
		return baseValue | 1 << bitIdx;
	}
	static unsetBit(baseValue,bitIdx) {
		return baseValue & ~(1 << bitIdx);
	}
	static makeBitsFromArray(bools) {
		if(bools.length > 32) {
			throw haxe_Exception.thrown("Too many values (32bits max)");
		}
		let v = 0;
		let _g = 0;
		let _g1 = bools.length;
		while(_g < _g1) {
			let i = _g++;
			if(bools[i] == true) {
				v |= 1 << i;
			}
		}
		return v;
	}
	static makeBitsFromBools(b0,b1,b2,b3,b4,b5,b6,b7) {
		if(b7 == null) {
			b7 = false;
		}
		if(b6 == null) {
			b6 = false;
		}
		if(b5 == null) {
			b5 = false;
		}
		if(b4 == null) {
			b4 = false;
		}
		if(b3 == null) {
			b3 = false;
		}
		if(b2 == null) {
			b2 = false;
		}
		if(b1 == null) {
			b1 = false;
		}
		if(b0 == null) {
			b0 = false;
		}
		let v = 0;
		if(b0) {
			v |= 1;
		}
		if(b1) {
			v |= 2;
		}
		if(b2) {
			v |= 4;
		}
		if(b3) {
			v |= 8;
		}
		if(b4) {
			v |= 16;
		}
		if(b5) {
			v |= 32;
		}
		if(b6) {
			v |= 64;
		}
		if(b7) {
			v |= 128;
		}
		return v;
	}
	static hasBit(v,bitIdx) {
		return (v & 1 << bitIdx) != 0;
	}
	static setUnsignedBit(baseValue,bitIdx) {
		return baseValue | 1 << bitIdx;
	}
	static hasUnsignedBit(v,bitIdx) {
		let one = 1;
		return (v & one << bitIdx) != 0;
	}
	static isValidNumber(v) {
		if(v != null && !isNaN(v)) {
			return isFinite(v);
		} else {
			return false;
		}
	}
	static nullDefault(v,defaultIfNull) {
		let tmp = v;
		if(tmp != null) {
			return tmp;
		} else {
			return defaultIfNull;
		}
	}
	static parseInt(str,defaultIfInvalid) {
		if(str == null) {
			return defaultIfInvalid;
		} else {
			let v = Std.parseInt(str);
			if(v != null && !isNaN(v) && isFinite(v)) {
				return v;
			} else {
				return defaultIfInvalid;
			}
		}
	}
	static bezier4(t,p0,p1,p2,p3) {
		if(t < 0) {
			t = 0;
		} else if(t > 1) {
			t = 1;
		}
		let n = 1 - t;
		let n1 = 1 - t;
		return n * n * n * p0 + 3 * t * (n1 * n1) * p1 + 3 * (t * t) * (1 - t) * p2 + t * t * t * p3;
	}
	static bezier3(t,p0,p1,p2) {
		if(t < 0) {
			t = 0;
		} else if(t > 1) {
			t = 1;
		}
		let n = 1 - t;
		return n * n * p0 + 2 * t * (1 - t) * p1 + t * t * p2;
	}
	static bezierFull4(t,x0,x1,x2,x3,y0,y1,y2,y3) {
		let t1 = t;
		if(t1 < 0) {
			t1 = 0;
		} else if(t1 > 1) {
			t1 = 1;
		}
		let n = 1 - t1;
		let n1 = 1 - t1;
		let t2 = n * n * n * x0 + 3 * t1 * (n1 * n1) * x1 + 3 * (t1 * t1) * (1 - t1) * x2 + t1 * t1 * t1 * x3;
		if(t2 < 0) {
			t2 = 0;
		} else if(t2 > 1) {
			t2 = 1;
		}
		let n2 = 1 - t2;
		let n3 = 1 - t2;
		return n2 * n2 * n2 * y0 + 3 * t2 * (n3 * n3) * y1 + 3 * (t2 * t2) * (1 - t2) * y2 + t2 * t2 * t2 * y3;
	}
	static fastPow2(n) {
		return n * n;
	}
	static fastPow3(n) {
		return n * n * n;
	}
	static rotateX(x,y,rotCenterX,rotCenterY,ang) {
		return rotCenterX + (x - rotCenterX) * Math.cos(ang) + (y - rotCenterY) * Math.sin(ang);
	}
	static rotateY(x,y,rotCenterX,rotCenterY,ang) {
		return rotCenterY - (x - rotCenterX) * Math.sin(ang) + (y - rotCenterY) * Math.cos(ang);
	}
	static cosZeroToOne(ang) {
		return (1 + Math.cos(ang)) / 2;
	}
	static sinZeroToOne(ang) {
		return (1 + Math.sin(ang)) / 2;
	}
	static sinOscillate(from,to,ang) {
		return from + (to - from) * ((1 + Math.sin(ang)) / 2);
	}
	static subRatio(r,min,max) {
		let x = (r - min) / (max - min);
		if(x < 0) {
			return 0;
		} else if(x > 1) {
			return 1;
		} else {
			return x;
		}
	}
}
dn_M.__name__ = "dn.M";
class dn_struct_FixedArray {
	constructor(name,maxSize) {
		this.preserveOrder = false;
		this.autoExpandSize = 0;
		this.name = name;
		this.values = new Array(maxSize);
		this.nalloc = 0;
	}
	get_allocated() {
		return this.nalloc;
	}
	get_maxSize() {
		return this.values.length;
	}
	get_length() {
		return this.nalloc;
	}
	iterator() {
		return new dn_struct__$FixedArray_FixedArrayIterator(this);
	}
	toString() {
		let a = [];
		let _g_arr = this;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let v = _g_arr.values[_g_i++];
			a.push(v);
		}
		let _gthis = this;
		let result = new Array(a.length);
		let _g = 0;
		let _g1 = a.length;
		while(_g < _g1) {
			let i = _g++;
			result[i] = _gthis.toStringValue(a[i]);
		}
		return result.toString() + ("<" + this.nalloc + "/" + this.values.length + ">");
	}
	shortString() {
		let a = [];
		let _g_arr = this;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let v = _g_arr.values[_g_i++];
			a.push(v);
		}
		let _gthis = this;
		let result = new Array(a.length);
		let _g = 0;
		let _g1 = a.length;
		while(_g < _g1) {
			let i = _g++;
			result[i] = _gthis.toStringValue(a[i]);
		}
		return result.join(",");
	}
	toStringValue(v) {
		return Std.string(v);
	}
	loadArray(arr) {
		this.values = new Array(arr.length);
		this.nalloc = 0;
		let _g = 0;
		while(_g < arr.length) {
			let e = arr[_g];
			++_g;
			this.push(e);
		}
	}
	mapToArray(mapValue) {
		let out = [];
		let _g_arr = this;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let v = _g_arr.values[_g_i++];
			out.push(mapValue(v));
		}
		return out;
	}
	enableAutoExpand(sizeIncrease) {
		this.autoExpandSize = sizeIncrease;
	}
	isAutoExpandable() {
		return this.autoExpandSize > 0;
	}
	exists(idx) {
		if(idx >= 0) {
			return idx < this.nalloc;
		} else {
			return false;
		}
	}
	get(idx) {
		if(idx >= 0 && idx < this.nalloc) {
			return this.values[idx];
		} else {
			return null;
		}
	}
	filter(filterFunc) {
		let out = [];
		let _g_arr = this;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let v = _g_arr.values[_g_i++];
			if(filterFunc(v)) {
				out.push(v);
			}
		}
		return out;
	}
	findFirst(filterFunc) {
		let _g_arr = this;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let v = _g_arr.values[_g_i++];
			if(filterFunc(v)) {
				return v;
			}
		}
		return null;
	}
	pickRandom(rndFunc,removeAfterPick) {
		if(removeAfterPick == null) {
			removeAfterPick = false;
		}
		if(this.nalloc == 0) {
			return null;
		} else {
			let idx = (rndFunc == null ? Std.random : rndFunc)(this.nalloc);
			if(removeAfterPick) {
				let e = idx >= 0 && idx < this.nalloc ? this.values[idx] : null;
				this.removeIndex(idx);
				return e;
			} else if(idx >= 0 && idx < this.nalloc) {
				return this.values[idx];
			} else {
				return null;
			}
		}
	}
	set(idx,v) {
		if(idx < 0 || idx >= this.nalloc) {
			throw haxe_Exception.thrown("Out-of-bounds FixedArray set (idx=" + idx + ", allocated=" + this.nalloc + ")");
		}
		return this.values[idx] = v;
	}
	first() {
		return this.values[0];
	}
	oneRandomly() {
		if(this.nalloc > 0) {
			return this.values[Std.random(this.nalloc)];
		} else {
			return null;
		}
	}
	last() {
		return this.values[this.nalloc - 1];
	}
	pop() {
		if(this.nalloc > 0) {
			return this.values[this.nalloc-- - 1];
		} else {
			return null;
		}
	}
	shift() {
		if(this.nalloc == 0) {
			return null;
		} else {
			let v = this.values[0];
			this.removeIndex(0);
			return v;
		}
	}
	empty() {
		this.nalloc = 0;
	}
	contains(search) {
		let _g_arr = this;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let v = _g_arr.values[_g_i++];
			if(v == search) {
				return true;
			}
		}
		return false;
	}
	push(e) {
		if(this.nalloc >= this.values.length) {
			if(this.autoExpandSize <= 0) {
				throw haxe_Exception.thrown("FixedArray limit reached (" + this.values.length + ")");
			} else {
				let newValues = new Array(this.values.length + this.autoExpandSize);
				let _g = 0;
				let _g1 = this.values.length;
				while(_g < _g1) {
					let i = _g++;
					newValues[i] = this.values[i];
				}
				this.values = newValues;
			}
		}
		this.values[this.nalloc] = e;
		this.nalloc++;
	}
	remove(e) {
		let found = false;
		let _g = 0;
		let _g1 = this.nalloc;
		while(_g < _g1) {
			let i = _g++;
			if(this.values[i] == e) {
				this.removeIndex(i);
				found = true;
				break;
			}
		}
		return found;
	}
	removeIndex(i) {
		if(i < this.nalloc) {
			if(this.nalloc == 1) {
				this.nalloc = 0;
			} else if(!this.preserveOrder) {
				this.values[i] = this.values[this.nalloc - 1];
				this.nalloc--;
			} else {
				let _g = i + 1;
				let _g1 = this.nalloc;
				while(_g < _g1) {
					let j = _g++;
					this.values[j - 1] = this.values[j];
				}
				this.nalloc--;
			}
		}
	}
	dispose() {
		this.values = null;
		this.nalloc = 0;
	}
	bubbleSort(getSortWeight) {
		let tmp;
		let _g = 0;
		let _g1 = this.nalloc - 1;
		while(_g < _g1) {
			let i = _g++;
			let _g1 = i + 1;
			let _g2 = this.nalloc;
			while(_g1 < _g2) {
				let j = _g1++;
				if(getSortWeight(i >= 0 && i < this.nalloc ? this.values[i] : null) > getSortWeight(j >= 0 && j < this.nalloc ? this.values[j] : null)) {
					tmp = i >= 0 && i < this.nalloc ? this.values[i] : null;
					let v = j >= 0 && j < this.nalloc ? this.values[j] : null;
					if(i < 0 || i >= this.nalloc) {
						throw haxe_Exception.thrown("Out-of-bounds FixedArray set (idx=" + i + ", allocated=" + this.nalloc + ")");
					}
					this.values[i] = v;
					if(j < 0 || j >= this.nalloc) {
						throw haxe_Exception.thrown("Out-of-bounds FixedArray set (idx=" + j + ", allocated=" + this.nalloc + ")");
					}
					this.values[j] = tmp;
				}
			}
		}
	}
	shuffle(randFunc) {
		if(randFunc == null) {
			randFunc = Std.random;
		}
		let m = this.nalloc;
		let i = 0;
		let tmp;
		while(m > 0) {
			i = randFunc(m--);
			tmp = this.values[m];
			this.values[m] = this.values[i];
			this.values[i] = tmp;
		}
	}
	static fromArray(arr) {
		let fa = new dn_struct_FixedArray(null,0);
		fa.loadArray(arr);
		return fa;
	}
}
dn_struct_FixedArray.__name__ = "dn.struct.FixedArray";
Object.assign(dn_struct_FixedArray.prototype, {
	__class__: dn_struct_FixedArray
});
class dn_Process {
	constructor(parent) {
		this.tmpProfilerTimes = new haxe_ds_StringMap();
		this._initOnceDone = false;
		this._fixedUpdateAccu = 0.;
		this.ignoreTimeMultipliers = false;
		this.baseTimeMul = 1.0;
		this.init();
		if(parent == null) {
			dn_Process.ROOTS.push(this);
		} else {
			parent.addChild(this);
		}
		dn_Process.resizeAll(false);
	}
	get_paused() {
		return this._manuallyPaused;
	}
	get_tmod() {
		return this.utmod * this.getComputedTimeMultiplier();
	}
	get_speedMod() {
		return this.baseTimeMul;
	}
	set_speedMod(v) {
		return this.baseTimeMul = v;
	}
	get_dt() {
		return this.utmod * this.getComputedTimeMultiplier();
	}
	init() {
		this.uniqId = dn_Process.UNIQ_ID++;
		this.children = new dn_struct_FixedArray(null,dn_Process.MAX_PROCESSES);
		this._manuallyPaused = false;
		this.destroyed = false;
		this.ftime = 0;
		this.uftime = 0;
		this.utmod = 1;
		this.baseTimeMul = 1;
		this.cd = new dn_Cooldown(this.getDefaultFrameRate());
		this.delayer = new dn_Delayer(this.getDefaultFrameRate());
		this.tw = new dn_Tweenie(this.getDefaultFrameRate());
		this.ucd = new dn_Cooldown(this.getDefaultFrameRate());
		this.udelayer = new dn_Delayer(this.getDefaultFrameRate());
	}
	initOnceBeforeUpdate() {
	}
	rprintChildren() {
		let _crawl = null;
		_crawl = function(p,depth) {
			if(depth == null) {
				depth = 0;
			}
			let out = [];
			let indent = depth > 0 ? " |--- " : "";
			let out1 = "";
			let _g = 0;
			let _g1 = 6 * (depth - 1);
			while(_g < _g1) {
				let i = _g++;
				out1 += " ";
			}
			indent = out1 + indent;
			out.push(indent + p.toString());
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let cp = _g_arr.values[_g_i++];
				out = out.concat(_crawl(cp,depth + 1));
			}
			return out;
		};
		return _crawl(this).join("\n");
	}
	markProfilingStart(id) {
		if(dn_Process.PROFILING) {
			id = this.getDisplayName() + "." + id;
			let this1 = this.tmpProfilerTimes;
			let hrtime = process.hrtime();
			this1.h[id] = hrtime[0] + hrtime[1] / 1e9;
		}
	}
	markProfilingEnd(id) {
		if(dn_Process.PROFILING) {
			id = this.getDisplayName() + "." + id;
			if(Object.prototype.hasOwnProperty.call(this.tmpProfilerTimes.h,id)) {
				let hrtime = process.hrtime();
				let t = hrtime[0] + hrtime[1] / 1e9 - this.tmpProfilerTimes.h[id];
				let _this = this.tmpProfilerTimes;
				if(Object.prototype.hasOwnProperty.call(_this.h,id)) {
					delete(_this.h[id]);
				}
				if(!Object.prototype.hasOwnProperty.call(dn_Process.PROFILER_TIMES.h,id)) {
					dn_Process.PROFILER_TIMES.h[id] = t;
				} else {
					dn_Process.PROFILER_TIMES.h[id] += t;
				}
			}
		}
	}
	countChildren() {
		return this.children.nalloc;
	}
	preUpdate() {
	}
	update() {
	}
	fixedUpdate() {
	}
	getFixedUpdateAccuRatio() {
		return this._fixedUpdateAccu / (this.getDefaultFrameRate() / dn_Process.FIXED_UPDATE_FPS);
	}
	postUpdate() {
	}
	finalUpdate() {
	}
	onResize() {
		this.onResizeCb();
	}
	onDispose() {
	}
	onUpdateCb() {
	}
	onFixedUpdateCb() {
	}
	onDisposeCb() {
	}
	onResizeCb() {
	}
	toString() {
		return "#" + this.uniqId + " " + this.getDisplayName() + (this.isPaused() ? " [PAUSED]" : "");
	}
	getDisplayName() {
		if(this.name != null) {
			return this.name;
		} else {
			if(this._cachedClassName == null) {
				let c = js_Boot.getClass(this);
				this._cachedClassName = c.__name__;
			}
			return this._cachedClassName;
		}
	}
	get_itime() {
		return this.ftime | 0;
	}
	get_stime() {
		return this.ftime / this.getDefaultFrameRate();
	}
	addAlpha(c,a) {
		if(a == null) {
			a = 1.0;
		}
		return (a * 255 | 0) << 24 | c;
	}
	rnd(min,max,sign) {
		let sign1 = sign;
		if(sign1 == null) {
			sign1 = false;
		}
		if(sign1) {
			return (min + Math.random() * (max - min)) * (Std.random(2) * 2 - 1);
		} else {
			return min + Math.random() * (max - min);
		}
	}
	irnd(min,max,sign) {
		let sign1 = sign;
		if(sign1 == null) {
			sign1 = false;
		}
		if(sign1) {
			return (min + Std.random(max - min + 1)) * (Std.random(2) * 2 - 1);
		} else {
			return min + Std.random(max - min + 1);
		}
	}
	rndSign() {
		return Std.random(2) * 2 - 1;
	}
	rndSecondsF(min,max,sign) {
		let sign1 = sign;
		if(sign1 == null) {
			sign1 = false;
		}
		return this.secToFrames(sign1 ? (min + Math.random() * (max - min)) * (Std.random(2) * 2 - 1) : min + Math.random() * (max - min));
	}
	irndSecondsF(min,max,sign) {
		let sign1 = sign;
		if(sign1 == null) {
			sign1 = false;
		}
		return this.secToFrames(sign1 ? (min + Math.random() * (max - min)) * (Std.random(2) * 2 - 1) : min + Math.random() * (max - min));
	}
	pretty(v,precision) {
		if(precision == null) {
			precision = 2;
		}
		let precision1 = precision;
		if(precision1 == null) {
			precision1 = 2;
		}
		if(precision1 <= 0) {
			return (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
		} else {
			let d = Math.pow(10,precision1);
			let x = v * d;
			return ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
		}
	}
	secToFrames(v) {
		return v * this.getDefaultFrameRate();
	}
	framesToSec(v) {
		return v / this.getDefaultFrameRate();
	}
	msToFrames(v) {
		return v / 1000 * this.getDefaultFrameRate();
	}
	framesToMs(v) {
		return 1000 * v / this.getDefaultFrameRate();
	}
	getDefaultFrameRate() {
		return 30;
	}
	setTimeMultiplier(v) {
		this.baseTimeMul = v;
	}
	getComputedTimeMultiplier() {
		if(this.ignoreTimeMultipliers) {
			return 1.0;
		} else {
			let y = this.baseTimeMul * (this.parent == null ? 1 : this.parent.getComputedTimeMultiplier());
			if(0 > y) {
				return 0;
			} else {
				return y;
			}
		}
	}
	w() {
		if(dn_Process.CUSTOM_STAGE_WIDTH > 0) {
			return dn_Process.CUSTOM_STAGE_WIDTH;
		} else {
			return 1;
		}
	}
	get_stageWid() {
		if(dn_Process.CUSTOM_STAGE_WIDTH > 0) {
			return dn_Process.CUSTOM_STAGE_WIDTH;
		}
		return 1;
	}
	h() {
		if(dn_Process.CUSTOM_STAGE_HEIGHT > 0) {
			return dn_Process.CUSTOM_STAGE_HEIGHT;
		} else {
			return 1;
		}
	}
	get_stageHei() {
		if(dn_Process.CUSTOM_STAGE_HEIGHT > 0) {
			return dn_Process.CUSTOM_STAGE_HEIGHT;
		}
		return 1;
	}
	anyParentPaused() {
		if(this.parent != null) {
			return this.parent.isPaused();
		} else {
			return false;
		}
	}
	isPaused() {
		if(this._manuallyPaused) {
			return true;
		} else if(this.parent != null) {
			return this.parent.isPaused();
		} else {
			return false;
		}
	}
	pause() {
		this._manuallyPaused = true;
	}
	resume() {
		this._manuallyPaused = false;
	}
	togglePause() {
		if(this._manuallyPaused) {
			this.resume();
		} else {
			this.pause();
		}
		return this._manuallyPaused;
	}
	destroy() {
		this.destroyed = true;
	}
	addChild(p) {
		if(p.parent == null) {
			dn_Process.ROOTS.remove(p);
		} else {
			p.parent.children.remove(p);
		}
		p.parent = this;
		this.children.push(p);
	}
	isRootProcess() {
		return this.parent == null;
	}
	moveChildToRootProcesses(p) {
		if(p.parent != this) {
			throw haxe_Exception.thrown("Not a child of this process");
		}
		p.parent = null;
		this.children.remove(p);
		dn_Process.ROOTS.push(p);
	}
	removeAndDestroyChild(p) {
		if(p.parent != this) {
			throw haxe_Exception.thrown("Not a child of this process");
		}
		p.parent = null;
		this.children.remove(p);
		dn_Process.ROOTS.push(p);
		p.destroyed = true;
	}
	createChildProcess(onUpdate,onDispose,runUpdateImmediatly) {
		if(runUpdateImmediatly == null) {
			runUpdateImmediatly = false;
		}
		let p = new dn_Process(this);
		if(onUpdate != null) {
			p.onUpdateCb = function() {
				onUpdate(p);
			};
		}
		if(onDispose != null) {
			p.onDisposeCb = function() {
				onDispose(p);
			};
		}
		if(runUpdateImmediatly) {
			if(!p.isPaused() && !p.destroyed) {
				p.utmod = 1;
				p.ftime += p.utmod * p.getComputedTimeMultiplier();
				p.uftime += p.utmod;
				p.delayer.update(p.utmod * p.getComputedTimeMultiplier());
				if(!p.isPaused() && !p.destroyed) {
					p.udelayer.update(p.utmod);
				}
				if(!p.isPaused() && !p.destroyed) {
					p.cd.update(p.utmod * p.getComputedTimeMultiplier());
				}
				if(!p.isPaused() && !p.destroyed) {
					p.ucd.update(p.utmod);
				}
				if(!p.isPaused() && !p.destroyed) {
					p.tw.update(p.utmod * p.getComputedTimeMultiplier());
				}
				if(!p.isPaused() && !p.destroyed) {
					if(!p._initOnceDone) {
						p.initOnceBeforeUpdate();
						p._initOnceDone = true;
					}
					p.preUpdate();
				}
				if(!p.isPaused() && !p.destroyed) {
					let _g_arr = p.children;
					let _g_i = 0;
					while(_g_i < _g_arr.nalloc) {
						let c = _g_arr.values[_g_i++];
						let utmod = p.utmod;
						if(!c.isPaused() && !c.destroyed) {
							c.utmod = utmod;
							c.ftime += c.utmod * c.getComputedTimeMultiplier();
							c.uftime += c.utmod;
							c.delayer.update(c.utmod * c.getComputedTimeMultiplier());
							if(!c.isPaused() && !c.destroyed) {
								c.udelayer.update(c.utmod);
							}
							if(!c.isPaused() && !c.destroyed) {
								c.cd.update(c.utmod * c.getComputedTimeMultiplier());
							}
							if(!c.isPaused() && !c.destroyed) {
								c.ucd.update(c.utmod);
							}
							if(!c.isPaused() && !c.destroyed) {
								c.tw.update(c.utmod * c.getComputedTimeMultiplier());
							}
							if(!c.isPaused() && !c.destroyed) {
								if(!c._initOnceDone) {
									c.initOnceBeforeUpdate();
									c._initOnceDone = true;
								}
								c.preUpdate();
							}
							if(!c.isPaused() && !c.destroyed) {
								let _g_arr = c.children;
								let _g_i = 0;
								while(_g_i < _g_arr.nalloc) {
									let c1 = _g_arr.values[_g_i++];
									dn_Process._doPreUpdate(c1,c.utmod);
								}
							}
						}
					}
				}
			}
			dn_Process._doMainUpdate(p);
		}
		return p;
	}
	killAllChildrenProcesses() {
		let _g_arr = this.children;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let p = _g_arr.values[_g_i++];
			p.destroyed = true;
		}
	}
	emitResizeAtEndOfFrame() {
		dn_Process.resizeAll(false);
	}
	emitResizeNow() {
		dn_Process.resizeAll(true);
	}
	static rprintAll() {
		let all = [];
		let _g_arr = dn_Process.ROOTS;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let p = _g_arr.values[_g_i++];
			all.push(p.rprintChildren());
		}
		return all.join("\n");
	}
	static destroyAllExcept(excepts) {
		let _g_arr = dn_Process.ROOTS;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let p = _g_arr.values[_g_i++];
			if(p.destroyed) {
				continue;
			}
			let keep = false;
			let _g = 0;
			while(_g < excepts.length) {
				let e = excepts[_g];
				++_g;
				if(e == p) {
					keep = true;
					break;
				}
			}
			if(!keep) {
				p.destroyed = true;
			}
		}
	}
	static clearProfilingTimes() {
		dn_Process.PROFILER_TIMES = new haxe_ds_StringMap();
	}
	static getSortedProfilerTimes() {
		let all = [];
		let h = dn_Process.PROFILER_TIMES.h;
		let i_h = h;
		let i_keys = Object.keys(h);
		let i_length = i_keys.length;
		let i_current = 0;
		while(i_current < i_length) {
			let key = i_keys[i_current++];
			let i = { key : key, value : i_h[key]};
			all.push(i);
		}
		all.sort(function(a,b) {
			return -Reflect.compare(a.value,b.value);
		});
		return all;
	}
	static canRun(p) {
		if(!p.isPaused()) {
			return !p.destroyed;
		} else {
			return false;
		}
	}
	static _doPreUpdate(p,utmod) {
		if(!(!p.isPaused() && !p.destroyed)) {
			return;
		}
		p.utmod = utmod;
		p.ftime += p.utmod * p.getComputedTimeMultiplier();
		p.uftime += p.utmod;
		p.delayer.update(p.utmod * p.getComputedTimeMultiplier());
		if(!p.isPaused() && !p.destroyed) {
			p.udelayer.update(p.utmod);
		}
		if(!p.isPaused() && !p.destroyed) {
			p.cd.update(p.utmod * p.getComputedTimeMultiplier());
		}
		if(!p.isPaused() && !p.destroyed) {
			p.ucd.update(p.utmod);
		}
		if(!p.isPaused() && !p.destroyed) {
			p.tw.update(p.utmod * p.getComputedTimeMultiplier());
		}
		if(!p.isPaused() && !p.destroyed) {
			if(!p._initOnceDone) {
				p.initOnceBeforeUpdate();
				p._initOnceDone = true;
			}
			p.preUpdate();
		}
		if(!p.isPaused() && !p.destroyed) {
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let c = _g_arr.values[_g_i++];
				dn_Process._doPreUpdate(c,p.utmod);
			}
		}
	}
	static _doMainUpdate(p) {
		if(!(!p.isPaused() && !p.destroyed)) {
			return;
		}
		let id = "update";
		if(dn_Process.PROFILING) {
			id = p.getDisplayName() + "." + id;
			let this1 = p.tmpProfilerTimes;
			let hrtime = process.hrtime();
			this1.h[id] = hrtime[0] + hrtime[1] / 1e9;
		}
		p.update();
		if(p.onUpdateCb != null) {
			p.onUpdateCb();
		}
		let id1 = "update";
		if(dn_Process.PROFILING) {
			id1 = p.getDisplayName() + "." + id1;
			if(Object.prototype.hasOwnProperty.call(p.tmpProfilerTimes.h,id1)) {
				let hrtime = process.hrtime();
				let t = hrtime[0] + hrtime[1] / 1e9 - p.tmpProfilerTimes.h[id1];
				let _this = p.tmpProfilerTimes;
				if(Object.prototype.hasOwnProperty.call(_this.h,id1)) {
					delete(_this.h[id1]);
				}
				if(!Object.prototype.hasOwnProperty.call(dn_Process.PROFILER_TIMES.h,id1)) {
					dn_Process.PROFILER_TIMES.h[id1] = t;
				} else {
					dn_Process.PROFILER_TIMES.h[id1] += t;
				}
			}
		}
		if(!p.isPaused() && !p.destroyed) {
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let p = _g_arr.values[_g_i++];
				dn_Process._doMainUpdate(p);
			}
		}
	}
	static _doFixedUpdate(p) {
		if(!(!p.isPaused() && !p.destroyed)) {
			return;
		}
		let id = "fixed";
		if(dn_Process.PROFILING) {
			id = p.getDisplayName() + "." + id;
			let this1 = p.tmpProfilerTimes;
			let hrtime = process.hrtime();
			this1.h[id] = hrtime[0] + hrtime[1] / 1e9;
		}
		p._fixedUpdateAccu += p.utmod * p.getComputedTimeMultiplier();
		while(p._fixedUpdateAccu >= p.getDefaultFrameRate() / dn_Process.FIXED_UPDATE_FPS) {
			p._fixedUpdateAccu -= p.getDefaultFrameRate() / dn_Process.FIXED_UPDATE_FPS;
			if(!p.isPaused() && !p.destroyed) {
				p.fixedUpdate();
				if(p.onFixedUpdateCb != null) {
					p.onFixedUpdateCb();
				}
			}
		}
		let id1 = "fixed";
		if(dn_Process.PROFILING) {
			id1 = p.getDisplayName() + "." + id1;
			if(Object.prototype.hasOwnProperty.call(p.tmpProfilerTimes.h,id1)) {
				let hrtime = process.hrtime();
				let t = hrtime[0] + hrtime[1] / 1e9 - p.tmpProfilerTimes.h[id1];
				let _this = p.tmpProfilerTimes;
				if(Object.prototype.hasOwnProperty.call(_this.h,id1)) {
					delete(_this.h[id1]);
				}
				if(!Object.prototype.hasOwnProperty.call(dn_Process.PROFILER_TIMES.h,id1)) {
					dn_Process.PROFILER_TIMES.h[id1] = t;
				} else {
					dn_Process.PROFILER_TIMES.h[id1] += t;
				}
			}
		}
		if(!p.isPaused() && !p.destroyed) {
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let p = _g_arr.values[_g_i++];
				dn_Process._doFixedUpdate(p);
			}
		}
	}
	static _doPostUpdate(p) {
		if(!(!p.isPaused() && !p.destroyed)) {
			return;
		}
		let id = "post";
		if(dn_Process.PROFILING) {
			id = p.getDisplayName() + "." + id;
			let this1 = p.tmpProfilerTimes;
			let hrtime = process.hrtime();
			this1.h[id] = hrtime[0] + hrtime[1] / 1e9;
		}
		p.postUpdate();
		let id1 = "post";
		if(dn_Process.PROFILING) {
			id1 = p.getDisplayName() + "." + id1;
			if(Object.prototype.hasOwnProperty.call(p.tmpProfilerTimes.h,id1)) {
				let hrtime = process.hrtime();
				let t = hrtime[0] + hrtime[1] / 1e9 - p.tmpProfilerTimes.h[id1];
				let _this = p.tmpProfilerTimes;
				if(Object.prototype.hasOwnProperty.call(_this.h,id1)) {
					delete(_this.h[id1]);
				}
				if(!Object.prototype.hasOwnProperty.call(dn_Process.PROFILER_TIMES.h,id1)) {
					dn_Process.PROFILER_TIMES.h[id1] = t;
				} else {
					dn_Process.PROFILER_TIMES.h[id1] += t;
				}
			}
		}
		if(!p.destroyed) {
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let c = _g_arr.values[_g_i++];
				dn_Process._doPostUpdate(c);
			}
		}
	}
	static _doFinalUpdate(p) {
		if(!(!p.isPaused() && !p.destroyed)) {
			return;
		}
		p.finalUpdate();
		if(!p.destroyed) {
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let c = _g_arr.values[_g_i++];
				dn_Process._doFinalUpdate(c);
			}
		}
	}
	static _garbageCollector(plist) {
		let i = 0;
		let p;
		while(i < plist.nalloc) {
			p = i >= 0 && i < plist.nalloc ? plist.values[i] : null;
			if(p.destroyed) {
				dn_Process._disposeProcess(p);
			} else {
				dn_Process._garbageCollector(p.children);
				++i;
			}
		}
	}
	static _disposeProcess(p) {
		let _g_arr = p.children;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let p = _g_arr.values[_g_i++];
			p.destroyed = true;
		}
		dn_Process._garbageCollector(p.children);
		if(p.parent != null) {
			p.parent.children.remove(p);
		} else {
			dn_Process.ROOTS.remove(p);
		}
		p.onDispose();
		if(p.onDisposeCb != null) {
			p.onDisposeCb();
		}
		p.delayer.dispose();
		p.delayer = null;
		p.udelayer.dispose();
		p.udelayer = null;
		p.cd.dispose();
		p.cd = null;
		p.ucd.dispose();
		p.ucd = null;
		p.tw.destroy();
		p.tw = null;
		p.parent = null;
		p.children = null;
	}
	static _resizeProcess(p) {
		if(!p.destroyed) {
			p.onResize();
			let _g_arr = p.children;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let p = _g_arr.values[_g_i++];
				dn_Process._resizeProcess(p);
			}
		}
	}
	static updateAll(utmod) {
		if(dn_Process.BEGINNING_OF_FRAME_CALLBACKS.nalloc > 0) {
			let _g_arr = dn_Process.BEGINNING_OF_FRAME_CALLBACKS;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let cb = _g_arr.values[_g_i++];
				cb();
			}
			dn_Process.BEGINNING_OF_FRAME_CALLBACKS.empty();
		}
		let _g_arr = dn_Process.ROOTS;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let p = _g_arr.values[_g_i++];
			if(!p.isPaused() && !p.destroyed) {
				p.utmod = utmod;
				p.ftime += p.utmod * p.getComputedTimeMultiplier();
				p.uftime += p.utmod;
				p.delayer.update(p.utmod * p.getComputedTimeMultiplier());
				if(!p.isPaused() && !p.destroyed) {
					p.udelayer.update(p.utmod);
				}
				if(!p.isPaused() && !p.destroyed) {
					p.cd.update(p.utmod * p.getComputedTimeMultiplier());
				}
				if(!p.isPaused() && !p.destroyed) {
					p.ucd.update(p.utmod);
				}
				if(!p.isPaused() && !p.destroyed) {
					p.tw.update(p.utmod * p.getComputedTimeMultiplier());
				}
				if(!p.isPaused() && !p.destroyed) {
					if(!p._initOnceDone) {
						p.initOnceBeforeUpdate();
						p._initOnceDone = true;
					}
					p.preUpdate();
				}
				if(!p.isPaused() && !p.destroyed) {
					let _g_arr = p.children;
					let _g_i = 0;
					while(_g_i < _g_arr.nalloc) {
						let c = _g_arr.values[_g_i++];
						let utmod = p.utmod;
						if(!c.isPaused() && !c.destroyed) {
							c.utmod = utmod;
							c.ftime += c.utmod * c.getComputedTimeMultiplier();
							c.uftime += c.utmod;
							c.delayer.update(c.utmod * c.getComputedTimeMultiplier());
							if(!c.isPaused() && !c.destroyed) {
								c.udelayer.update(c.utmod);
							}
							if(!c.isPaused() && !c.destroyed) {
								c.cd.update(c.utmod * c.getComputedTimeMultiplier());
							}
							if(!c.isPaused() && !c.destroyed) {
								c.ucd.update(c.utmod);
							}
							if(!c.isPaused() && !c.destroyed) {
								c.tw.update(c.utmod * c.getComputedTimeMultiplier());
							}
							if(!c.isPaused() && !c.destroyed) {
								if(!c._initOnceDone) {
									c.initOnceBeforeUpdate();
									c._initOnceDone = true;
								}
								c.preUpdate();
							}
							if(!c.isPaused() && !c.destroyed) {
								let _g_arr = c.children;
								let _g_i = 0;
								while(_g_i < _g_arr.nalloc) {
									let c1 = _g_arr.values[_g_i++];
									dn_Process._doPreUpdate(c1,c.utmod);
								}
							}
						}
					}
				}
			}
		}
		let _g_arr1 = dn_Process.ROOTS;
		let _g_i1 = 0;
		while(_g_i1 < _g_arr1.nalloc) {
			let p = _g_arr1.values[_g_i1++];
			dn_Process._doMainUpdate(p);
		}
		let _g_arr2 = dn_Process.ROOTS;
		let _g_i2 = 0;
		while(_g_i2 < _g_arr2.nalloc) {
			let p = _g_arr2.values[_g_i2++];
			dn_Process._doFixedUpdate(p);
		}
		let _g_arr3 = dn_Process.ROOTS;
		let _g_i3 = 0;
		while(_g_i3 < _g_arr3.nalloc) {
			let p = _g_arr3.values[_g_i3++];
			if(!p.isPaused() && !p.destroyed) {
				let id = "post";
				if(dn_Process.PROFILING) {
					id = p.getDisplayName() + "." + id;
					let this1 = p.tmpProfilerTimes;
					let hrtime = process.hrtime();
					this1.h[id] = hrtime[0] + hrtime[1] / 1e9;
				}
				p.postUpdate();
				let id1 = "post";
				if(dn_Process.PROFILING) {
					id1 = p.getDisplayName() + "." + id1;
					if(Object.prototype.hasOwnProperty.call(p.tmpProfilerTimes.h,id1)) {
						let hrtime = process.hrtime();
						let t = hrtime[0] + hrtime[1] / 1e9 - p.tmpProfilerTimes.h[id1];
						let _this = p.tmpProfilerTimes;
						if(Object.prototype.hasOwnProperty.call(_this.h,id1)) {
							delete(_this.h[id1]);
						}
						if(!Object.prototype.hasOwnProperty.call(dn_Process.PROFILER_TIMES.h,id1)) {
							dn_Process.PROFILER_TIMES.h[id1] = t;
						} else {
							dn_Process.PROFILER_TIMES.h[id1] += t;
						}
					}
				}
				if(!p.destroyed) {
					let _g_arr = p.children;
					let _g_i = 0;
					while(_g_i < _g_arr.nalloc) {
						let c = _g_arr.values[_g_i++];
						if(!c.isPaused() && !c.destroyed) {
							let id = "post";
							if(dn_Process.PROFILING) {
								id = c.getDisplayName() + "." + id;
								let this1 = c.tmpProfilerTimes;
								let hrtime = process.hrtime();
								this1.h[id] = hrtime[0] + hrtime[1] / 1e9;
							}
							c.postUpdate();
							let id1 = "post";
							if(dn_Process.PROFILING) {
								id1 = c.getDisplayName() + "." + id1;
								if(Object.prototype.hasOwnProperty.call(c.tmpProfilerTimes.h,id1)) {
									let hrtime = process.hrtime();
									let t = hrtime[0] + hrtime[1] / 1e9 - c.tmpProfilerTimes.h[id1];
									let _this = c.tmpProfilerTimes;
									if(Object.prototype.hasOwnProperty.call(_this.h,id1)) {
										delete(_this.h[id1]);
									}
									if(!Object.prototype.hasOwnProperty.call(dn_Process.PROFILER_TIMES.h,id1)) {
										dn_Process.PROFILER_TIMES.h[id1] = t;
									} else {
										dn_Process.PROFILER_TIMES.h[id1] += t;
									}
								}
							}
							if(!c.destroyed) {
								let _g_arr = c.children;
								let _g_i = 0;
								while(_g_i < _g_arr.nalloc) {
									let c = _g_arr.values[_g_i++];
									dn_Process._doPostUpdate(c);
								}
							}
						}
					}
				}
			}
		}
		let _g_arr4 = dn_Process.ROOTS;
		let _g_i4 = 0;
		while(_g_i4 < _g_arr4.nalloc) {
			let p = _g_arr4.values[_g_i4++];
			if(!p.isPaused() && !p.destroyed) {
				p.finalUpdate();
				if(!p.destroyed) {
					let _g_arr = p.children;
					let _g_i = 0;
					while(_g_i < _g_arr.nalloc) {
						let c = _g_arr.values[_g_i++];
						if(!c.isPaused() && !c.destroyed) {
							c.finalUpdate();
							if(!c.destroyed) {
								let _g_arr = c.children;
								let _g_i = 0;
								while(_g_i < _g_arr.nalloc) {
									let c = _g_arr.values[_g_i++];
									dn_Process._doFinalUpdate(c);
								}
							}
						}
					}
				}
			}
		}
		if(dn_Process.RESIZE_REQUESTED) {
			dn_Process.RESIZE_REQUESTED = false;
			let _g_arr = dn_Process.ROOTS;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let p = _g_arr.values[_g_i++];
				if(!p.destroyed) {
					p.onResize();
					let _g_arr = p.children;
					let _g_i = 0;
					while(_g_i < _g_arr.nalloc) {
						let p = _g_arr.values[_g_i++];
						if(!p.destroyed) {
							p.onResize();
							let _g_arr = p.children;
							let _g_i = 0;
							while(_g_i < _g_arr.nalloc) {
								let p = _g_arr.values[_g_i++];
								dn_Process._resizeProcess(p);
							}
						}
					}
				}
			}
		}
		dn_Process._garbageCollector(dn_Process.ROOTS);
		if(dn_Process.END_OF_FRAME_CALLBACKS.nalloc > 0) {
			let _g_arr = dn_Process.END_OF_FRAME_CALLBACKS;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let cb = _g_arr.values[_g_i++];
				cb();
			}
			dn_Process.END_OF_FRAME_CALLBACKS.empty();
		}
	}
	static forceProcessGarbageCollection() {
		dn_Process._garbageCollector(dn_Process.ROOTS);
	}
	static resizeAll(immediately) {
		if(immediately == null) {
			immediately = false;
		}
		if(immediately) {
			let _g_arr = dn_Process.ROOTS;
			let _g_i = 0;
			while(_g_i < _g_arr.nalloc) {
				let p = _g_arr.values[_g_i++];
				if(!p.destroyed) {
					p.onResize();
					let _g_arr = p.children;
					let _g_i = 0;
					while(_g_i < _g_arr.nalloc) {
						let p = _g_arr.values[_g_i++];
						if(!p.destroyed) {
							p.onResize();
							let _g_arr = p.children;
							let _g_i = 0;
							while(_g_i < _g_arr.nalloc) {
								let p = _g_arr.values[_g_i++];
								dn_Process._resizeProcess(p);
							}
						}
					}
				}
			}
		} else {
			dn_Process.RESIZE_REQUESTED = true;
		}
	}
	static callAtTheBeginningOfNextFrame(cb) {
		dn_Process.BEGINNING_OF_FRAME_CALLBACKS.push(cb);
	}
	static callAtTheEndOfCurrentFrame(cb) {
		dn_Process.END_OF_FRAME_CALLBACKS.push(cb);
	}
}
dn_Process.__name__ = "dn.Process";
Object.assign(dn_Process.prototype, {
	__class__: dn_Process
});
var dn_TType = $hxEnums["dn.TType"] = { __ename__:true,__constructs__:null
	,TLinear: {_hx_name:"TLinear",_hx_index:0,__enum__:"dn.TType",toString:$estr}
	,TLoop: {_hx_name:"TLoop",_hx_index:1,__enum__:"dn.TType",toString:$estr}
	,TLoopEaseIn: {_hx_name:"TLoopEaseIn",_hx_index:2,__enum__:"dn.TType",toString:$estr}
	,TLoopEaseOut: {_hx_name:"TLoopEaseOut",_hx_index:3,__enum__:"dn.TType",toString:$estr}
	,TEase: {_hx_name:"TEase",_hx_index:4,__enum__:"dn.TType",toString:$estr}
	,TEaseIn: {_hx_name:"TEaseIn",_hx_index:5,__enum__:"dn.TType",toString:$estr}
	,TEaseOut: {_hx_name:"TEaseOut",_hx_index:6,__enum__:"dn.TType",toString:$estr}
	,TBurn: {_hx_name:"TBurn",_hx_index:7,__enum__:"dn.TType",toString:$estr}
	,TBurnIn: {_hx_name:"TBurnIn",_hx_index:8,__enum__:"dn.TType",toString:$estr}
	,TBurnOut: {_hx_name:"TBurnOut",_hx_index:9,__enum__:"dn.TType",toString:$estr}
	,TZigZag: {_hx_name:"TZigZag",_hx_index:10,__enum__:"dn.TType",toString:$estr}
	,TRand: {_hx_name:"TRand",_hx_index:11,__enum__:"dn.TType",toString:$estr}
	,TShake: {_hx_name:"TShake",_hx_index:12,__enum__:"dn.TType",toString:$estr}
	,TShakeBoth: {_hx_name:"TShakeBoth",_hx_index:13,__enum__:"dn.TType",toString:$estr}
	,TJump: {_hx_name:"TJump",_hx_index:14,__enum__:"dn.TType",toString:$estr}
	,TElasticEnd: {_hx_name:"TElasticEnd",_hx_index:15,__enum__:"dn.TType",toString:$estr}
	,TBackOut: {_hx_name:"TBackOut",_hx_index:16,__enum__:"dn.TType",toString:$estr}
};
dn_TType.__constructs__ = [dn_TType.TLinear,dn_TType.TLoop,dn_TType.TLoopEaseIn,dn_TType.TLoopEaseOut,dn_TType.TEase,dn_TType.TEaseIn,dn_TType.TEaseOut,dn_TType.TBurn,dn_TType.TBurnIn,dn_TType.TBurnOut,dn_TType.TZigZag,dn_TType.TRand,dn_TType.TShake,dn_TType.TShakeBoth,dn_TType.TJump,dn_TType.TElasticEnd,dn_TType.TBackOut];
class dn_Tween {
	constructor(tw) {
		this.tw = tw;
		this.paused = false;
		this.done = false;
		this.n = this.ln = 0;
		this.delay = 0;
		this.speed = 1;
		this.set_type(dn_TType.TEase);
		this.plays = 1;
		this.pixelSnap = false;
	}
	toString() {
		return "" + this.from + " => " + this.to + " (" + Std.string(this.type) + "): " + this.ln * 100 + "%";
	}
	set_type(t) {
		this.type = t;
		let tmp;
		switch(this.type._hx_index) {
		case 0:
			tmp = function(step) {
				return step;
			};
			break;
		case 1:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 1.33 + 3 * (step * step) * (1 - step) * 1.33 + step * step * step * 0;
			};
			break;
		case 2:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 0 + 3 * (step * step) * (1 - step) * 2.25 + step * step * step * 0;
			};
			break;
		case 3:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 2.25 + 3 * (step * step) * (1 - step) * 0 + step * step * step * 0;
			};
			break;
		case 4:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 0 + 3 * (step * step) * (1 - step) + step * step * step;
			};
			break;
		case 5:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 0 + 3 * (step * step) * (1 - step) * 0.5 + step * step * step;
			};
			break;
		case 6:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 0.5 + 3 * (step * step) * (1 - step) + step * step * step;
			};
			break;
		case 7:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) + 3 * (step * step) * (1 - step) * 0 + step * step * step;
			};
			break;
		case 8:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) + 3 * (step * step) * (1 - step) + step * step * step;
			};
			break;
		case 9:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 0 + 3 * (step * step) * (1 - step) * 0 + step * step * step;
			};
			break;
		case 10:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 2.5 + 3 * (step * step) * (1 - step) * -1.5 + step * step * step;
			};
			break;
		case 11:
			tmp = function(step) {
				return step;
			};
			break;
		case 12:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0.5 + 3 * step * (n1 * n1) * 1.22 + 3 * (step * step) * (1 - step) * 1.25 + step * step * step * 0;
			};
			break;
		case 13:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0.5 + 3 * step * (n1 * n1) * 1.22 + 3 * (step * step) * (1 - step) * 1.25 + step * step * step * 0;
			};
			break;
		case 14:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 2 + 3 * (step * step) * (1 - step) * 2.79 + step * step * step;
			};
			break;
		case 15:
			tmp = function(step) {
				let n = 1 - step;
				let n1 = 1 - step;
				return n * n * n * 0 + 3 * step * (n1 * n1) * 0.7 + 3 * (step * step) * (1 - step) * 1.5 + step * step * step;
			};
			break;
		case 16:
			tmp = function(step) {
				let s = 1.70158;
				step = step / 1 - 1;
				return step * step * ((s + 1) * step + s) + 1;
			};
			break;
		}
		this.interpolate = tmp;
		return this.type;
	}
	onUpdate() {
	}
	onUpdateT(t) {
	}
	onEnd() {
	}
	onStart() {
	}
	end(cb) {
		this.onEnd = cb;
		return this;
	}
	start(cb) {
		this.onStart = cb;
		return this;
	}
	update(cb) {
		this.onUpdate = cb;
		return this;
	}
	updateT(cb) {
		this.onUpdateT = cb;
		return this;
	}
	chainedEvent() {
	}
	interpolate(v) {
		return v;
	}
	pixel() {
		this.pixelSnap = true;
		return this;
	}
	bezier(t,p0,p1,p2,p3) {
		let n = 1 - t;
		let n1 = 1 - t;
		return n * n * n * p0 + 3 * t * (n1 * n1) * p1 + 3 * (t * t) * (1 - t) * p2 + t * t * t * p3;
	}
	fastPow2(n) {
		return n * n;
	}
	fastPow3(n) {
		return n * n * n;
	}
	delayFrames(d) {
		this.delay = d;
	}
	delayMs(d) {
		let x = d * this.tw.baseFps / 1000;
		this.delay = (x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0;
	}
	chainMs(to,ease,duration_ms) {
		let t = this.tw.create_(this.getter,this.setter,null,to,ease,duration_ms,true);
		t.paused = true;
		this.chainedEvent = function() {
			t.paused = false;
			t.from = t.getter();
		};
		return t;
	}
	endWithoutCallbacks() {
		this.done = true;
	}
	complete(fl_allowLoop) {
		if(fl_allowLoop == null) {
			fl_allowLoop = false;
		}
		let v = this.from + (this.to - this.from) * this.interpolate(1);
		if(this.pixelSnap) {
			v = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
		}
		this.setter(v);
		this.onUpdate();
		this.onUpdateT(1);
		this.onEnd();
		this.chainedEvent();
		if(fl_allowLoop && (this.plays == -1 || this.plays > 1)) {
			if(this.plays != -1) {
				this.plays--;
			}
			this.n = this.ln = 0;
		} else {
			this.done = true;
		}
	}
	internalUpdate(dt) {
		if(this.done) {
			return true;
		}
		if(this.paused) {
			return false;
		}
		if(this.delay > 0) {
			this.delay -= dt;
			return false;
		}
		if(this.onStart != null) {
			let cb = $bind(this,this.onStart);
			this.onStart = null;
			cb();
		}
		let dist = this.to - this.from;
		if(this.type == dn_TType.TRand) {
			this.ln += Std.random(100) < 33 ? this.speed * dt : 0;
		} else {
			this.ln += this.speed * dt;
		}
		this.n = this.interpolate(this.ln);
		if(this.ln < 1) {
			let val;
			if(this.type != dn_TType.TShake && this.type != dn_TType.TShakeBoth) {
				val = this.from + this.n * dist;
			} else if(this.type == dn_TType.TShake) {
				let val1 = this.from;
				let val2 = Math.random();
				let x = this.n * dist;
				val = val1 + val2 * (x < 0 ? -x : x) * (dist > 0 ? 1 : -1);
			} else {
				val = this.from + Math.random() * this.n * dist * (Std.random(2) * 2 - 1);
			}
			if(this.pixelSnap) {
				val = (val > 0 ? val + .5 : val < 0 ? val - .5 : 0) | 0;
			}
			this.setter(val);
			this.onUpdate();
			this.onUpdateT(this.ln);
		} else {
			this.complete(true);
		}
		return this.done;
	}
}
dn_Tween.__name__ = "dn.Tween";
Object.assign(dn_Tween.prototype, {
	__class__: dn_Tween
});
class dn_Tweenie {
	constructor(fps) {
		this.baseFps = fps;
		this.allTweens = new dn_struct_FixedArray(null,512);
	}
	count() {
		return this.allTweens.nalloc;
	}
	terminate_(getter,setter,withCallbacks) {
		if(this.allTweens == null) {
			return;
		}
		let v = getter();
		let _g_arr = this.allTweens;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let t = _g_arr.values[_g_i++];
			if(t.done) {
				continue;
			}
			let old = t.getter();
			t.setter(old + 1);
			if(getter() != v) {
				t.setter(old);
				if(withCallbacks) {
					t.ln = 1;
					t.complete(false);
				} else {
					t.endWithoutCallbacks();
				}
			} else {
				t.setter(old);
			}
		}
	}
	create_(getter,setter,from,to,tp,duration_ms,allowDuplicates) {
		if(allowDuplicates == null) {
			allowDuplicates = false;
		}
		if(duration_ms == null) {
			duration_ms = dn_Tweenie.DEFAULT_DURATION;
		}
		if(!allowDuplicates) {
			this.terminate_(getter,setter,false);
		}
		let t = new dn_Tween(this);
		t.getter = getter;
		t.setter = setter;
		t.from = from == null ? getter() : from;
		t.speed = 1 / (duration_ms * this.baseFps / 1000);
		t.to = to;
		if(tp != null) {
			t.set_type(tp);
		}
		if(from != null) {
			setter(from);
		}
		this.allTweens.push(t);
		return t;
	}
	destroy() {
		this.allTweens = null;
	}
	completeAll() {
		let _g_arr = this.allTweens;
		let _g_i = 0;
		while(_g_i < _g_arr.nalloc) {
			let t = _g_arr.values[_g_i++];
			t.ln = 1;
		}
		this.update();
	}
	update(dt) {
		if(dt == null) {
			dt = 1.0;
		}
		let i = 0;
		while(i < this.allTweens.nalloc) {
			let _this = this.allTweens;
			let t = i >= 0 && i < _this.nalloc ? _this.values[i] : null;
			if(t.internalUpdate(dt)) {
				this.allTweens.remove(t);
			} else {
				++i;
			}
		}
	}
}
dn_Tweenie.__name__ = "dn.Tweenie";
Object.assign(dn_Tweenie.prototype, {
	__class__: dn_Tweenie
});
class dn_data_GetText {
	constructor() {
		this.dict = new haxe_ds_StringMap();
	}
	getRawDict() {
		return this.dict;
	}
	getEntryCount() {
		return Lambda.count(this.dict);
	}
	readMo(p) {
		return null;
	}
	readPo(bytes) {
		let msgidReg = new EReg("^[ \t]*msgid[ \t]+\"(.*?)\"\\s*$","i");
		let msgstrReg = new EReg("^[ \t]*msgstr[ \t]+\"(.*?)\"\\s*$","i");
		let contextReg = new EReg("^[ \t]*msgctxt[ \t]+\"(.*?)\"\\s*$","i");
		let stringReg = new EReg("^[ \t]*\"(.*?)\"[ \t]*$","i");
		let commentReg_r = new RegExp("^#.*?$","i".split("u").join(""));
		this.dict = new haxe_ds_StringMap();
		let raw = bytes.toString();
		let lines = raw.split("\n");
		let lastId = null;
		let lastCtx = null;
		let pendingMsgId = false;
		let pendingMsgStr = false;
		let _g = 0;
		while(_g < lines.length) {
			let line = lines[_g];
			++_g;
			line = StringTools.replace(line,"\r","");
			if(contextReg.match(line)) {
				pendingMsgId = false;
				pendingMsgStr = false;
				lastCtx = dn_data_GetText.unescapePoString(contextReg.matched(1));
			} else if(msgidReg.match(line)) {
				pendingMsgId = true;
				pendingMsgStr = false;
				lastId = dn_data_GetText.unescapePoString(msgidReg.matched(1));
			} else if(msgstrReg.match(line)) {
				pendingMsgId = false;
				pendingMsgStr = true;
				if(lastCtx != null) {
					lastId += dn_data_GetText.CONTEXT_DISAMB_SEP + lastCtx;
					lastCtx = null;
				}
				let this1 = this.dict;
				let value = dn_data_GetText.unescapePoString(msgstrReg.matched(1));
				this1.h[lastId] = value;
			} else if(stringReg.match(line)) {
				if(pendingMsgId) {
					lastId += dn_data_GetText.unescapePoString(stringReg.matched(1));
				} else if(pendingMsgStr) {
					let this1 = this.dict;
					let value = this.dict.h[lastId] + dn_data_GetText.unescapePoString(stringReg.matched(1));
					this1.h[lastId] = value;
				}
			} else {
				pendingMsgStr = false;
				pendingMsgId = pendingMsgStr;
			}
		}
	}
	fixTranslations(fixCb) {
		let h = this.dict.h;
		let k_h = h;
		let k_keys = Object.keys(h);
		let k_length = k_keys.length;
		let k_current = 0;
		while(k_current < k_length) {
			let k = k_keys[k_current++];
			if(this.dict.h[k] != null) {
				let this1 = this.dict;
				let value = fixCb(k,this.dict.h[k]);
				this1.h[k] = value;
			}
		}
	}
	get(msgId,vars) {
		msgId = msgId.replace(dn_data_GetText.TRANSLATOR_NOTE_REG.r,"$3");
		msgId = msgId.replace(dn_data_GetText.COMMENT_REG.r,"$3");
		let str = Object.prototype.hasOwnProperty.call(this.dict.h,msgId) && this.dict.h[msgId] != "" ? this.dict.h[msgId] : msgId;
		if(vars != null) {
			let _g = 0;
			let _g1 = Reflect.fields(vars);
			while(_g < _g1.length) {
				let k = _g1[_g];
				++_g;
				str = StringTools.replace(str,"::" + k + "::",Std.string(Reflect.field(vars,k)));
			}
		}
		str = str.replace(dn_data_GetText.TRANSLATOR_NOTE_REG.r,"$3");
		str = str.replace(dn_data_GetText.COMMENT_REG.r,"$3");
		str = str.replace(dn_data_GetText.CONTEXT_DISAMB_REG.r,"$3");
		return Std.string(str);
	}
	hasTranslation(msgId) {
		return Object.prototype.hasOwnProperty.call(this.dict.h,msgId);
	}
	untranslated(str) {
		return Std.string(str);
	}
	check(reference,checkEntry) {
		let errors = [];
		try {
			let refKeys = new haxe_ds_StringMap();
			if(reference != null) {
				let h = reference.dict.h;
				let k_h = h;
				let k_keys = Object.keys(h);
				let k_length = k_keys.length;
				let k_current = 0;
				while(k_current < k_length) {
					let k = k_keys[k_current++];
					refKeys.h[k] = k;
				}
				let nref = Lambda.count(refKeys);
				let ngt = Lambda.count(this.dict);
				if(nref != ngt) {
					errors.push("Entry count in this PO fire (" + ngt + ") differs from reference PO file (" + nref + ")");
				}
			}
			let missing = [];
			let h = this.dict.h;
			let e_h = h;
			let e_keys = Object.keys(h);
			let e_length = e_keys.length;
			let e_current = 0;
			while(e_current < e_length) {
				let key = e_keys[e_current++];
				let e_key = key;
				let e_value = e_h[key];
				if(e_value == "") {
					missing.push(e_key);
					continue;
				}
				if(reference != null && !Object.prototype.hasOwnProperty.call(refKeys.h,e_key)) {
					let clean = StringTools.replace(e_key,"\n","");
					if(clean.length > 20) {
						clean = HxOverrides.substr(clean,0,20) + "[...]";
					}
					errors.push("In \"" + clean + "\"  =>  " + "Entry not present in reference PO file");
				}
				if(e_key.split("::").length != e_value.split("::").length) {
					let clean = StringTools.replace(e_key,"\n","");
					if(clean.length > 20) {
						clean = HxOverrides.substr(clean,0,20) + "[...]";
					}
					errors.push("In \"" + clean + "\"  =>  " + "Malformed variable (verify the \"::\")");
					continue;
				}
				let map_h = Object.create(null);
				let odd = true;
				let _g = 0;
				let _g1 = e_key.split("::");
				while(_g < _g1.length) {
					let v = _g1[_g];
					++_g;
					if(!odd) {
						map_h[v] = v;
					}
					odd = !odd;
				}
				odd = true;
				let _g2 = 0;
				let _g3 = e_value.split("::");
				while(_g2 < _g3.length) {
					let v = _g3[_g2];
					++_g2;
					if(!odd && !Object.prototype.hasOwnProperty.call(map_h,v)) {
						let clean = StringTools.replace(e_key,"\n","");
						if(clean.length > 20) {
							clean = HxOverrides.substr(clean,0,20) + "[...]";
						}
						errors.push("In \"" + clean + "\"  =>  " + ("Incorrect variable name ::" + v + "::"));
					}
					odd = !odd;
				}
				if(e_value.indexOf("\\v") >= 0) {
					let clean = StringTools.replace(e_key,"\n","");
					if(clean.length > 20) {
						clean = HxOverrides.substr(clean,0,20) + "[...]";
					}
					errors.push("In \"" + clean + "\"  =>  " + "Unauthorized \\v sequence");
				}
				if(checkEntry != null) {
					let err = checkEntry(e_key,e_value);
					if(err != null) {
						let clean = StringTools.replace(e_key,"\n","");
						if(clean.length > 20) {
							clean = HxOverrides.substr(clean,0,20) + "[...]";
						}
						errors.push("In \"" + clean + "\"  =>  " + err);
					}
				}
			}
			if(missing.length > 0) {
				if(missing.length > 5) {
					errors.push("" + missing.length + " missing translations");
				} else {
					let _g = 0;
					while(_g < missing.length) {
						let k = missing[_g];
						++_g;
						let clean = StringTools.replace(k,"\n","");
						if(clean.length > 20) {
							clean = HxOverrides.substr(clean,0,20) + "[...]";
						}
						errors.push("In \"" + clean + "\"  =>  " + "Missing translation");
					}
				}
			}
		} catch( _g ) {
			let e = haxe_Exception.caught(_g).unwrap();
			errors.push("EXCEPTION: " + Std.string(e));
		}
		return errors;
	}
	static escapePoString(str) {
		let r_r = new RegExp("([^\\\\]|^)\\" + "\"","gim".split("u").join(""));
		str = str.replace(r_r,"$1\\" + "\"");
		str = StringTools.replace(str,"\n","\\n");
		return str;
	}
	static unescapePoString(str) {
		str = StringTools.replace(str,"\\\"","\"");
		str = StringTools.replace(str,"\\n","\n");
		return str;
	}
	static doParseGlobal(o) {
		throw haxe_Exception.thrown("Not supported anymore");
	}
	static concatEntries(cur,to) {
		if(cur != null && to != null) {
			let _g = 0;
			while(_g < to.length) {
				let e = to[_g];
				++_g;
				cur.push(e);
			}
		}
	}
	static parseSourceCode(appendToEntries,dir) {
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("");
		}
		if(!sys_FileSystem.exists(dir)) {
			dn_Lib.println("ERROR: " + (dir != null ? dir + ": " : "") + "Folder not found");
			process.exit(-1);
			return [];
		}
		dn_Lib.println("Parsing source code (" + dir + ")...");
		let all = [];
		let files = dn_data_GetText.listFilesRec(["hx"],dir);
		let _g = 0;
		while(_g < files.length) {
			let file = files[_g];
			++_g;
			let raw = js_node_Fs.readFileSync(file,{ encoding : "utf8"});
			let n = 0;
			while(dn_data_GetText.SRC_REG.match(raw)) {
				let id = dn_data_GetText.SRC_REG.matched(1);
				let e = new dn_data_PoEntry(id);
				e.references.push(file);
				all.push(e);
				raw = dn_data_GetText.SRC_REG.matchedRight();
				++n;
			}
			if(n > 0 && dn_data_GetText.VERBOSE) {
				dn_Lib.println("  - " + file + ", " + n + " entrie(s)");
			}
		}
		if(appendToEntries != null && all != null) {
			let _g = 0;
			while(_g < all.length) {
				let e = all[_g];
				++_g;
				appendToEntries.push(e);
			}
		}
		return all;
	}
	static parseAchievementsVdf(appendToEntries,filePath) {
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("");
		}
		if(!sys_FileSystem.exists(filePath)) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + "File not found");
			process.exit(-1);
			return [];
		}
		dn_Lib.println("Parsing VDF (" + filePath + ")...");
		let p = new dn_FilePath();
		p.parse(filePath,true);
		let fp = p;
		let raw;
		try {
			raw = js_node_Fs.readFileSync(filePath,{ encoding : "utf8"});
		} catch( _g ) {
			raw = null;
		}
		if(raw == null) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + ("Couldn't read file: " + filePath));
			process.exit(-1);
			return [];
		}
		let all = [];
		let tokenReg = new EReg("\"(NEW_ACHIEVEMENT_[0-9]+_[0-9]+_(DESC|NAME))\"\\s+\"","i");
		while(tokenReg.match(raw)) {
			let left = tokenReg.matchedPos().pos + tokenReg.matchedPos().len;
			let right = left;
			let skip = false;
			while(right < raw.length) {
				if(raw.charAt(right) == "\\" && !skip) {
					skip = true;
				} else if(raw.charAt(right) == "\"" && !skip) {
					let e = new dn_data_PoEntry(raw.substring(left,right),dn_data_GetText.VDF_ACHIEVEMENT_CONTEXT + tokenReg.matched(1));
					e.references.push(fp.fileName == null && fp.extension == null ? null : (fp.fileName == null ? "" : fp.fileName) + (fp.extension == null ? "" : "." + fp.extension));
					e.addTranslatorNote("This is a Steam achievement.");
					all.push(e);
					break;
				} else if(skip) {
					skip = false;
				}
				++right;
			}
			raw = tokenReg.matchedRight();
		}
		if(appendToEntries != null && all != null) {
			let _g = 0;
			while(_g < all.length) {
				let e = all[_g];
				++_g;
				appendToEntries.push(e);
			}
		}
		return all;
	}
	static writeAchievementsVdfFromPo(poPath,vdfPath,langId) {
		dn_Lib.println("Writing VDF (" + vdfPath + ") from PO (" + poPath + ")...");
		let gt = new dn_data_GetText();
		let bytes;
		try {
			bytes = js_node_buffer__$Buffer_Helper.bytesOfBuffer(js_node_Fs.readFileSync(poPath));
		} catch( _g ) {
			dn_Lib.println("ERROR: " + (poPath != null ? poPath + ": " : "") + "Couldn't read PO");
			process.exit(-1);
			return false;
		}
		gt.readPo(bytes);
		let header = ["\"lang\"","{","\t\"Language\"\t\"" + langId + "\"","\t\"Tokens\"","\t{"];
		let footer = ["\t}","}"];
		if(sys_FileSystem.exists(vdfPath)) {
			try {
				js_node_Fs.unlinkSync(vdfPath);
			} catch( _g ) {
				dn_Lib.println("ERROR: " + (vdfPath != null ? vdfPath + ": " : "") + "Couldn't init VDF output file");
				process.exit(-1);
				return false;
			}
		}
		let fo = new sys_io_FileOutput(js_node_Fs.openSync(vdfPath,"w"));
		fo.writeString(header.join("\n") + "\n");
		let p = new dn_FilePath();
		p.parse(vdfPath,true);
		let vdfFp = p;
		let allTokens = [];
		let h = gt.dict.h;
		let e_h = h;
		let e_keys = Object.keys(h);
		let e_length = e_keys.length;
		let e_current = 0;
		while(e_current < e_length) {
			let key = e_keys[e_current++];
			let e_key = key;
			let e_value = e_h[key];
			if(e_key.indexOf(dn_data_GetText.VDF_ACHIEVEMENT_CONTEXT) >= 0) {
				let token = HxOverrides.substr(e_key,e_key.indexOf(dn_data_GetText.VDF_ACHIEVEMENT_CONTEXT) + dn_data_GetText.VDF_ACHIEVEMENT_CONTEXT.length,null);
				let loc = e_value;
				allTokens.push("\"" + token + "\" \"" + dn_data_GetText.sanitizeVdfToken(loc) + "\"");
			}
		}
		allTokens.sort(function(a,b) {
			return Reflect.compare(a,b);
		});
		let _g = 0;
		while(_g < allTokens.length) {
			let t = allTokens[_g];
			++_g;
			fo.writeString("\t\t" + t + "\n");
		}
		fo.writeString(footer.join("\n"));
		fo.close();
		return true;
	}
	static sanitizeVdfToken(str) {
		return StringTools.replace(str,"\"","\\\"");
	}
	static parseTextSections(appendToEntries,filePath,sectionReg) {
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("");
		}
		if(sectionReg == null) {
			sectionReg = new EReg("^\\s*\\[([a-z0-9_]+)\\]\\s*$","gi");
		}
		if(!sys_FileSystem.exists(filePath)) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + "File not found");
			process.exit(-1);
			return [];
		}
		dn_Lib.println("Parsing Text with sections (" + filePath + ")...");
		let p = new dn_FilePath();
		p.parse(filePath,true);
		let fp = p;
		let raw;
		try {
			raw = js_node_Fs.readFileSync(filePath,{ encoding : "utf8"});
		} catch( _g ) {
			raw = null;
		}
		if(raw == null) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + ("Couldn't read file: " + filePath));
			process.exit(-1);
			return [];
		}
		let all = [];
		let _addEntry = function(id,lines) {
			let e = new dn_data_PoEntry(dn_Lib.trimEmptyLines(lines.join("\n")));
			if(id != null) {
				e.addContextDisambiguation((fp.fileName == null && fp.extension == null ? null : (fp.fileName == null ? "" : fp.fileName) + (fp.extension == null ? "" : "." + fp.extension)) + "#" + id);
			}
			e.references.push(fp.fileName == null && fp.extension == null ? null : (fp.fileName == null ? "" : fp.fileName) + (fp.extension == null ? "" : "." + fp.extension));
			all.push(e);
		};
		let lastId = null;
		let entryLines = [];
		let _g = 0;
		let _g1 = raw.split("\n");
		while(_g < _g1.length) {
			let line = _g1[_g];
			++_g;
			line = StringTools.replace(line,"\r","");
			if(sectionReg.match(line)) {
				if(entryLines.length > 0) {
					_addEntry(lastId,entryLines);
				}
				lastId = sectionReg.matched(1);
				entryLines = [];
			} else {
				entryLines.push(line);
			}
		}
		if(entryLines.length > 0) {
			_addEntry(lastId,entryLines);
		}
		if(appendToEntries != null && all != null) {
			let _g = 0;
			while(_g < all.length) {
				let e = all[_g];
				++_g;
				appendToEntries.push(e);
			}
		}
		return all;
	}
	static parseLdtk(appendToEntries,filePath,options) {
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("");
		}
		if(!sys_FileSystem.exists(filePath)) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + "File not found");
			process.exit(-1);
			return [];
		}
		dn_Lib.println("Parsing LDtk (" + filePath + ")...");
		let all = [];
		if(!sys_FileSystem.exists(filePath)) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + ("File not found: " + filePath));
			process.exit(-1);
		} else {
			let entityLookup_h = Object.create(null);
			let _g = 0;
			let _g1 = options.entityFields;
			while(_g < _g1.length) {
				let ef = _g1[_g];
				++_g;
				if(!Object.prototype.hasOwnProperty.call(entityLookup_h,ef.entityId)) {
					entityLookup_h[ef.entityId] = new haxe_ds_StringMap();
				}
				entityLookup_h[ef.entityId].h[ef.fieldId] = true;
			}
			let levelLookup_h = Object.create(null);
			let _g2 = 0;
			let _g3 = options.levelFieldIds;
			while(_g2 < _g3.length) {
				let f = _g3[_g2];
				++_g2;
				levelLookup_h[f] = true;
			}
			let p = new dn_FilePath();
			p.parse(filePath,true);
			let fp = p;
			let projectJson;
			try {
				projectJson = JSON.parse(js_node_Fs.readFileSync(filePath,{ encoding : "utf8"}));
			} catch( _g ) {
				projectJson = null;
			}
			let _g4 = 0;
			let arr = projectJson.levels;
			let _g5;
			if(arr == null) {
				_g5 = [];
			} else {
				let _g = Type.typeof(arr);
				_g5 = _g._hx_index == 6 ? _g.c == Array ? arr : [] : [];
			}
			while(_g4 < _g5.length) {
				let l = _g5[_g4];
				++_g4;
				let levelJson = l;
				let levelPath = filePath;
				let globalComment = options.globalComment == null ? "Level design" : options.globalComment;
				let n = 0;
				if(projectJson.externalLevels) {
					levelPath = (fp.directory == null ? null : fp.directory == (fp.backslashes ? "\\" : "/") ? fp.directory : fp.directory + (fp.backslashes ? "\\" : "/")) + Std.string(levelJson.externalRelPath);
					let raw = js_node_Fs.readFileSync(levelPath,{ encoding : "utf8"});
					try {
						levelJson = JSON.parse(raw);
					} catch( _g ) {
						dn_Lib.println("ERROR: " + (levelPath != null ? levelPath + ": " : "") + "Couldn't parse external level");
						process.exit(-1);
						levelJson = null;
					}
				}
				let _g = 0;
				let arr = l.fieldInstances;
				let _g1;
				if(arr == null) {
					_g1 = [];
				} else {
					let _g = Type.typeof(arr);
					_g1 = _g._hx_index == 6 ? _g.c == Array ? arr : [] : [];
				}
				while(_g < _g1.length) {
					let f = _g1[_g];
					++_g;
					if(Object.prototype.hasOwnProperty.call(levelLookup_h,f.__identifier)) {
						if(f.__value == null) {
							continue;
						}
						let e = new dn_data_PoEntry(f.__value);
						all.push(e);
						e.addComment(globalComment);
						e.references.push(levelPath);
						e.addComment("Level_" + Std.string(levelJson.identifier) + "_" + Std.string(f.__identifier));
						++n;
					}
				}
				let _g2 = 0;
				let arr1 = levelJson.layerInstances;
				let _g3;
				if(arr1 == null) {
					_g3 = [];
				} else {
					let _g = Type.typeof(arr1);
					_g3 = _g._hx_index == 6 ? _g.c == Array ? arr1 : [] : [];
				}
				while(_g2 < _g3.length) {
					let layer = _g3[_g2];
					++_g2;
					let type = layer.__type;
					if(type == "Entities") {
						let _g = 0;
						let arr = layer.entityInstances;
						let _g1;
						if(arr == null) {
							_g1 = [];
						} else {
							let _g = Type.typeof(arr);
							_g1 = _g._hx_index == 6 ? _g.c == Array ? arr : [] : [];
						}
						while(_g < _g1.length) {
							let e = _g1[_g];
							++_g;
							if(!Object.prototype.hasOwnProperty.call(entityLookup_h,e.__identifier)) {
								continue;
							}
							let _g2 = 0;
							let arr = e.fieldInstances;
							let _g3;
							if(arr == null) {
								_g3 = [];
							} else {
								let _g = Type.typeof(arr);
								_g3 = _g._hx_index == 6 ? _g.c == Array ? arr : [] : [];
							}
							while(_g2 < _g3.length) {
								let f = _g3[_g2];
								++_g2;
								if(!Object.prototype.hasOwnProperty.call(entityLookup_h[e.__identifier].h,f.__identifier)) {
									continue;
								}
								let arr = e.__grid;
								let pt;
								if(arr == null) {
									pt = [];
								} else {
									let _g = Type.typeof(arr);
									pt = _g._hx_index == 6 ? _g.c == Array ? arr : [] : [];
								}
								let pt1 = Std.string(pt[0]) + "_";
								let arr1 = e.__grid;
								let pt2;
								if(arr1 == null) {
									pt2 = [];
								} else {
									let _g = Type.typeof(arr1);
									pt2 = _g._hx_index == 6 ? _g.c == Array ? arr1 : [] : [];
								}
								let pt3 = pt1 + Std.string(pt2[1]);
								let ctx = "Level_" + Std.string(levelJson.identifier) + "_" + Std.string(e.__identifier);
								let v = f.__value;
								let tmp;
								if(v == null) {
									tmp = false;
								} else {
									let _g = Type.typeof(v);
									tmp = _g._hx_index == 6 && _g.c == Array;
								}
								if(tmp) {
									let i = 0;
									let arr = f.__value;
									let values;
									if(arr == null) {
										values = [];
									} else {
										let _g = Type.typeof(arr);
										values = _g._hx_index == 6 ? _g.c == Array ? arr : [] : [];
									}
									let _g = 0;
									while(_g < values.length) {
										let v = values[_g];
										++_g;
										let e = new dn_data_PoEntry(v);
										all.push(e);
										e.addComment(globalComment);
										e.references.push(levelPath);
										e.addComment(ctx + (values.length > 1 ? "_" + i++ : "") + "_at_" + pt3);
										++n;
									}
								} else {
									let e = new dn_data_PoEntry(f.__value);
									all.push(e);
									e.addComment(globalComment);
									e.references.push(levelPath);
									e.addComment(ctx + "_at_" + pt3);
									++n;
								}
							}
						}
					}
				}
				if(n > 0 && dn_data_GetText.VERBOSE) {
					dn_Lib.println("  - " + levelPath + ", " + n + " entrie(s)");
				}
			}
		}
		if(appendToEntries != null && all != null) {
			let _g = 0;
			while(_g < all.length) {
				let e = all[_g];
				++_g;
				appendToEntries.push(e);
			}
		}
		return all;
	}
	static jsonArray(arr) {
		if(arr == null) {
			return [];
		} else {
			let _g = Type.typeof(arr);
			if(_g._hx_index == 6) {
				if(_g.c == Array) {
					return arr;
				} else {
					return [];
				}
			} else {
				return [];
			}
		}
	}
	static isArray(v) {
		if(v == null) {
			return false;
		} else {
			let _g = Type.typeof(v);
			if(_g._hx_index == 6) {
				if(_g.c == Array) {
					return true;
				} else {
					return false;
				}
			} else {
				return false;
			}
		}
	}
	static parseCastleDB(appendToEntries,filePath,globalComment) {
		if(globalComment == null) {
			globalComment = "CastleDB";
		}
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("");
		}
		if(!sys_FileSystem.exists(filePath)) {
			dn_Lib.println("ERROR: " + (filePath != null ? filePath + ": " : "") + "File not found");
			process.exit(-1);
			return [];
		}
		dn_Lib.println("Parsing CastleDB (" + filePath + ")...");
		let all = [];
		let cbdData = cdb_Parser.parse(js_node_Fs.readFileSync(filePath,{ encoding : "utf8"}),false);
		let columns_h = Object.create(null);
		let _g = 0;
		let _g1 = cbdData.sheets;
		while(_g < _g1.length) {
			let sheet = _g1[_g];
			++_g;
			let p = sheet.name.split("@");
			let sheetName = p.shift();
			if(!Object.prototype.hasOwnProperty.call(columns_h,sheetName)) {
				columns_h[sheetName] = [];
			}
			let sheetColumns = columns_h[sheetName];
			let cid = p;
			let _g2 = 0;
			let _g3 = sheet.columns;
			while(_g2 < _g3.length) {
				let column = _g3[_g2];
				++_g2;
				if(Std.string(column.kind) == "localizable" && column.type == cdb_ColumnType.TString) {
					let p1 = p.slice();
					p1.push(column.name);
					sheetColumns.push(p1);
				}
			}
		}
		let _exploreSheet = null;
		_exploreSheet = function(idx,id,lines,columns) {
			if(lines == null) {
				return;
			}
			let n = 0;
			let i = 0;
			let _g = 0;
			while(_g < lines.length) {
				let line = lines[_g];
				++_g;
				if(line.enabled == false || line.active == false) {
					continue;
				}
				let _g1 = 0;
				while(_g1 < columns.length) {
					let col = columns[_g1];
					++_g1;
					let col1 = col.slice();
					let cname = col1.shift();
					let id1 = id;
					if(line.id != null) {
						id1 += " " + Std.string(line.id);
					}
					id1 += " (" + cname + ")";
					if(col1.length == 0) {
						let e = new dn_data_PoEntry(Reflect.field(line,cname));
						all.push(e);
						e.addComment(globalComment);
						e.references.push(idx + "/#" + i + "." + cname);
						++n;
					} else {
						_exploreSheet(idx + "/#" + i + "." + cname,id1,Reflect.field(line,cname),[col1]);
					}
				}
				++i;
			}
			if(n > 0 && dn_data_GetText.VERBOSE) {
				dn_Lib.println("  - " + idx + ", " + n + " entrie(s)");
			}
		};
		let _g2 = 0;
		let _g3 = cbdData.sheets;
		while(_g2 < _g3.length) {
			let sheet = _g3[_g2];
			++_g2;
			let sColumns = columns_h[sheet.name];
			if(sColumns == null || sColumns.length == 0) {
				continue;
			}
			_exploreSheet(filePath + ":" + sheet.name,"",sheet.lines,sColumns);
		}
		if(appendToEntries != null && all != null) {
			let _g = 0;
			while(_g < all.length) {
				let e = all[_g];
				++_g;
				appendToEntries.push(e);
			}
		}
		return all;
	}
	static error(file,msg) {
		dn_Lib.println("ERROR: " + (file != null ? file + ": " : "") + msg);
		process.exit(-1);
	}
	static removeDuplicates(entries) {
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("Merging duplicates...");
		}
		let dones_h = Object.create(null);
		let i = 0;
		while(i < entries.length) {
			let e = entries[i];
			if(Object.prototype.hasOwnProperty.call(dones_h,e.msgid + (e.contextDisamb == null ? "" : dn_data_GetText.CONTEXT_DISAMB_SEP + e.contextDisamb))) {
				let orig = dones_h[e.msgid + (e.contextDisamb == null ? "" : dn_data_GetText.CONTEXT_DISAMB_SEP + e.contextDisamb)];
				if(e.comment != null) {
					orig.addComment(e.comment);
				}
				if(e.translatorNote != null) {
					orig.addTranslatorNote(e.translatorNote);
				}
				let _g = 0;
				let _g1 = e.references;
				while(_g < _g1.length) {
					let r = _g1[_g];
					++_g;
					orig.references.push(r);
				}
				entries.splice(i,1);
				if(dn_data_GetText.VERBOSE) {
					dn_Lib.println("  - Merged duplicate: " + (e.msgid + (e.contextDisamb == null ? "" : dn_data_GetText.CONTEXT_DISAMB_SEP + e.contextDisamb)));
				}
			} else {
				dones_h[e.msgid + (e.contextDisamb == null ? "" : dn_data_GetText.CONTEXT_DISAMB_SEP + e.contextDisamb)] = e;
				++i;
			}
		}
	}
	static writePOT(potPath,entries) {
		let p = new dn_FilePath();
		p.parse(potPath,true);
		let potPath1 = p;
		dn_data_GetText.removeDuplicates(entries);
		let lines = ["msgid \"\"","msgstr \"\"","\"Content-Type: text/plain; charset=UTF-8\\n\"","\"Content-Transfer-Encoding: 8bit\\n\"","\"MIME-Version: 1.0\\n\"","\n"];
		let _g = 0;
		while(_g < entries.length) {
			let e = entries[_g];
			++_g;
			if(e.comment != null) {
				lines.push("# " + e.comment);
			}
			if(e.translatorNote != null) {
				lines.push("#. " + e.translatorNote);
			}
			let _g1 = 0;
			let _g2 = e.references;
			while(_g1 < _g2.length) {
				let r = _g2[_g1];
				++_g1;
				lines.push("#: \"" + r + "\"");
			}
			if(e.contextDisamb != null) {
				lines.push("msgctxt \"" + dn_data_GetText.escapePoString(e.contextDisamb) + "\"");
			}
			lines.push("msgid \"" + dn_data_GetText.escapePoString(e.msgid) + "\"");
			lines.push("msgstr \"" + dn_data_GetText.escapePoString(e.msgstr) + "\"");
			lines.push("");
		}
		dn_Lib.println("Writing " + (potPath1.fileName == null && potPath1.extension == null ? null : (potPath1.fileName == null ? "" : potPath1.fileName) + (potPath1.extension == null ? "" : "." + potPath1.extension)) + "...");
		let fo = new sys_io_FileOutput(js_node_Fs.openSync((potPath1.isWindowsNetworkDrive && !potPath1._useWinNetDriveUriFormat ? potPath1.backslashes ? "\\" : "/" : potPath1.uriScheme != null ? potPath1.uriAuthority == null ? "" + potPath1.uriScheme + ":/" : "" + potPath1.uriScheme + "://" + potPath1.uriAuthority + "/" : "") + (potPath1.directory == null ? "" : potPath1.fileName == null && potPath1.extension == null || potPath1.directory == (potPath1.backslashes ? "\\" : "/") ? potPath1.directory : potPath1.directory == null ? null : potPath1.directory == (potPath1.backslashes ? "\\" : "/") ? potPath1.directory : potPath1.directory + (potPath1.backslashes ? "\\" : "/")) + ((potPath1.fileName == null && potPath1.extension == null ? null : (potPath1.fileName == null ? "" : potPath1.fileName) + (potPath1.extension == null ? "" : "." + potPath1.extension)) == null ? "" : potPath1.fileName == null && potPath1.extension == null ? null : (potPath1.fileName == null ? "" : potPath1.fileName) + (potPath1.extension == null ? "" : "." + potPath1.extension)),"w"));
		fo.writeString(lines.join("\n"));
		fo.close();
	}
	static listFilesRec(exts,dir) {
		let all = [];
		let pending = [dir];
		while(pending.length > 0) {
			let dir = pending.shift();
			let _g = 0;
			let _g1 = js_node_Fs.readdirSync(dir);
			while(_g < _g1.length) {
				let name = _g1[_g];
				++_g;
				let path = dir + "/" + name;
				if(sys_FileSystem.isDirectory(path)) {
					pending.push(path);
				} else {
					let _g = 0;
					while(_g < exts.length) {
						let e = exts[_g];
						++_g;
						if(name.indexOf("." + e) == name.length - 1 - e.length) {
							all.push(path);
						}
					}
				}
			}
		}
		return all;
	}
}
dn_data_GetText.__name__ = "dn.data.GetText";
Object.assign(dn_data_GetText.prototype, {
	__class__: dn_data_GetText
});
class dn_data_PoEntry {
	constructor(rawId,ctx) {
		this.references = [];
		this.msgstr = "";
		this.msgid = rawId;
		this.contextDisamb = ctx;
		if(dn_data_GetText.TRANSLATOR_NOTE_REG.match(this.msgid)) {
			this.msgid = this.msgid.replace(dn_data_GetText.TRANSLATOR_NOTE_REG.r,"$3");
			this.translatorNote = dn_data_GetText.TRANSLATOR_NOTE_REG.matched(2);
		}
		if(dn_data_GetText.CONTEXT_DISAMB_REG.match(this.msgid)) {
			this.msgid = this.msgid.replace(dn_data_GetText.CONTEXT_DISAMB_REG.r,"$3");
			this.contextDisamb = dn_data_GetText.CONTEXT_DISAMB_REG.matched(2);
		}
		if(dn_data_GetText.COMMENT_REG.match(this.msgid)) {
			this.msgid = this.msgid.replace(dn_data_GetText.COMMENT_REG.r,"$3");
			this.comment = dn_data_GetText.COMMENT_REG.matched(2);
		}
		if(dn_data_GetText.VERBOSE) {
			dn_Lib.println("    - New entry: " + this.msgid);
		}
	}
	addComment(str) {
		if(str != null) {
			if(this.comment == null) {
				this.comment = str;
			} else if(this.comment.indexOf(str) < 0) {
				this.comment += " ; " + str;
			}
		}
	}
	addTranslatorNote(str) {
		if(str != null) {
			if(this.translatorNote == null) {
				this.translatorNote = str;
			} else if(this.translatorNote.indexOf(str) < 0) {
				this.translatorNote += " ; " + str;
			}
		}
	}
	addContextDisambiguation(str) {
		if(str != null) {
			if(this.contextDisamb == null) {
				this.contextDisamb = str;
			} else if(this.contextDisamb.indexOf(str) < 0) {
				this.contextDisamb += " ; " + str;
			}
		}
	}
	get_uniqKey() {
		return this.msgid + (this.contextDisamb == null ? "" : dn_data_GetText.CONTEXT_DISAMB_SEP + this.contextDisamb);
	}
	toString() {
		return this.msgid + (this.contextDisamb == null ? "" : dn_data_GetText.CONTEXT_DISAMB_SEP + this.contextDisamb);
	}
}
dn_data_PoEntry.__name__ = "dn.data.PoEntry";
Object.assign(dn_data_PoEntry.prototype, {
	__class__: dn_data_PoEntry
});
class dn_data_LocaleString {
	static _new(s) {
		return s;
	}
	static toUpperCase(this1) {
		return this1.toUpperCase();
	}
	static toLowerCase(this1) {
		return this1.toLowerCase();
	}
	static charAt(this1,i) {
		return this1.charAt(i);
	}
	static charCodeAt(this1,i) {
		return HxOverrides.cca(this1,i);
	}
	static indexOf(this1,i,s) {
		return this1.indexOf(i,s);
	}
	static lastIndexOf(this1,i,s) {
		return this1.lastIndexOf(i,s);
	}
	static split(this1,i) {
		return this1.split(i);
	}
	static substr(this1,i,l) {
		return HxOverrides.substr(this1,i,l);
	}
	static substring(this1,i,e) {
		return this1.substring(i,e);
	}
	static trim(this1) {
		return StringTools.trim(this1);
	}
	static add(this1,to) {
		return this1 + to;
	}
	static get_length(this1) {
		return this1.length;
	}
}
class dn_debug_MemAlloc {
	constructor() {
		this.calls = 0;
		this.total = 0.;
	}
}
dn_debug_MemAlloc.__name__ = "dn.debug.MemAlloc";
Object.assign(dn_debug_MemAlloc.prototype, {
	__class__: dn_debug_MemAlloc
});
class dn_debug_MemTrack {
	static reset() {
		dn_debug_MemTrack.allocs = new haxe_ds_StringMap();
		dn_debug_MemTrack.firstMeasure = -1;
	}
	static padRight(str,minLen,padChar) {
		if(padChar == null) {
			padChar = " ";
		}
		while(str.length < minLen) str += padChar;
		return str;
	}
	static report(printer,alsoReset) {
		if(alsoReset == null) {
			alsoReset = true;
		}
		let hrtime = process.hrtime();
		let t = hrtime[0] + hrtime[1] / 1e9 - dn_debug_MemTrack.firstMeasure;
		if(printer == null) {
			printer = function(v) {
				console.log("dn/debug/MemTrack.hx:92:",v);
			};
		}
		let all = [];
		let h = dn_debug_MemTrack.allocs.h;
		let a_h = h;
		let a_keys = Object.keys(h);
		let a_length = a_keys.length;
		let a_current = 0;
		while(a_current < a_length) {
			let key = a_keys[a_current++];
			let a_key = key;
			let a_value = a_h[key];
			all.push({ id : a_key, mem : a_value});
		}
		all.sort(function(a,b) {
			return -Reflect.compare(a.mem.total / t,b.mem.total / t);
		});
		if(all.length == 0) {
			printer("MemTrack has nothing to report.");
			return;
		}
		printer("MEMTRACK REPORT");
		let precision = 1;
		if(precision == null) {
			precision = 2;
		}
		let tmp;
		if(precision <= 0) {
			tmp = (t > 0 ? t + .5 : t < 0 ? t - .5 : 0) | 0;
		} else {
			let d = Math.pow(10,precision);
			let x = t * d;
			tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
		}
		printer("Elapsed time: " + tmp + "s");
		let table = [["","MEM/S","TOTAL"]];
		let total = 0.;
		let _g = 0;
		while(_g < all.length) {
			let a = all[_g];
			++_g;
			total += a.mem.total;
			let v = a.mem.total / t;
			let tmp;
			if((v < 0 ? -v : v) < 1000) {
				let precision = 1;
				if(precision == null) {
					precision = 2;
				}
				let tmp1;
				if(precision <= 0) {
					tmp1 = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
				} else {
					let d = Math.pow(10,precision);
					let x = v * d;
					tmp1 = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
				}
				tmp = Std.string(tmp1);
			} else if((v < 0 ? -v : v) < 1000000) {
				let v1 = v / 1000;
				let precision = 1;
				if(precision == null) {
					precision = 2;
				}
				let tmp1;
				if(precision <= 0) {
					tmp1 = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
				} else {
					let d = Math.pow(10,precision);
					let x = v1 * d;
					tmp1 = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
				}
				tmp = tmp1 + "K";
			} else {
				let v1 = v / 1000000;
				let precision = 1;
				if(precision == null) {
					precision = 2;
				}
				let tmp1;
				if(precision <= 0) {
					tmp1 = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
				} else {
					let d = Math.pow(10,precision);
					let x = v1 * d;
					tmp1 = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
				}
				tmp = tmp1 + "M";
			}
			let v1 = a.mem.total;
			let tmp1;
			if((v1 < 0 ? -v1 : v1) < 1000) {
				let precision = 1;
				if(precision == null) {
					precision = 2;
				}
				let tmp;
				if(precision <= 0) {
					tmp = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
				} else {
					let d = Math.pow(10,precision);
					let x = v1 * d;
					tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
				}
				tmp1 = Std.string(tmp);
			} else if((v1 < 0 ? -v1 : v1) < 1000000) {
				let v = v1 / 1000;
				let precision = 1;
				if(precision == null) {
					precision = 2;
				}
				let tmp;
				if(precision <= 0) {
					tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
				} else {
					let d = Math.pow(10,precision);
					let x = v * d;
					tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
				}
				tmp1 = tmp + "K";
			} else {
				let v = v1 / 1000000;
				let precision = 1;
				if(precision == null) {
					precision = 2;
				}
				let tmp;
				if(precision <= 0) {
					tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
				} else {
					let d = Math.pow(10,precision);
					let x = v * d;
					tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
				}
				tmp1 = tmp + "M";
			}
			table.push([a.id,tmp + "/s",tmp1]);
		}
		let colWidths = [];
		let _g1 = 0;
		while(_g1 < table.length) {
			let line = table[_g1];
			++_g1;
			let _g = 0;
			let _g2 = line.length;
			while(_g < _g2) {
				let i = _g++;
				let v = colWidths[i];
				if(!(v != null && !isNaN(v) && isFinite(v))) {
					colWidths[i] = line[i].length;
				} else {
					let x = colWidths[i];
					let y = line[i].length;
					colWidths[i] = x > y ? x : y;
				}
			}
		}
		let line = [];
		let _g2 = 0;
		let _g3 = colWidths.length;
		while(_g2 < _g3) {
			let i = _g2++;
			let str = "";
			let padChar = "-";
			if(padChar == null) {
				padChar = " ";
			}
			while(str.length < colWidths[i]) str += padChar;
			line.push(str);
		}
		table.splice(1,0,line);
		let line1 = [];
		let _g4 = 0;
		let _g5 = colWidths.length;
		while(_g4 < _g5) {
			let i = _g4++;
			let str = "";
			let padChar = "-";
			if(padChar == null) {
				padChar = " ";
			}
			while(str.length < colWidths[i]) str += padChar;
			line1.push(str);
		}
		table.push(line1);
		let v = total / t;
		let tmp1;
		if((v < 0 ? -v : v) < 1000) {
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			tmp1 = Std.string(tmp);
		} else if((v < 0 ? -v : v) < 1000000) {
			let v1 = v / 1000;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v1 * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			tmp1 = tmp + "K";
		} else {
			let v1 = v / 1000000;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v1 > 0 ? v1 + .5 : v1 < 0 ? v1 - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v1 * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			tmp1 = tmp + "M";
		}
		let tmp2;
		if((total < 0 ? -total : total) < 1000) {
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (total > 0 ? total + .5 : total < 0 ? total - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = total * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			tmp2 = Std.string(tmp);
		} else if((total < 0 ? -total : total) < 1000000) {
			let v = total / 1000;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			tmp2 = tmp + "K";
		} else {
			let v = total / 1000000;
			let precision = 1;
			if(precision == null) {
				precision = 2;
			}
			let tmp;
			if(precision <= 0) {
				tmp = (v > 0 ? v + .5 : v < 0 ? v - .5 : 0) | 0;
			} else {
				let d = Math.pow(10,precision);
				let x = v * d;
				tmp = ((x > 0 ? x + .5 : x < 0 ? x - .5 : 0) | 0) / d;
			}
			tmp2 = tmp + "M";
		}
		table.push(["",tmp1 + "/s",tmp2]);
		let _g6 = 0;
		while(_g6 < table.length) {
			let line = table[_g6];
			++_g6;
			let _g = 0;
			let _g1 = line.length;
			while(_g < _g1) {
				let i = _g++;
				let str = line[i];
				while(str.length < colWidths[i]) str += " ";
				line[i] = str;
			}
			printer("| " + line.join(" | ") + " |");
		}
		if(alsoReset) {
			dn_debug_MemTrack.reset();
		}
	}
}
dn_debug_MemTrack.__name__ = "dn.debug.MemTrack";
class dn_struct__$FixedArray_FixedArrayIterator {
	constructor(arr) {
		this.arr = arr;
		this.i = 0;
	}
	hasNext() {
		return this.i < this.arr.nalloc;
	}
	next() {
		return this.arr.values[this.i++];
	}
}
dn_struct__$FixedArray_FixedArrayIterator.__name__ = "dn.struct._FixedArray.FixedArrayIterator";
Object.assign(dn_struct__$FixedArray_FixedArrayIterator.prototype, {
	__class__: dn_struct__$FixedArray_FixedArrayIterator
});
class dn_struct_RecyclablePool {
	constructor(size,valueConstructor) {
		this.nalloc = 0;
		this.size = size;
		this.pool = new Array(size);
		let _g = 0;
		let _g1 = this.pool.length;
		while(_g < _g1) {
			let i = _g++;
			this.pool[i] = valueConstructor();
		}
	}
	get_allocated() {
		return this.nalloc;
	}
	get_maxSize() {
		return this.size;
	}
	toString() {
		return "RecyclablePool(" + this.nalloc + "/" + this.size + ")";
	}
	debugContent() {
		let idx = 0;
		let _gthis = this;
		let tmp = this.toString() + " => ";
		let this1 = this.pool;
		let length = this1.length;
		let r = new Array(length);
		let len = length;
		let _g = 0;
		let _g1 = len;
		while(_g < _g1) {
			let i = _g++;
			let v = this1[i];
			let v1 = idx++ < _gthis.nalloc ? Std.string(v) : "{deleted:" + Std.string(v) + "}";
			r[i] = v1;
		}
		return tmp + Std.string(r);
	}
	get(index) {
		if(index < 0 || index >= this.nalloc) {
			return null;
		} else {
			return this.pool[index];
		}
	}
	getUnsafe(index) {
		return this.pool[index];
	}
	dispose(elementDisposer) {
		if(elementDisposer != null) {
			let _g = 0;
			let _g1 = this.pool;
			while(_g < _g1.length) {
				let e = _g1[_g];
				++_g;
				elementDisposer(e);
			}
		}
		this.nalloc = 0;
		this.pool = null;
	}
	alloc() {
		if(this.nalloc >= this.size) {
			this.garbageCollectNow();
			if(this.nalloc >= this.size) {
				throw haxe_Exception.thrown("RecyclablePool limit reached (" + this.size + ")");
			}
		}
		let e = this.pool[this.nalloc++];
		e.recycle();
		return e;
	}
	canBeGarbageCollected(v) {
		return false;
	}
	garbageCollectNow() {
		let i = 0;
		while(i < this.nalloc) if(this.canBeGarbageCollected(i < 0 || i >= this.nalloc ? null : this.pool[i])) {
			if(i >= 0 && i < this.nalloc) {
				if(i == this.nalloc - 1) {
					this.nalloc--;
				} else {
					let tmp = this.pool[i];
					this.pool[i] = this.pool[this.nalloc - 1];
					this.pool[this.nalloc - 1] = tmp;
					this.nalloc--;
				}
			}
		} else {
			++i;
		}
	}
	freeAll() {
		this.nalloc = 0;
	}
	freeIndex(i) {
		if(i >= 0 && i < this.nalloc) {
			if(i == this.nalloc - 1) {
				this.nalloc--;
			} else {
				let tmp = this.pool[i];
				this.pool[i] = this.pool[this.nalloc - 1];
				this.pool[this.nalloc - 1] = tmp;
				this.nalloc--;
			}
		}
	}
	freeElement(search) {
		let _g = 0;
		let _g1 = this.nalloc;
		while(_g < _g1) {
			let i = _g++;
			if(this.pool[i] == search) {
				if(i >= 0 && i < this.nalloc) {
					if(i == this.nalloc - 1) {
						this.nalloc--;
					} else {
						let tmp = this.pool[i];
						this.pool[i] = this.pool[this.nalloc - 1];
						this.pool[this.nalloc - 1] = tmp;
						this.nalloc--;
					}
				}
				break;
			}
		}
	}
	iterator() {
		return new dn_struct__$RecyclablePool_RecyclablePoolIterator(this);
	}
}
dn_struct_RecyclablePool.__name__ = "dn.struct.RecyclablePool";
Object.assign(dn_struct_RecyclablePool.prototype, {
	__class__: dn_struct_RecyclablePool
});
class dn_struct__$RecyclablePool_UnitTestObject {
	constructor(id) {
		this.id = id;
		this.value = -1;
	}
	toString() {
		return "#" + this.id + "=" + Std.string(this.value);
	}
	recycle() {
		this.value = Std.random(999999);
	}
}
dn_struct__$RecyclablePool_UnitTestObject.__name__ = "dn.struct._RecyclablePool.UnitTestObject";
Object.assign(dn_struct__$RecyclablePool_UnitTestObject.prototype, {
	__class__: dn_struct__$RecyclablePool_UnitTestObject
});
class dn_struct__$RecyclablePool_RecyclablePoolIterator {
	constructor(r) {
		this.rpool = r;
		this.i = 0;
	}
	hasNext() {
		return this.i < this.rpool.nalloc;
	}
	next() {
		return this.rpool.pool[this.i++];
	}
}
dn_struct__$RecyclablePool_RecyclablePoolIterator.__name__ = "dn.struct._RecyclablePool.RecyclablePoolIterator";
Object.assign(dn_struct__$RecyclablePool_RecyclablePoolIterator.prototype, {
	__class__: dn_struct__$RecyclablePool_RecyclablePoolIterator
});
class haxe_Exception extends Error {
	constructor(message,previous,native) {
		super(message);
		this.message = message;
		this.__previousException = previous;
		this.__nativeException = native != null ? native : this;
	}
	unwrap() {
		return this.__nativeException;
	}
	toString() {
		return this.get_message();
	}
	get_message() {
		return this.message;
	}
	get_native() {
		return this.__nativeException;
	}
	static caught(value) {
		if(((value) instanceof haxe_Exception)) {
			return value;
		} else if(((value) instanceof Error)) {
			return new haxe_Exception(value.message,null,value);
		} else {
			return new haxe_ValueException(value,null,value);
		}
	}
	static thrown(value) {
		if(((value) instanceof haxe_Exception)) {
			return value.get_native();
		} else if(((value) instanceof Error)) {
			return value;
		} else {
			let e = new haxe_ValueException(value);
			return e;
		}
	}
}
haxe_Exception.__name__ = "haxe.Exception";
Object.assign(haxe_Exception.prototype, {
	__class__: haxe_Exception
});
class haxe_ValueException extends haxe_Exception {
	constructor(value,previous,native) {
		super(String(value),previous,native);
		this.value = value;
	}
	unwrap() {
		return this.value;
	}
}
haxe_ValueException.__name__ = "haxe.ValueException";
Object.assign(haxe_ValueException.prototype, {
	__class__: haxe_ValueException
});
class haxe_ds_ArraySort {
	static sort(a,cmp) {
		haxe_ds_ArraySort.rec(a,cmp,0,a.length);
	}
	static rec(a,cmp,from,to) {
		let middle = from + to >> 1;
		if(to - from < 12) {
			if(to <= from) {
				return;
			}
			let _g = from + 1;
			let _g1 = to;
			while(_g < _g1) {
				let i = _g++;
				let j = i;
				while(j > from) {
					if(cmp(a[j],a[j - 1]) < 0) {
						haxe_ds_ArraySort.swap(a,j - 1,j);
					} else {
						break;
					}
					--j;
				}
			}
			return;
		}
		haxe_ds_ArraySort.rec(a,cmp,from,middle);
		haxe_ds_ArraySort.rec(a,cmp,middle,to);
		haxe_ds_ArraySort.doMerge(a,cmp,from,middle,to,middle - from,to - middle);
	}
	static doMerge(a,cmp,from,pivot,to,len1,len2) {
		let first_cut;
		let second_cut;
		let len11;
		let len22;
		if(len1 == 0 || len2 == 0) {
			return;
		}
		if(len1 + len2 == 2) {
			if(cmp(a[pivot],a[from]) < 0) {
				haxe_ds_ArraySort.swap(a,pivot,from);
			}
			return;
		}
		if(len1 > len2) {
			len11 = len1 >> 1;
			first_cut = from + len11;
			second_cut = haxe_ds_ArraySort.lower(a,cmp,pivot,to,first_cut);
			len22 = second_cut - pivot;
		} else {
			len22 = len2 >> 1;
			second_cut = pivot + len22;
			first_cut = haxe_ds_ArraySort.upper(a,cmp,from,pivot,second_cut);
			len11 = first_cut - from;
		}
		haxe_ds_ArraySort.rotate(a,cmp,first_cut,pivot,second_cut);
		let new_mid = first_cut + len22;
		haxe_ds_ArraySort.doMerge(a,cmp,from,first_cut,new_mid,len11,len22);
		haxe_ds_ArraySort.doMerge(a,cmp,new_mid,second_cut,to,len1 - len11,len2 - len22);
	}
	static rotate(a,cmp,from,mid,to) {
		if(from == mid || mid == to) {
			return;
		}
		let n = haxe_ds_ArraySort.gcd(to - from,mid - from);
		while(n-- != 0) {
			let val = a[from + n];
			let shift = mid - from;
			let p1 = from + n;
			let p2 = from + n + shift;
			while(p2 != from + n) {
				a[p1] = a[p2];
				p1 = p2;
				if(to - p2 > shift) {
					p2 += shift;
				} else {
					p2 = from + (shift - (to - p2));
				}
			}
			a[p1] = val;
		}
	}
	static gcd(m,n) {
		while(n != 0) {
			let t = m % n;
			m = n;
			n = t;
		}
		return m;
	}
	static upper(a,cmp,from,to,val) {
		let len = to - from;
		let half;
		let mid;
		while(len > 0) {
			half = len >> 1;
			mid = from + half;
			if(cmp(a[val],a[mid]) < 0) {
				len = half;
			} else {
				from = mid + 1;
				len = len - half - 1;
			}
		}
		return from;
	}
	static lower(a,cmp,from,to,val) {
		let len = to - from;
		let half;
		let mid;
		while(len > 0) {
			half = len >> 1;
			mid = from + half;
			if(cmp(a[mid],a[val]) < 0) {
				from = mid + 1;
				len = len - half - 1;
			} else {
				len = half;
			}
		}
		return from;
	}
	static swap(a,i,j) {
		let tmp = a[i];
		a[i] = a[j];
		a[j] = tmp;
	}
}
haxe_ds_ArraySort.__name__ = "haxe.ds.ArraySort";
class haxe_ds_IntMap {
	constructor() {
		this.h = { };
	}
	remove(key) {
		if(!this.h.hasOwnProperty(key)) {
			return false;
		}
		delete(this.h[key]);
		return true;
	}
}
haxe_ds_IntMap.__name__ = "haxe.ds.IntMap";
Object.assign(haxe_ds_IntMap.prototype, {
	__class__: haxe_ds_IntMap
});
class haxe_ds_StringMap {
	constructor() {
		this.h = Object.create(null);
	}
	iterator() {
		return new haxe_ds__$StringMap_StringMapValueIterator(this.h);
	}
}
haxe_ds_StringMap.__name__ = "haxe.ds.StringMap";
Object.assign(haxe_ds_StringMap.prototype, {
	__class__: haxe_ds_StringMap
});
class haxe_ds__$StringMap_StringMapValueIterator {
	constructor(h) {
		this.h = h;
		this.keys = Object.keys(h);
		this.length = this.keys.length;
		this.current = 0;
	}
	hasNext() {
		return this.current < this.length;
	}
	next() {
		return this.h[this.keys[this.current++]];
	}
}
haxe_ds__$StringMap_StringMapValueIterator.__name__ = "haxe.ds._StringMap.StringMapValueIterator";
Object.assign(haxe_ds__$StringMap_StringMapValueIterator.prototype, {
	__class__: haxe_ds__$StringMap_StringMapValueIterator
});
class haxe_exceptions_PosException extends haxe_Exception {
	constructor(message,previous,pos) {
		super(message,previous);
		if(pos == null) {
			this.posInfos = { fileName : "(unknown)", lineNumber : 0, className : "(unknown)", methodName : "(unknown)"};
		} else {
			this.posInfos = pos;
		}
	}
	toString() {
		return "" + super.toString() + " in " + this.posInfos.className + "." + this.posInfos.methodName + " at " + this.posInfos.fileName + ":" + this.posInfos.lineNumber;
	}
}
haxe_exceptions_PosException.__name__ = "haxe.exceptions.PosException";
Object.assign(haxe_exceptions_PosException.prototype, {
	__class__: haxe_exceptions_PosException
});
class haxe_exceptions_NotImplementedException extends haxe_exceptions_PosException {
	constructor(message,previous,pos) {
		if(message == null) {
			message = "Not implemented";
		}
		super(message,previous,pos);
	}
}
haxe_exceptions_NotImplementedException.__name__ = "haxe.exceptions.NotImplementedException";
Object.assign(haxe_exceptions_NotImplementedException.prototype, {
	__class__: haxe_exceptions_NotImplementedException
});
class haxe_io_Bytes {
	constructor(data) {
		this.length = data.byteLength;
		this.b = new Uint8Array(data);
		this.b.bufferValue = data;
		data.hxBytes = this;
		data.bytes = this.b;
	}
	getString(pos,len,encoding) {
		if(pos < 0 || len < 0 || pos + len > this.length) {
			throw haxe_Exception.thrown(haxe_io_Error.OutsideBounds);
		}
		if(encoding == null) {
			encoding = haxe_io_Encoding.UTF8;
		}
		let s = "";
		let b = this.b;
		let i = pos;
		let max = pos + len;
		switch(encoding._hx_index) {
		case 0:
			let debug = pos > 0;
			while(i < max) {
				let c = b[i++];
				if(c < 128) {
					if(c == 0) {
						break;
					}
					s += String.fromCodePoint(c);
				} else if(c < 224) {
					let code = (c & 63) << 6 | b[i++] & 127;
					s += String.fromCodePoint(code);
				} else if(c < 240) {
					let c2 = b[i++];
					let code = (c & 31) << 12 | (c2 & 127) << 6 | b[i++] & 127;
					s += String.fromCodePoint(code);
				} else {
					let c2 = b[i++];
					let c3 = b[i++];
					let u = (c & 15) << 18 | (c2 & 127) << 12 | (c3 & 127) << 6 | b[i++] & 127;
					s += String.fromCodePoint(u);
				}
			}
			break;
		case 1:
			while(i < max) {
				let c = b[i++] | b[i++] << 8;
				s += String.fromCodePoint(c);
			}
			break;
		}
		return s;
	}
	toString() {
		return this.getString(0,this.length);
	}
	static ofString(s,encoding) {
		if(encoding == haxe_io_Encoding.RawNative) {
			let buf = new Uint8Array(s.length << 1);
			let _g = 0;
			let _g1 = s.length;
			while(_g < _g1) {
				let i = _g++;
				let c = s.charCodeAt(i);
				buf[i << 1] = c & 255;
				buf[i << 1 | 1] = c >> 8;
			}
			return new haxe_io_Bytes(buf.buffer);
		}
		let a = [];
		let i = 0;
		while(i < s.length) {
			let c = s.charCodeAt(i++);
			if(55296 <= c && c <= 56319) {
				c = c - 55232 << 10 | s.charCodeAt(i++) & 1023;
			}
			if(c <= 127) {
				a.push(c);
			} else if(c <= 2047) {
				a.push(192 | c >> 6);
				a.push(128 | c & 63);
			} else if(c <= 65535) {
				a.push(224 | c >> 12);
				a.push(128 | c >> 6 & 63);
				a.push(128 | c & 63);
			} else {
				a.push(240 | c >> 18);
				a.push(128 | c >> 12 & 63);
				a.push(128 | c >> 6 & 63);
				a.push(128 | c & 63);
			}
		}
		return new haxe_io_Bytes(new Uint8Array(a).buffer);
	}
}
haxe_io_Bytes.__name__ = "haxe.io.Bytes";
Object.assign(haxe_io_Bytes.prototype, {
	__class__: haxe_io_Bytes
});
var haxe_io_Encoding = $hxEnums["haxe.io.Encoding"] = { __ename__:true,__constructs__:null
	,UTF8: {_hx_name:"UTF8",_hx_index:0,__enum__:"haxe.io.Encoding",toString:$estr}
	,RawNative: {_hx_name:"RawNative",_hx_index:1,__enum__:"haxe.io.Encoding",toString:$estr}
};
haxe_io_Encoding.__constructs__ = [haxe_io_Encoding.UTF8,haxe_io_Encoding.RawNative];
class haxe_io_Eof {
	constructor() {
	}
	toString() {
		return "Eof";
	}
}
haxe_io_Eof.__name__ = "haxe.io.Eof";
Object.assign(haxe_io_Eof.prototype, {
	__class__: haxe_io_Eof
});
var haxe_io_Error = $hxEnums["haxe.io.Error"] = { __ename__:true,__constructs__:null
	,Blocked: {_hx_name:"Blocked",_hx_index:0,__enum__:"haxe.io.Error",toString:$estr}
	,Overflow: {_hx_name:"Overflow",_hx_index:1,__enum__:"haxe.io.Error",toString:$estr}
	,OutsideBounds: {_hx_name:"OutsideBounds",_hx_index:2,__enum__:"haxe.io.Error",toString:$estr}
	,Custom: ($_=function(e) { return {_hx_index:3,e:e,__enum__:"haxe.io.Error",toString:$estr}; },$_._hx_name="Custom",$_.__params__ = ["e"],$_)
};
haxe_io_Error.__constructs__ = [haxe_io_Error.Blocked,haxe_io_Error.Overflow,haxe_io_Error.OutsideBounds,haxe_io_Error.Custom];
class haxe_iterators_ArrayIterator {
	constructor(array) {
		this.current = 0;
		this.array = array;
	}
	hasNext() {
		return this.current < this.array.length;
	}
	next() {
		return this.array[this.current++];
	}
}
haxe_iterators_ArrayIterator.__name__ = "haxe.iterators.ArrayIterator";
Object.assign(haxe_iterators_ArrayIterator.prototype, {
	__class__: haxe_iterators_ArrayIterator
});
class haxe_rtti_Meta {
	static getType(t) {
		let meta = haxe_rtti_Meta.getMeta(t);
		if(meta == null || meta.obj == null) {
			return { };
		} else {
			return meta.obj;
		}
	}
	static getMeta(t) {
		return t.__meta__;
	}
	static getFields(t) {
		let meta = haxe_rtti_Meta.getMeta(t);
		if(meta == null || meta.fields == null) {
			return { };
		} else {
			return meta.fields;
		}
	}
}
haxe_rtti_Meta.__name__ = "haxe.rtti.Meta";
class js_Boot {
	static getClass(o) {
		if(o == null) {
			return null;
		} else if(((o) instanceof Array)) {
			return Array;
		} else {
			let cl = o.__class__;
			if(cl != null) {
				return cl;
			}
			let name = js_Boot.__nativeClassName(o);
			if(name != null) {
				return js_Boot.__resolveNativeClass(name);
			}
			return null;
		}
	}
	static __string_rec(o,s) {
		if(o == null) {
			return "null";
		}
		if(s.length >= 5) {
			return "<...>";
		}
		let t = typeof(o);
		if(t == "function" && (o.__name__ || o.__ename__)) {
			t = "object";
		}
		switch(t) {
		case "function":
			return "<function>";
		case "object":
			if(o.__enum__) {
				let e = $hxEnums[o.__enum__];
				let con = e.__constructs__[o._hx_index];
				let n = con._hx_name;
				if(con.__params__) {
					s = s + "\t";
					return n + "(" + ((function($this) {
						var $r;
						let _g = [];
						{
							let _g1 = 0;
							let _g2 = con.__params__;
							while(true) {
								if(!(_g1 < _g2.length)) {
									break;
								}
								let p = _g2[_g1];
								_g1 = _g1 + 1;
								_g.push(js_Boot.__string_rec(o[p],s));
							}
						}
						$r = _g;
						return $r;
					}(this))).join(",") + ")";
				} else {
					return n;
				}
			}
			if(((o) instanceof Array)) {
				let str = "[";
				s += "\t";
				let _g = 0;
				let _g1 = o.length;
				while(_g < _g1) {
					let i = _g++;
					str += (i > 0 ? "," : "") + js_Boot.__string_rec(o[i],s);
				}
				str += "]";
				return str;
			}
			let tostr;
			try {
				tostr = o.toString;
			} catch( _g ) {
				return "???";
			}
			if(tostr != null && tostr != Object.toString && typeof(tostr) == "function") {
				let s2 = o.toString();
				if(s2 != "[object Object]") {
					return s2;
				}
			}
			let str = "{\n";
			s += "\t";
			let hasp = o.hasOwnProperty != null;
			let k = null;
			for( k in o ) {
			if(hasp && !o.hasOwnProperty(k)) {
				continue;
			}
			if(k == "prototype" || k == "__class__" || k == "__super__" || k == "__interfaces__" || k == "__properties__") {
				continue;
			}
			if(str.length != 2) {
				str += ", \n";
			}
			str += s + k + " : " + js_Boot.__string_rec(o[k],s);
			}
			s = s.substring(1);
			str += "\n" + s + "}";
			return str;
		case "string":
			return o;
		default:
			return String(o);
		}
	}
	static __nativeClassName(o) {
		let name = js_Boot.__toStr.call(o).slice(8,-1);
		if(name == "Object" || name == "Function" || name == "Math" || name == "JSON") {
			return null;
		}
		return name;
	}
	static __resolveNativeClass(name) {
		return $global[name];
	}
}
js_Boot.__name__ = "js.Boot";
var js_node_Fs = require("fs");
class js_node_KeyValue {
	static get_key(this1) {
		return this1[0];
	}
	static get_value(this1) {
		return this1[1];
	}
}
var js_node_buffer_Buffer = require("buffer").Buffer;
class js_node_buffer__$Buffer_Helper {
	static bytesOfBuffer(b) {
		let o = Object.create(haxe_io_Bytes.prototype);
		o.length = b.byteLength;
		o.b = b;
		b.bufferValue = b;
		b.hxBytes = o;
		b.bytes = b;
		return o;
	}
}
js_node_buffer__$Buffer_Helper.__name__ = "js.node.buffer._Buffer.Helper";
class js_node_stream_WritableNewOptionsAdapter {
	static from(options) {
		if(!Object.prototype.hasOwnProperty.call(options,"final")) {
			Object.defineProperty(options,"final",{ get : function() {
				return options.final_;
			}});
		}
		return options;
	}
}
class js_node_url_URLSearchParamsEntry {
	static _new(name,value) {
		return [name,value];
	}
	static get_name(this1) {
		return this1[0];
	}
	static get_value(this1) {
		return this1[1];
	}
}
class sys_FileSystem {
	static exists(path) {
		try {
			js_node_Fs.accessSync(path);
			return true;
		} catch( _g ) {
			return false;
		}
	}
	static isDirectory(path) {
		try {
			return js_node_Fs.statSync(path).isDirectory();
		} catch( _g ) {
			return false;
		}
	}
}
sys_FileSystem.__name__ = "sys.FileSystem";
class sys_io_FileInput extends haxe_io_Input {
	constructor(fd) {
		super();
		this.hasReachedEof = false;
		this.fd = fd;
		this.pos = 0;
	}
	throwEof() {
		this.hasReachedEof = true;
		throw haxe_Exception.thrown(new haxe_io_Eof());
	}
	readByte() {
		let buf = js_node_buffer_Buffer.alloc(1);
		let bytesRead;
		try {
			bytesRead = js_node_Fs.readSync(this.fd,buf,0,1,this.pos);
		} catch( _g ) {
			let e = haxe_Exception.caught(_g).unwrap();
			if(e.code == "EOF") {
				this.hasReachedEof = true;
				throw haxe_Exception.thrown(new haxe_io_Eof());
			}
			throw haxe_Exception.thrown(haxe_io_Error.Custom(e));
		}
		if(bytesRead == 0) {
			this.hasReachedEof = true;
			throw haxe_Exception.thrown(new haxe_io_Eof());
		}
		this.pos++;
		return buf[0];
	}
	readBytes(s,pos,len) {
		let data = s.b;
		let buf = js_node_buffer_Buffer.from(data.buffer,data.byteOffset,s.length);
		let bytesRead;
		try {
			bytesRead = js_node_Fs.readSync(this.fd,buf,pos,len,this.pos);
		} catch( _g ) {
			let e = haxe_Exception.caught(_g).unwrap();
			if(e.code == "EOF") {
				this.hasReachedEof = true;
				throw haxe_Exception.thrown(new haxe_io_Eof());
			}
			throw haxe_Exception.thrown(haxe_io_Error.Custom(e));
		}
		if(bytesRead == 0) {
			this.hasReachedEof = true;
			throw haxe_Exception.thrown(new haxe_io_Eof());
		}
		this.pos += bytesRead;
		return bytesRead;
	}
	close() {
		js_node_Fs.closeSync(this.fd);
	}
	seek(p,pos) {
		this.hasReachedEof = false;
		switch(pos._hx_index) {
		case 0:
			this.pos = p;
			break;
		case 1:
			this.pos += p;
			break;
		case 2:
			this.pos = js_node_Fs.fstatSync(this.fd).size + p;
			break;
		}
	}
	tell() {
		return this.pos;
	}
	eof() {
		return this.hasReachedEof;
	}
}
sys_io_FileInput.__name__ = "sys.io.FileInput";
Object.assign(sys_io_FileInput.prototype, {
	__class__: sys_io_FileInput
});
class sys_io_FileOutput extends haxe_io_Output {
	constructor(fd) {
		super();
		this.fd = fd;
		this.pos = 0;
	}
	writeByte(b) {
		let buf = js_node_buffer_Buffer.alloc(1);
		buf[0] = b;
		js_node_Fs.writeSync(this.fd,buf,0,1,this.pos);
		this.pos++;
	}
	writeBytes(s,pos,len) {
		let data = s.b;
		let buf = js_node_buffer_Buffer.from(data.buffer,data.byteOffset,s.length);
		let wrote = js_node_Fs.writeSync(this.fd,buf,pos,len,this.pos);
		this.pos += wrote;
		return wrote;
	}
	close() {
		js_node_Fs.closeSync(this.fd);
	}
	seek(p,pos) {
		switch(pos._hx_index) {
		case 0:
			this.pos = p;
			break;
		case 1:
			this.pos += p;
			break;
		case 2:
			this.pos = js_node_Fs.fstatSync(this.fd).size + p;
			break;
		}
	}
	tell() {
		return this.pos;
	}
}
sys_io_FileOutput.__name__ = "sys.io.FileOutput";
Object.assign(sys_io_FileOutput.prototype, {
	__class__: sys_io_FileOutput
});
var sys_io_FileSeek = $hxEnums["sys.io.FileSeek"] = { __ename__:true,__constructs__:null
	,SeekBegin: {_hx_name:"SeekBegin",_hx_index:0,__enum__:"sys.io.FileSeek",toString:$estr}
	,SeekCur: {_hx_name:"SeekCur",_hx_index:1,__enum__:"sys.io.FileSeek",toString:$estr}
	,SeekEnd: {_hx_name:"SeekEnd",_hx_index:2,__enum__:"sys.io.FileSeek",toString:$estr}
};
sys_io_FileSeek.__constructs__ = [sys_io_FileSeek.SeekBegin,sys_io_FileSeek.SeekCur,sys_io_FileSeek.SeekEnd];
function $getIterator(o) { if( o instanceof Array ) return new haxe_iterators_ArrayIterator(o); else return o.iterator(); }
function $bind(o,m) { if( m == null ) return null; if( m.__id__ == null ) m.__id__ = $global.$haxeUID++; var f; if( o.hx__closures__ == null ) o.hx__closures__ = {}; else f = o.hx__closures__[m.__id__]; if( f == null ) { f = m.bind(o); o.hx__closures__[m.__id__] = f; } return f; }
$global.$haxeUID |= 0;
if(typeof(performance) != "undefined" ? typeof(performance.now) == "function" : false) {
	HxOverrides.now = performance.now.bind(performance);
}
if( String.fromCodePoint == null ) String.fromCodePoint = function(c) { return c < 0x10000 ? String.fromCharCode(c) : String.fromCharCode((c>>10)+0xD7C0)+String.fromCharCode((c&0x3FF)+0xDC00); }
{
	Object.defineProperty(String.prototype,"__class__",{ value : String, enumerable : false, writable : true});
	String.__name__ = "String";
	Array.__name__ = "Array";
	Date.prototype.__class__ = Date;
	Date.__name__ = "Date";
}
js_Boot.__toStr = ({ }).toString;
DateTools.DAY_SHORT_NAMES = ["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];
DateTools.DAY_NAMES = ["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"];
DateTools.MONTH_SHORT_NAMES = ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"];
DateTools.MONTH_NAMES = ["January","February","March","April","May","June","July","August","September","October","November","December"];
cdb_TileMode.Tile = "tile";
cdb_TileMode.Ground = "ground";
cdb_TileMode.Border = "border";
cdb_TileMode.Object = "object";
cdb_TileMode.Group = "group";
dn_Cooldown.DEFAULT_COUNT_LIMIT = 512;
dn_FilePath.WIN_NETWORK_DRIVE_REG = new EReg("^\\\\\\\\([a-z0-9-]+)\\\\(.*)","i");
dn_FilePath.SLASH_MODE = dn_PathSlashMode.Preserve;
dn_Lib.L_WHITESPACE_TRIM = new EReg("^\\s*","gi");
dn_Lib.R_WHITESPACE_TRIM = new EReg("\\s*$","gi");
dn_Lib.VOWELS = (function($this) {
	var $r;
	let _g = new haxe_ds_StringMap();
	_g.h["a"] = true;
	_g.h["e"] = true;
	_g.h["i"] = true;
	_g.h["o"] = true;
	_g.h["u"] = true;
	_g.h["y"] = true;
	$r = _g;
	return $r;
}(this));
dn_M.T_INT8_MIN = -128;
dn_M.T_INT8_MAX = 127;
dn_M.T_UINT8_MAX = 255;
dn_M.T_INT16_MIN = -32768;
dn_M.T_INT16_MAX = 32767;
dn_M.T_UINT16_MAX = 65535;
dn_M.T_INT32_MIN = -2147483648;
dn_M.T_INT32_MAX = 2147483647;
dn_M.T_UINT32_MAX = -1;
dn_M.T_INT_BITS = 32;
dn_M.T_FLOAT_MAX = 3.4028234663852886e+38;
dn_M.T_FLOAT_MIN = -3.4028234663852886e+38;
dn_M.T_DOUBLE_MAX = 1.7976931348623157e+308;
dn_M.T_DOUBLE_MIN = -1.7976931348623157e+308;
dn_M.RAD_DEG = 57.2957795130823229;
dn_M.DEG_RAD = 0.0174532925199432955;
dn_M.LN2 = 0.6931471805599453;
dn_M.PIHALF = 1.5707963267948966;
dn_M.PI = 3.141592653589793;
dn_M.PI2 = 6.283185307179586;
dn_M.A360 = 6.283185307179586;
dn_M.A180 = 3.141592653589793;
dn_M.A90 = 1.5707963267948966;
dn_M.A45 = 0.785398163397448;
dn_M.EPS = 1e-6;
dn_M.SQRT2 = 1.414213562373095;
dn_Process.MAX_PROCESSES = 1024;
dn_Process.FIXED_UPDATE_FPS = 30;
dn_Process.CUSTOM_STAGE_WIDTH = -1;
dn_Process.CUSTOM_STAGE_HEIGHT = -1;
dn_Process.UNIQ_ID = 0;
dn_Process.ROOTS = new dn_struct_FixedArray("RootProcesses",dn_Process.MAX_PROCESSES);
dn_Process.BEGINNING_OF_FRAME_CALLBACKS = new dn_struct_FixedArray(null,256);
dn_Process.END_OF_FRAME_CALLBACKS = new dn_struct_FixedArray(null,256);
dn_Process.RESIZE_REQUESTED = true;
dn_Process.PROFILING = false;
dn_Process.PROFILER_TIMES = new haxe_ds_StringMap();
dn_Tweenie.DEFAULT_DURATION = 1000.0;
dn_data_GetText.VERBOSE = false;
dn_data_GetText.CONTEXT_DISAMB_SEP = "||@";
dn_data_GetText.COMMENT_REG = new EReg("(\\|\\|\\?(.*?))($|\\|\\|)","i");
dn_data_GetText.CONTEXT_DISAMB_REG = new EReg("(\\|\\|@(.*?))($|\\|\\|)","i");
dn_data_GetText.TRANSLATOR_NOTE_REG = new EReg("(\\|\\|!(.*?))($|\\|\\|)","i");
dn_data_GetText.VDF_ACHIEVEMENT_CONTEXT = "vdfAchievement#";
dn_data_GetText.SRC_REG = new EReg("\\._\\(\\s*\"((\\\\\"|[^\"])+)\"","i");
dn_debug_MemTrack.allocs = new haxe_ds_StringMap();
dn_debug_MemTrack.firstMeasure = -1.;
LangParser.main();
})(typeof window != "undefined" ? window : typeof global != "undefined" ? global : typeof self != "undefined" ? self : this);
